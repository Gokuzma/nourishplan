---
phase: 01-foundation-auth
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/contexts/AuthContext.tsx
  - src/hooks/useAuth.ts
  - src/pages/AuthPage.tsx
  - src/components/auth/AuthForm.tsx
  - src/components/auth/ResetModal.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/TabBar.tsx
  - src/components/layout/Sidebar.tsx
  - src/App.tsx
  - src/main.tsx
  - tests/auth.test.ts
  - tests/AuthContext.test.tsx
  - tests/AppShell.test.tsx
  - tests/theme.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - PLAT-01

must_haves:
  truths:
    - "User can create an account with email and password"
    - "User can log in with email and password"
    - "User can sign in with Google OAuth"
    - "User stays logged in across browser sessions"
    - "User can log out from any page"
    - "User can request a password reset via inline modal"
    - "Bottom tab bar shows on mobile viewports"
    - "Sidebar navigation shows on desktop viewports (768px+)"
  artifacts:
    - path: "src/contexts/AuthContext.tsx"
      provides: "Session state management via onAuthStateChange"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/pages/AuthPage.tsx"
      provides: "Login/signup toggle page with reset modal"
      min_lines: 40
    - path: "src/components/layout/AppShell.tsx"
      provides: "Responsive shell with tabs/sidebar"
      min_lines: 30
  key_links:
    - from: "src/contexts/AuthContext.tsx"
      to: "src/lib/supabase.ts"
      via: "onAuthStateChange subscription"
      pattern: "onAuthStateChange"
    - from: "src/pages/AuthPage.tsx"
      to: "src/lib/supabase.ts"
      via: "signUp, signInWithPassword, signInWithOAuth"
      pattern: "supabase\\.auth\\.(signUp|signInWith)"
    - from: "src/App.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "AuthProvider wrapper"
      pattern: "AuthProvider"
    - from: "src/components/layout/AppShell.tsx"
      to: "src/components/layout/TabBar.tsx"
      via: "mobile tab rendering"
      pattern: "TabBar"
---

<objective>
Build the complete auth system (signup, login, Google OAuth, logout, password reset) and the responsive app shell (bottom tabs on mobile, sidebar on desktop).

Purpose: Auth is the gateway to all features. The app shell provides the navigation structure every subsequent page lives inside.
Output: Working auth flow + responsive navigation shell.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — types and client the executor needs -->

From src/lib/supabase.ts:
```typescript
export const supabase: SupabaseClient
```

From src/types/database.ts:
```typescript
export interface Profile { id: string; display_name: string | null; avatar_url: string | null; created_at: string }
export interface Household { id: string; name: string; created_at: string }
export interface HouseholdMember { id: string; household_id: string; user_id: string; role: 'admin' | 'member'; joined_at: string }
```

From src/styles/global.css:
- Tailwind theme tokens: `--color-primary`, `--color-secondary`, `--color-accent`, `--color-text`, `--color-background`, `--color-surface`
- Dark mode via `.dark` class on `<html>`
- Font: Nunito Variable
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth system — AuthContext, AuthPage, login/signup/OAuth/reset</name>
  <files>src/contexts/AuthContext.tsx, src/hooks/useAuth.ts, src/pages/AuthPage.tsx, src/components/auth/AuthForm.tsx, src/components/auth/ResetModal.tsx, src/App.tsx, src/main.tsx, tests/auth.test.ts, tests/AuthContext.test.tsx</files>
  <behavior>
    - AuthContext initialises session from supabase.auth.getSession() on mount
    - AuthContext updates session when onAuthStateChange fires
    - useAuth() returns { session, loading, signOut }
    - signOut calls supabase.auth.signOut() and sets session to null
    - AuthForm calls signUp with email/password when in signup mode
    - AuthForm calls signInWithPassword when in login mode
    - AuthForm calls signInWithOAuth({ provider: 'google' }) for Google button
    - ResetModal calls resetPasswordForEmail when submitted
  </behavior>
  <action>
1. **AuthContext + useAuth hook:**
   - Create `src/contexts/AuthContext.tsx` following the exact pattern from 01-RESEARCH.md Pattern 2.
   - Export `AuthProvider` and `useAuth`.
   - `useAuth()` returns `{ session, loading, signOut }` where `signOut` calls `supabase.auth.signOut()`.
   - Use `useRef<boolean>(null)` (React 19 pattern) for cleanup tracking if needed.

2. **AuthForm component** (`src/components/auth/AuthForm.tsx`):
   - Single form with Login/Sign Up toggle (per locked decision — single page, not separate).
   - Login mode: email + password fields, submit calls `signInWithPassword`.
   - Signup mode: email + password + display name fields, submit calls `signUp` with `data: { display_name }`.
   - Google OAuth button: calls `signInWithOAuth({ provider: 'google' })`.
   - "Forgot password?" link opens ResetModal.
   - Show error messages from Supabase auth responses.
   - Style with Tailwind using theme tokens: sage green primary button, warm cream background, peach accent for links. Rounded corners (radius-btn). Nunito font inherits from global.

3. **ResetModal component** (`src/components/auth/ResetModal.tsx`):
   - Inline modal (per locked decision — no separate page navigation).
   - Email input field.
   - Submit calls `supabase.auth.resetPasswordForEmail(email, { redirectTo })`.
   - Show success/error feedback.
   - Close button.

4. **AuthPage** (`src/pages/AuthPage.tsx`):
   - Minimal layout: centered card with logo/app name + AuthForm.
   - Per user's specific idea: "Auth screen should feel minimal — just the logo, toggle, and form fields. No marketing copy."
   - Use background colour, surface colour for the card.

5. **Wire into App.tsx and main.tsx:**
   - `main.tsx`: Wrap app in `QueryClientProvider` (TanStack Query) and `BrowserRouter`.
   - `App.tsx`: Wrap routes in `AuthProvider`. If no session and not loading, show AuthPage. If session exists, show main app layout (placeholder for now — just "Logged in as {email}" text).
   - After signup, the app will route to household setup (implemented in Plan 03). For now, just show the authenticated state.

6. **Fill in test stubs** from Plan 01:
   - `tests/auth.test.ts`: Mock `supabase.auth.signUp` and `supabase.auth.resetPasswordForEmail`. Test that AuthForm calls the right methods with correct arguments.
   - `tests/AuthContext.test.tsx`: Mock `supabase.auth.getSession` and `onAuthStateChange`. Test session initialization and logout.
  </action>
  <verify>
    <automated>npx vitest run tests/auth.test.ts tests/AuthContext.test.tsx --reporter=verbose</automated>
  </verify>
  <done>Auth tests pass. AuthPage renders login/signup toggle form. Google OAuth button present. Reset modal opens and submits. Session persists via AuthContext. Logout clears session.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Responsive app shell — bottom tabs (mobile) and sidebar (desktop)</name>
  <files>src/components/layout/AppShell.tsx, src/components/layout/TabBar.tsx, src/components/layout/Sidebar.tsx, src/pages/HomePage.tsx, src/pages/SettingsPage.tsx, src/App.tsx, tests/AppShell.test.tsx, tests/theme.test.ts</files>
  <behavior>
    - AppShell renders TabBar at viewports below 768px
    - AppShell renders Sidebar at viewports 768px and above
    - TabBar shows 4 tabs: Home, Plan, Household, Settings
    - Plan tab shows a "Coming soon" indicator (not active in Phase 1)
    - Dark mode toggle in Settings page toggles .dark class and persists to localStorage
    - Theme applies system preference on first load when no stored preference exists
  </behavior>
  <action>
1. **TabBar** (`src/components/layout/TabBar.tsx`):
   - Fixed bottom bar with 4 tabs: Home, Plan, Household, Settings.
   - Each tab has icon (use simple SVG or emoji placeholders — discretion area) + label.
   - Plan tab is visually muted/disabled with "Coming soon" tooltip or badge.
   - Active tab highlighted with sage green primary colour.
   - Style: background-surface, border-top, rounded icons.
   - Uses `react-router-dom` NavLink for active state detection.

2. **Sidebar** (`src/components/layout/Sidebar.tsx`):
   - Left sidebar with same 4 navigation items as TabBar.
   - Shows at `md:` breakpoint (768px+) via Tailwind `hidden md:flex`.
   - App name "NourishPlan" at top.
   - Logout button at bottom (calls useAuth().signOut).
   - Same active state highlighting as TabBar.

3. **AppShell** (`src/components/layout/AppShell.tsx`):
   - Responsive wrapper: shows TabBar on mobile, Sidebar on desktop.
   - Use Tailwind responsive classes (not JS media queries) for the switch.
   - Content area: `<Outlet />` from react-router-dom.
   - Mobile: content fills viewport above fixed bottom TabBar.
   - Desktop: content sits to the right of the sidebar.

4. **HomePage placeholder** (`src/pages/HomePage.tsx`):
   - Welcome greeting: "Welcome to NourishPlan" (will be personalized in Plan 03).
   - Placeholder cards for "Meal Plans — Coming Soon" and "Quick Invite — Coming Soon".
   - Style with pastel theme: cream background, sage green headings, peach accents.

5. **SettingsPage** (`src/pages/SettingsPage.tsx`):
   - Theme toggle: switch between light/dark/system.
   - Persist choice to localStorage key 'theme'.
   - Apply/remove .dark class on document.documentElement.
   - Show current user email from useAuth().
   - Logout button (calls signOut, redirects to auth page).

6. **Update App.tsx routing:**
   - Unauthenticated: show `<AuthPage />`
   - Authenticated: show `<AppShell>` with nested routes:
     - `/` → HomePage
     - `/household` → placeholder (Plan 03 fills this in)
     - `/plan` → placeholder "Coming Soon" page
     - `/settings` → SettingsPage

7. **Fill in test stubs:**
   - `tests/AppShell.test.tsx`: Test that TabBar renders with 4 tabs. Test that the Plan tab is marked as inactive/coming-soon.
   - `tests/theme.test.ts`: Test dark mode toggle logic — when localStorage has 'dark', `.dark` class should be set. When cleared, should respect system preference.
  </action>
  <verify>
    <automated>npx vitest run tests/AppShell.test.tsx tests/theme.test.ts --reporter=verbose</automated>
  </verify>
  <done>AppShell renders responsive layout. Bottom tabs on mobile, sidebar on desktop. All 4 navigation items present. Dark mode toggle persists to localStorage. Settings page shows logout. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (auth + context + shell + theme)
2. `npx tsc --noEmit` — zero TypeScript errors
3. Auth flow: signup form visible, toggle to login works, Google button present, reset modal opens
4. Navigation: 4 tabs visible on mobile viewport, sidebar visible on desktop
5. Dark mode: toggle in settings persists and applies immediately
</verification>

<success_criteria>
- User can sign up with email/password and see authenticated state
- User can toggle between login/signup modes on single page
- Google OAuth button triggers Supabase OAuth flow
- Password reset modal accepts email and calls Supabase
- Session persists (refresh page stays logged in)
- Logout button clears session from any page
- Bottom tab bar with 4 tabs on mobile, sidebar on desktop
- Dark mode toggle works and persists
- All 8+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-02-SUMMARY.md`
</output>
