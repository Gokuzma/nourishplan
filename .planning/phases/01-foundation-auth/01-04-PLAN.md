---
phase: 01-foundation-auth
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/App.tsx
  - src/pages/HomePage.tsx
  - src/components/layout/AppShell.tsx
autonomous: false
requirements:
  - HSHD-05
  - PLAT-01
  - PLAT-02

must_haves:
  truths:
    - "Authenticated user without household is routed to HouseholdSetup"
    - "Authenticated user with household sees AppShell with Home tab"
    - "Home page shows welcome greeting with user name and household member list"
    - "Household data is isolated — user cannot see another household's data"
    - "App renders correctly on mobile and desktop viewports"
    - "Pastel colour scheme is consistent across all pages (light and dark)"
  artifacts:
    - path: "src/pages/HomePage.tsx"
      provides: "Personalized home page with household context"
      min_lines: 30
    - path: "src/App.tsx"
      provides: "Complete routing with auth guards and household checks"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useHousehold.ts"
      via: "Household existence check for routing"
      pattern: "useHousehold"
    - from: "src/pages/HomePage.tsx"
      to: "src/hooks/useHousehold.ts"
      via: "Member list and household data display"
      pattern: "useHouseholdMembers"
---

<objective>
Wire the auth system and household system together into a cohesive user flow, build out the Home page with household context, and verify the complete Phase 1 experience end-to-end.

Purpose: Plans 02 and 03 built features in parallel. This plan connects them into the intended user journey: signup -> household setup -> home with household data. Also verifies RLS isolation (HSHD-05).
Output: Complete Phase 1 user flow ready for verification.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-auth/01-RESEARCH.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-03-SUMMARY.md

<interfaces>
<!-- From Plan 02 — auth system -->
From src/contexts/AuthContext.tsx:
```typescript
export function useAuth(): { session: Session | null; loading: boolean; signOut: () => Promise<void> }
export function AuthProvider({ children }: { children: React.ReactNode }): JSX.Element
```

<!-- From Plan 03 — household system -->
From src/hooks/useHousehold.ts:
```typescript
export function useHousehold(): UseQueryResult<{ household_id: string; role: string; households: { name: string } } | null>
export function useHouseholdMembers(): UseQueryResult<HouseholdMember[]>
export function useCreateInvite(): UseMutationResult
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire auth and household flows, build Home page with household context</name>
  <files>src/App.tsx, src/pages/HomePage.tsx, src/components/layout/AppShell.tsx</files>
  <action>
1. **Update App.tsx routing to implement the full user flow:**
   - Unauthenticated: show AuthPage (login/signup)
   - Authenticated + loading household: show loading spinner
   - Authenticated + no household: redirect to `/setup` (HouseholdSetup)
   - Authenticated + has household: show AppShell with nested routes
   - This is the locked decision: "After signup, route straight to household setup (no empty dashboard first)"

2. **Build out HomePage** (`src/pages/HomePage.tsx`):
   - Per locked decision: "Home tab in Phase 1: welcome greeting, household member list, quick invite link, 'coming soon' placeholders for meal plans"
   - Welcome greeting: "Welcome, {display_name}!" using auth session user metadata
   - Household member list card: show all members using `useHouseholdMembers()`
   - Quick invite link: mini version of InviteLink component (admin only)
   - "Coming soon" placeholder cards for Meal Plans and Daily Log
   - Style with pastel theme: cream card backgrounds, sage green headers, peach accent on placeholder badges

3. **Ensure AppShell integration is clean:**
   - Verify TabBar active states work with react-router NavLink
   - Verify Sidebar shows household name
   - Verify logout works from both TabBar (Settings) and Sidebar
   - Ensure all pages use consistent padding/margins within the shell
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Complete user flow works: signup -> household setup -> home page. Home shows greeting, member list, invite link. Routing guards enforce auth and household requirements. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 experience</name>
  <files>none</files>
  <action>
Human verifies the complete Phase 1 experience end-to-end. All code is already built — this is a manual verification checkpoint.
  </action>
  <verify>Human confirms all 22 verification steps pass, or reports issues for resolution.</verify>
  <done>Phase 1 approved by user after verifying auth flow, household flow, RLS isolation, responsive layout, and dark mode.</done>
  <what-built>
    Complete Phase 1: auth system (signup, login, Google OAuth, logout, password reset), household system (create, invite, join, member management), responsive app shell (bottom tabs mobile, sidebar desktop), pastel colour scheme with dark mode, and RLS data isolation.
  </what-built>
  <how-to-verify>
    **Auth flow:**
    1. Visit http://localhost:5173 — should see auth page with login form
    2. Toggle to "Sign Up" — create account with email/password
    3. After signup, should route to Household Setup (not dashboard)
    4. Verify "Forgot password?" opens inline modal (not new page)
    5. Verify Google sign-in button is present and triggers OAuth

    **Household flow:**
    6. Create a household with a name
    7. Should land on Home page with welcome greeting
    8. Go to Household tab — verify member list shows you as Admin
    9. Generate an invite link — verify it copies to clipboard
    10. Open invite link in incognito/different browser — join as new user
    11. Verify both members appear in the member list

    **RLS isolation (HSHD-05 — phase gate):**
    12. Create a SECOND household with a different user (sign up in another browser)
    13. Verify user A can only see Household A's members
    14. Verify user B can only see Household B's members
    15. No cross-household data leakage

    **Responsive + theme:**
    16. Resize browser to mobile width — verify bottom tab bar appears
    17. Resize to desktop — verify sidebar appears
    18. Toggle dark mode in Settings — verify colours switch correctly
    19. Refresh page — verify dark mode persists

    **Member management (admin only):**
    20. As admin: add a child profile with name and birth year
    21. Verify the child appears in the member list
    22. As the invited member: verify member profile management is NOT visible
  </how-to-verify>
  <resume-signal>Type "approved" if the full flow works, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. `npx vitest run` — full test suite passes
2. `npx tsc --noEmit` — zero TypeScript errors
3. Complete user flow: signup -> household setup -> home -> household management
4. RLS isolation verified with two separate user sessions
5. Responsive layout verified at mobile and desktop viewports
6. Dark mode toggles and persists correctly
</verification>

<success_criteria>
- Signup routes to household setup, not empty dashboard
- Home page shows personalized greeting and household members
- Invite link generation and join flow work end-to-end
- RLS prevents cross-household data access (HSHD-05 gate)
- Mobile shows bottom tabs, desktop shows sidebar
- Pastel colours and dark mode are correct across all pages
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-04-SUMMARY.md`
</output>
