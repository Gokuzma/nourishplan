---
phase: 02-food-data-recipe-builder
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/006_fix_food_recipe_rls.sql
autonomous: true
gap_closure: true
requirements:
  - FOOD-03
  - FOOD-04
  - RECP-01
  - RECP-04
  - RECP-05
  - RECP-06

must_haves:
  truths:
    - "Custom food INSERT succeeds for authenticated household members"
    - "Custom food SELECT returns household foods without 403"
    - "Recipe INSERT succeeds for authenticated household members"
    - "Recipe ingredient INSERT/SELECT/UPDATE/DELETE works for household members"
  artifacts:
    - path: "supabase/migrations/006_fix_food_recipe_rls.sql"
      provides: "Corrected RLS policies using security-definer helpers"
      contains: "get_user_household_id"
  key_links:
    - from: "supabase/migrations/006_fix_food_recipe_rls.sql"
      to: "public.get_user_household_id()"
      via: "function call in policy USING/WITH CHECK clauses"
      pattern: "public\\.get_user_household_id\\(\\)"
---

<objective>
Fix all RLS policies on custom_foods, recipes, and recipe_ingredients tables that use raw household_members subqueries causing 403 permission failures.

Purpose: Migration 004 policies query household_members directly, which hits RLS on that table and causes permission recursion/failure. Migration 002 established `get_user_household_id()` and `get_user_household_role()` security-definer helpers specifically to solve this pattern. This migration replaces all broken policies with ones using those helpers.

Output: New migration 006_fix_food_recipe_rls.sql that drops and recreates all 12 RLS policies across 3 tables.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Security-definer helpers from migration 002 -->
From supabase/migrations/002_fix_household_members_rls.sql:
```sql
-- Returns the household_id for the current authenticated user (bypasses RLS)
create or replace function public.get_user_household_id()
returns uuid
language sql
security definer
stable
set search_path = ''
as $$
  select household_id
  from public.household_members
  where user_id = (select auth.uid())
  limit 1;
$$;

-- Returns the role ('admin'/'member') for the current authenticated user (bypasses RLS)
create or replace function public.get_user_household_role()
returns text
language sql
security definer
stable
set search_path = ''
as $$
  select role::text
  from public.household_members
  where user_id = (select auth.uid())
  limit 1;
$$;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 006 to fix all food/recipe RLS policies</name>
  <files>supabase/migrations/006_fix_food_recipe_rls.sql</files>
  <action>
Create a new migration file `supabase/migrations/006_fix_food_recipe_rls.sql` that:

1. **Drops all 12 existing policies** from migration 004:
   - custom_foods: "household members read custom_foods", "household members insert custom_foods", "creator or admin updates custom_foods", "creator or admin deletes custom_foods"
   - recipes: "household members read recipes", "household members insert recipes", "creator or admin updates recipes", "creator or admin deletes recipes"
   - recipe_ingredients: "recipe members read recipe_ingredients", "recipe members insert recipe_ingredients", "recipe members update recipe_ingredients", "recipe members delete recipe_ingredients"

2. **Recreates custom_foods policies** using `public.get_user_household_id()`:
   - SELECT: `deleted_at is null and household_id = public.get_user_household_id()`
   - INSERT: `household_id = public.get_user_household_id() and created_by = (select auth.uid())`
   - UPDATE: `created_by = (select auth.uid()) or (household_id = public.get_user_household_id() and public.get_user_household_role() = 'admin')`
   - DELETE: same as UPDATE

3. **Recreates recipes policies** using same pattern:
   - SELECT: `deleted_at is null and household_id = public.get_user_household_id()`
   - INSERT: `household_id = public.get_user_household_id() and created_by = (select auth.uid())`
   - UPDATE: `created_by = (select auth.uid()) or (household_id = public.get_user_household_id() and public.get_user_household_role() = 'admin')`
   - DELETE: same as UPDATE

4. **Recreates recipe_ingredients policies** — these join through recipes table, so use a subquery that checks `recipes.household_id = public.get_user_household_id()` instead of joining household_members directly:
   - SELECT: `exists (select 1 from public.recipes r where r.id = recipe_ingredients.recipe_id and r.household_id = public.get_user_household_id())`
   - INSERT: same exists check in WITH CHECK
   - UPDATE: same exists check
   - DELETE: same exists check

Keep the same policy names so any references remain consistent. Add a comment header explaining this migration fixes the RLS recursion issue from migration 004.
  </action>
  <verify>
    <automated>grep -c "get_user_household_id" supabase/migrations/006_fix_food_recipe_rls.sql</automated>
  </verify>
  <done>Migration file exists with all 12 policies recreated using security-definer helpers. No raw household_members subqueries remain. The file is ready to be applied via Supabase Dashboard SQL Editor or `supabase db push`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify migration SQL syntax and policy coverage</name>
  <files>supabase/migrations/006_fix_food_recipe_rls.sql</files>
  <action>
Validate the created migration file:

1. Count that exactly 12 `drop policy` statements exist (4 for custom_foods + 4 for recipes + 4 for recipe_ingredients)
2. Count that exactly 12 `create policy` statements exist
3. Confirm zero occurrences of `from public.household_members` or `join public.household_members` in any policy clause (only allowed in security-definer function bodies, which are in migration 002)
4. Confirm `public.get_user_household_id()` appears in all SELECT/INSERT policies for custom_foods and recipes
5. Confirm `public.get_user_household_role()` appears in UPDATE/DELETE policies for custom_foods and recipes (admin check)
6. Confirm recipe_ingredients policies use the `exists (select 1 from public.recipes r ...)` pattern with `get_user_household_id()`

If any check fails, fix the migration file.
  </action>
  <verify>
    <automated>bash -c "DROP=$(grep -c 'drop policy' supabase/migrations/006_fix_food_recipe_rls.sql) && CREATE=$(grep -c 'create policy' supabase/migrations/006_fix_food_recipe_rls.sql) && RAW=$(grep -c 'from public.household_members\|join public.household_members' supabase/migrations/006_fix_food_recipe_rls.sql || echo 0) && echo \"drop=$DROP create=$CREATE raw_subquery=$RAW\" && [[ $DROP -eq 12 ]] && [[ $CREATE -eq 12 ]] && [[ $RAW -eq 0 ]] && echo PASS || echo FAIL"</automated>
  </verify>
  <done>Migration has exactly 12 drop + 12 create policy statements. Zero raw household_members references in policy clauses.</done>
</task>

</tasks>

<verification>
- Migration file `006_fix_food_recipe_rls.sql` exists in `supabase/migrations/`
- All 12 policies use `get_user_household_id()` instead of raw subqueries
- No references to `from public.household_members` or `join public.household_members` in policy clauses
- Policy names match original names for consistency
</verification>

<success_criteria>
- Migration file is syntactically valid SQL
- All custom_foods, recipes, and recipe_ingredients RLS policies are corrected
- Applying this migration will unblock UAT tests 5-8 (custom foods) and 10-20 (recipes)
</success_criteria>

<output>
After completion, create `.planning/phases/02-food-data-recipe-builder/02-06-SUMMARY.md`
</output>
