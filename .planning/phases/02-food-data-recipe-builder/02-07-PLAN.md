---
phase: 02-food-data-recipe-builder
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/deploy-edge-functions.sh
autonomous: false
gap_closure: true
requirements:
  - FOOD-01
  - FOOD-02
  - FOOD-05
  - FOOD-06
  - RECP-02

user_setup:
  - service: supabase-cli
    why: "Edge function deployment requires authenticated Supabase CLI"
    env_vars:
      - name: USDA_API_KEY
        source: "Free signup at https://fdc.nal.usda.gov/api-key-signup/"
    dashboard_config:
      - task: "Ensure Supabase CLI is linked to project"
        location: "Terminal: supabase link --project-ref <your-project-ref>"
  - service: anthropic
    why: "AI nutrition verification edge function"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys"

must_haves:
  truths:
    - "USDA food search returns results when user searches from /foods page"
    - "Open Food Facts search returns results in OFF tab"
    - "AI verification edge function is deployed and accessible"
  artifacts:
    - path: "scripts/deploy-edge-functions.sh"
      provides: "Deployment script for edge functions with secrets"
  key_links:
    - from: "src/hooks/useFoodSearch.ts"
      to: "search-usda edge function"
      via: "supabase.functions.invoke('search-usda')"
      pattern: "functions\\.invoke"
    - from: "src/hooks/useFoodSearch.ts"
      to: "search-off edge function"
      via: "supabase.functions.invoke('search-off')"
      pattern: "functions\\.invoke"
---

<objective>
Deploy the three Supabase Edge Functions (search-usda, search-off, verify-nutrition) and set required secrets so food search actually works end-to-end.

Purpose: Edge function code exists in supabase/functions/ but was never deployed. All food search and AI verification fails with net::ERR_FAILED because there are no running functions on the Supabase project.

Output: Deployment script and verified running edge functions.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create edge function deployment script</name>
  <files>scripts/deploy-edge-functions.sh</files>
  <action>
Create `scripts/deploy-edge-functions.sh` that:

1. Checks if `supabase` CLI is available on PATH (exit with helpful message if not)
2. Deploys all three edge functions:
   ```
   supabase functions deploy search-usda --no-verify-jwt
   supabase functions deploy search-off --no-verify-jwt
   supabase functions deploy verify-nutrition --no-verify-jwt
   ```
   Note: `--no-verify-jwt` is needed because the React client calls these via `supabase.functions.invoke()` which handles auth headers automatically, but the functions themselves don't verify JWT (they are proxy functions).
3. Prints reminder to set secrets if not already set:
   ```
   echo "Ensure secrets are set:"
   echo "  supabase secrets set USDA_API_KEY=your_key_here"
   echo "  supabase secrets set ANTHROPIC_API_KEY=sk-ant-..."
   ```

Make the script executable. Add a header comment explaining what it does and prerequisites (supabase CLI linked to project).
  </action>
  <verify>
    <automated>test -f scripts/deploy-edge-functions.sh && echo PASS || echo FAIL</automated>
  </verify>
  <done>Deployment script exists with all three function deploy commands and secret reminders.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy edge functions and apply RLS migration</name>
  <files>scripts/deploy-edge-functions.sh</files>
  <action>
User deploys edge functions and applies migration. This cannot be automated because it requires authenticated Supabase CLI access and API key secrets that only the user possesses.

Steps for the user:
1. Ensure Supabase CLI is installed and linked: `supabase link --project-ref your-project-ref`
2. Set secrets: `supabase secrets set USDA_API_KEY=your_key` and `supabase secrets set ANTHROPIC_API_KEY=sk-ant-...`
3. Apply RLS fix migration (from plan 02-06): run 006_fix_food_recipe_rls.sql via Supabase Dashboard SQL Editor or `supabase db push`
4. Deploy edge functions: `bash scripts/deploy-edge-functions.sh`
5. Verify: navigate to /foods, search "chicken" in USDA tab, confirm results appear
  </action>
  <verify>
    <automated>MISSING -- requires live Supabase project with deployed edge functions; user confirms manually</automated>
  </verify>
  <done>Edge functions deployed and returning search results. USDA tab shows food results with macros. OFF tab shows food results. Custom food creation succeeds without 403. Recipe creation succeeds without 403.</done>
</task>

</tasks>

<verification>
- Edge functions are deployed and accessible via supabase.functions.invoke()
- USDA search returns food results with nutrition data
- Open Food Facts search returns food results
- Custom food CRUD works (no 403 errors)
</verification>

<success_criteria>
- Food search on /foods page returns results from both USDA and OFF tabs
- Custom food creation succeeds without permission errors
- Recipe creation succeeds without permission errors
- UAT tests 2-21 are unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/02-food-data-recipe-builder/02-07-SUMMARY.md`
</output>
