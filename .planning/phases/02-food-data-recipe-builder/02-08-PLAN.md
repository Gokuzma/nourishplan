---
phase: 02-food-data-recipe-builder
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/007_ingredient_id_to_text.sql
  - supabase/functions/search-usda/index.ts
autonomous: false
gap_closure: true
requirements: [RECP-01, RECP-04, FOOD-01, FOOD-02]

must_haves:
  truths:
    - "USDA food can be added as a recipe ingredient without Postgres type error"
    - "Open Food Facts food can be added as a recipe ingredient without Postgres type error"
    - "Custom food (UUID) can still be added as a recipe ingredient"
    - "Nested recipe ingredients (UUID) still work after column type change"
    - "USDA search results have an id field matching NormalizedFoodResult interface"
  artifacts:
    - path: "supabase/migrations/007_ingredient_id_to_text.sql"
      provides: "ALTER ingredient_id from uuid to text"
      contains: "ALTER COLUMN ingredient_id TYPE text"
    - path: "supabase/functions/search-usda/index.ts"
      provides: "USDA results with id field"
      contains: "id:"
  key_links:
    - from: "supabase/functions/search-usda/index.ts"
      to: "src/types/database.ts"
      via: "NormalizedFoodResult.id field"
      pattern: "id:\\s.*fdcId"
    - from: "src/components/recipe/RecipeBuilder.tsx"
      to: "recipe_ingredients.ingredient_id"
      via: "food.id passed as ingredient_id"
      pattern: "ingredient_id: food\\.id"
---

<objective>
Fix the ingredient_id UUID type mismatch that prevents external foods (USDA numeric IDs, Open Food Facts barcode strings) from being added as recipe ingredients.

Purpose: Unblock the core recipe builder flow for USDA and OFF foods. Currently only custom foods (which have real UUIDs) can be added as ingredients.
Output: Migration changing ingredient_id to text, USDA edge function returning id field matching NormalizedFoodResult interface.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-food-data-recipe-builder/02-06-SUMMARY.md

<interfaces>
<!-- The gap: ingredient_id is uuid but external food IDs are strings -->

From supabase/migrations/004_food_recipe.sql (line 180):
```sql
ingredient_id   uuid not null,
```

From src/types/database.ts (line 91-104):
```typescript
export interface NormalizedFoodResult {
  id: string
  name: string
  source: 'usda' | 'off' | 'custom'
  calories: number
  protein: number
  fat: number
  carbs: number
  // ...
}
```

From supabase/functions/search-usda/index.ts (line 25-34):
```typescript
// PROBLEM: Returns fdcId (number), NOT id (string)
interface NormalizedFood {
  fdcId: number;
  name: string;
  source: "usda";
  // ...
}
```

From supabase/functions/search-off/index.ts (line 47):
```typescript
// OK: Returns id as barcode string
id: product.code,
```

From src/components/recipe/RecipeBuilder.tsx (line 305):
```typescript
// Uses food.id — undefined for USDA (fdcId exists instead), barcode string for OFF
ingredient_id: food.id,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ingredient_id to text and add id field to USDA edge function</name>
  <files>supabase/migrations/007_ingredient_id_to_text.sql, supabase/functions/search-usda/index.ts</files>
  <action>
1. Create `supabase/migrations/007_ingredient_id_to_text.sql`:
   - `ALTER TABLE public.recipe_ingredients ALTER COLUMN ingredient_id TYPE text USING ingredient_id::text;`
   - This preserves existing UUID values as text strings. No data loss.
   - No RLS policy changes needed — policies reference recipe_id (still uuid), not ingredient_id.

2. Update `supabase/functions/search-usda/index.ts`:
   - In the `NormalizedFood` interface: add `id: string` field (keep `fdcId` for backward compat).
   - In the `normalizeFood` function return object: add `id: String(food.fdcId)` as the first field.
   - This makes USDA results conform to `NormalizedFoodResult` interface so `food.id` is defined when RecipeBuilder passes it as `ingredient_id`.

Do NOT modify RecipeBuilder, useRecipes, useFoodSearch, or any other client code — the fix is purely at the data layer (schema + edge function response shape).
  </action>
  <verify>
    <automated>cd "C:/Claude/Projects/Project 3" && grep -c "id:" supabase/functions/search-usda/index.ts && grep -c "TYPE text" supabase/migrations/007_ingredient_id_to_text.sql && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Migration 007 exists with ALTER COLUMN ingredient_id TYPE text
    - USDA edge function normalizeFood returns an `id` field (string of fdcId)
    - TypeScript compilation passes
    - Existing custom food UUIDs and recipe UUIDs stored as text are still valid strings
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify external food can be added as recipe ingredient</name>
  <files>none</files>
  <action>
Human verifies the complete fix end-to-end after deploying migration and edge function. All code changes are already made in Task 1 — this is a manual deployment and verification checkpoint.
  </action>
  <verify>Human confirms USDA food ingredient insert succeeds without 22P02 error.</verify>
  <done>User adds a USDA food as a recipe ingredient and it appears in the ingredient list with nutrition in the sticky bar.</done>
  <what-built>Migration 007 (ingredient_id uuid to text) and USDA edge function id field fix. These must be deployed to test the full flow.</what-built>
  <how-to-verify>
    1. Apply migration 007 via Supabase Dashboard SQL Editor: paste contents of `supabase/migrations/007_ingredient_id_to_text.sql` and run
    2. Deploy updated USDA edge function: `supabase functions deploy search-usda --no-verify-jwt`
    3. Open the app, navigate to /recipes, create a new recipe
    4. Click "Add Ingredient", search USDA tab for "chicken breast"
    5. Select a result, enter a quantity in grams, confirm
    6. Expected: Ingredient appears in the recipe ingredient list with nutrition values in the sticky bar. No 22P02 UUID error.
    7. (Optional) Test OFF tab the same way — OFF foods should also insert without error
    8. (Optional) Test adding a custom food as ingredient — should still work as before
  </how-to-verify>
  <resume-signal>Type "approved" if USDA food was added as ingredient successfully, or describe the error</resume-signal>
</task>

</tasks>

<verification>
- Migration 007 file exists with correct ALTER statement
- USDA edge function returns `id` field in normalized results
- TypeScript compiles without errors
- After deployment: USDA food can be added as recipe ingredient (no 22P02 error)
</verification>

<success_criteria>
- External food IDs (USDA numeric, OFF barcode) can be stored in recipe_ingredients.ingredient_id as text
- USDA search results include an `id` field so RecipeBuilder's `food.id` is not undefined
- Existing data (custom food UUIDs, recipe UUIDs stored as ingredient_id) remains valid as text
</success_criteria>

<output>
After completion, create `.planning/phases/02-food-data-recipe-builder/02-08-SUMMARY.md`
</output>
