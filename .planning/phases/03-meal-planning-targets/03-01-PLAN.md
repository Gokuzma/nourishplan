---
phase: 03-meal-planning-targets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/008_meals_plans_targets.sql
  - src/types/database.ts
  - src/utils/nutrition.ts
  - src/utils/mealPlan.ts
  - tests/nutrition.test.ts
  - tests/meal-plan.test.ts
autonomous: true
requirements: [MEAL-01, MEAL-02, MEAL-06, TRCK-01, TRCK-02, TRCK-03]

must_haves:
  truths:
    - "All meal, plan, template, and target tables exist with correct constraints"
    - "TypeScript types match the DB schema exactly"
    - "calcMealNutrition and calcDayNutrition return correct sums"
    - "getWeekStart returns correct ISO date for any week start day"
  artifacts:
    - path: "supabase/migrations/008_meals_plans_targets.sql"
      provides: "All Phase 3 tables, RLS policies, and household week_start_day column"
      contains: "create table public.meals"
    - path: "src/types/database.ts"
      provides: "Meal, MealItem, MealPlan, MealPlanSlot, MealPlanTemplate, NutritionTarget types"
      contains: "interface Meal"
    - path: "src/utils/nutrition.ts"
      provides: "calcMealNutrition, calcDayNutrition functions"
      exports: ["calcMealNutrition", "calcDayNutrition"]
    - path: "src/utils/mealPlan.ts"
      provides: "getWeekStart, TARGET_PRESETS, DEFAULT_SLOTS"
      exports: ["getWeekStart", "TARGET_PRESETS", "DEFAULT_SLOTS"]
    - path: "tests/nutrition.test.ts"
      provides: "Unit tests for calcMealNutrition, calcDayNutrition"
    - path: "tests/meal-plan.test.ts"
      provides: "Unit tests for getWeekStart"
  key_links:
    - from: "src/types/database.ts"
      to: "supabase/migrations/008_meals_plans_targets.sql"
      via: "Type interfaces mirror DB columns"
      pattern: "interface Meal|MealItem|MealPlan|MealPlanSlot|NutritionTarget"
---

<objective>
Create all Phase 3 database tables, TypeScript types, and pure utility functions.

Purpose: Every subsequent plan in this phase depends on these tables, types, and utils. This is the foundation layer.
Output: One migration file, extended types, nutrition utils, meal plan utils, and unit tests for pure functions.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-meal-planning-targets/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs from existing codebase -->

From src/types/database.ts:
```typescript
export interface MacroSummary {
  calories: number
  protein: number
  fat: number
  carbs: number
}
```

From src/utils/nutrition.ts:
```typescript
export function calcIngredientNutrition(food: MacroSummary, quantityGrams: number): MacroSummary
export function calcRecipePerServing(ingredients: { nutrition: MacroSummary }[], servings: number): MacroSummary
```

From supabase/migrations/004_food_recipe.sql (RLS pattern):
- Uses `get_user_household_id()` security-definer helper for household isolation
- Uses `get_user_household_role()` security-definer helper for admin checks
- Soft delete via `deleted_at` column
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Database migration — all Phase 3 tables and RLS</name>
  <files>supabase/migrations/008_meals_plans_targets.sql</files>
  <behavior>
    - meals table: id, household_id (FK), created_by (FK), name, deleted_at, timestamps
    - meal_items table: id, meal_id (FK cascade), item_type check('food','recipe'), item_id text, quantity_grams, calories_per_100g, protein_per_100g, fat_per_100g, carbs_per_100g, sort_order, timestamp
    - meal_plans table: id, household_id (FK), week_start date, created_by (FK), timestamp, unique(household_id, week_start)
    - meal_plan_slots table: id, plan_id (FK cascade), day_index 0-6 check, slot_name text, slot_order int, meal_id (FK set null), is_override bool default false, timestamp, unique(plan_id, day_index, slot_name)
    - meal_plan_templates table: id, household_id (FK), name, created_by (FK), timestamp
    - meal_plan_template_slots table: id, template_id (FK cascade), day_index 0-6 check, slot_name, slot_order, meal_id (FK set null)
    - nutrition_targets table: id, household_id (FK), user_id (FK auth.users nullable), member_profile_id (FK member_profiles nullable), check exactly one non-null, calories, protein_g, carbs_g, fat_g, micronutrients jsonb default '{}', custom_goals jsonb default '{}', timestamps, unique(household_id, user_id), unique(household_id, member_profile_id)
    - ALTER households ADD week_start_day integer default 0 check 0-6
    - RLS on all tables: meals/meal_items mirror recipes/recipe_ingredients policies; meal_plans/slots use household_id subquery; nutrition_targets allow member-own OR admin write
  </behavior>
  <action>
    Create `supabase/migrations/008_meals_plans_targets.sql` with all tables listed in behavior.

    Follow the exact RLS pattern from migration 004 (recipes):
    - SELECT: `exists(select 1 from household_members where user_id = auth.uid() and household_id = [table].household_id)` — use subquery through the parent table for child tables (meal_items via meals, slots via meal_plans, template_slots via templates)
    - INSERT: `created_by = auth.uid()` for parent tables; for child tables, exists subquery checking parent's household membership
    - UPDATE/DELETE: creator or admin via `get_user_household_role()`

    For nutrition_targets RLS:
    - SELECT: household member can read any target in their household
    - INSERT/UPDATE: `(user_id = auth.uid()) OR (get_user_household_role() = 'admin' AND household_id = get_user_household_id())` — member writes own row OR admin writes any household row
    - Use the existing `get_user_household_role()` and `get_user_household_id()` security-definer helpers (DO NOT inline subqueries on household_members — causes RLS recursion, see pitfall 3 in RESEARCH.md)

    For meal_items, store per-100g macros snapshot columns (calories_per_100g, protein_per_100g, fat_per_100g, carbs_per_100g numeric) — these are captured at add-time to avoid re-resolution on load (see pitfall 6 in RESEARCH.md).

    ALTER households to add week_start_day column.

    Enable RLS on all new tables. Add indexes on foreign keys used in joins (meal_items.meal_id, meal_plan_slots.plan_id, template_slots.template_id, nutrition_targets.household_id).
  </action>
  <verify>
    <automated>cd "C:\Claude\Projects\Project 3" && npx supabase db reset 2>&1 | tail -5</automated>
  </verify>
  <done>Migration applies cleanly on db reset. All tables, constraints, RLS policies, and indexes exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TypeScript types, nutrition utils, meal plan utils, and unit tests</name>
  <files>src/types/database.ts, src/utils/nutrition.ts, src/utils/mealPlan.ts, tests/nutrition.test.ts, tests/meal-plan.test.ts</files>
  <behavior>
    - calcMealNutrition([{nutrition: {calories:200, protein:10, fat:5, carbs:30}}, {nutrition: {calories:300, protein:20, fat:10, carbs:40}}]) => {calories:500, protein:30, fat:15, carbs:70}
    - calcMealNutrition([]) => {calories:0, protein:0, fat:0, carbs:0}
    - calcDayNutrition([{calories:500,protein:30,fat:15,carbs:70}, {calories:800,protein:40,fat:20,carbs:100}]) => {calories:1300,protein:70,fat:35,carbs:170}
    - getWeekStart(new Date('2026-03-15'), 0) => '2026-03-15' (Sunday, week starts Sunday)
    - getWeekStart(new Date('2026-03-18'), 0) => '2026-03-15' (Wednesday, week starts Sunday)
    - getWeekStart(new Date('2026-03-18'), 4) => '2026-03-12' (Wednesday, week starts Thursday)
    - getWeekStart(new Date('2026-03-12'), 4) => '2026-03-12' (Thursday, week starts Thursday)
  </behavior>
  <action>
    **Types (src/types/database.ts):** Add interfaces matching migration columns:
    - `Meal` (id, household_id, created_by, name, deleted_at, created_at, updated_at)
    - `MealItem` (id, meal_id, item_type: 'food'|'recipe', item_id, quantity_grams, calories_per_100g, protein_per_100g, fat_per_100g, carbs_per_100g, sort_order, created_at)
    - `MealPlan` (id, household_id, week_start, created_by, created_at)
    - `MealPlanSlot` (id, plan_id, day_index, slot_name, slot_order, meal_id, is_override, created_at)
    - `MealPlanTemplate` (id, household_id, name, created_by, created_at)
    - `MealPlanTemplateSlot` (id, template_id, day_index, slot_name, slot_order, meal_id)
    - `NutritionTarget` (id, household_id, user_id, member_profile_id, calories, protein_g, carbs_g, fat_g, micronutrients: Record<string,number>, custom_goals: Record<string,number>, created_at, updated_at)
    - Add all new tables to the `Database` type (Row/Insert/Update following existing pattern)
    - Update `Household` interface to include `week_start_day: number`

    **Nutrition utils (src/utils/nutrition.ts):** Add:
    - `calcMealNutrition(items: { nutrition: MacroSummary }[]): MacroSummary` — sum all item nutritions (same reduce pattern as calcRecipePerServing but without division)
    - `calcDayNutrition(mealNutritions: MacroSummary[]): MacroSummary` — sum all meal nutritions

    **Meal plan utils (new src/utils/mealPlan.ts):** Add:
    - `getWeekStart(today: Date, weekStartDay: number): string` — returns ISO date string ('YYYY-MM-DD') for the week start containing today, given household's preferred start day (0=Sun through 6=Sat). Uses modular arithmetic: `diff = (today.getDay() - weekStartDay + 7) % 7`, subtract diff days.
    - `TARGET_PRESETS` const object with 'Adult maintenance', 'Weight loss', 'Child 5-12', 'Teen' presets (values from RESEARCH.md)
    - `DEFAULT_SLOTS = ['Breakfast', 'Lunch', 'Dinner', 'Snacks'] as const`

    **Tests:** Write tests FIRST (red), then implement (green):
    - `tests/nutrition.test.ts`: Extend existing file with describe blocks for calcMealNutrition (normal, empty array) and calcDayNutrition (normal, single meal, empty)
    - `tests/meal-plan.test.ts`: New file with describe block for getWeekStart covering: Sunday start, Thursday start, same-day-as-start, wrapping across month boundary
  </action>
  <verify>
    <automated>cd "C:\Claude\Projects\Project 3" && npm test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>All new tests pass. Types compile without errors. calcMealNutrition, calcDayNutrition, getWeekStart, TARGET_PRESETS, DEFAULT_SLOTS all exported.</done>
</task>

</tasks>

<verification>
- `npx supabase db reset` completes without errors
- `npm test` passes all existing + new tests
- `npx tsc --noEmit` compiles without type errors
</verification>

<success_criteria>
- Migration 008 creates all Phase 3 tables with RLS
- All TypeScript interfaces match DB schema
- Pure utility functions have passing unit tests
- Household type includes week_start_day
</success_criteria>

<output>
After completion, create `.planning/phases/03-meal-planning-targets/03-01-SUMMARY.md`
</output>
