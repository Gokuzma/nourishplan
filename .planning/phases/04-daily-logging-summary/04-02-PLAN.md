---
phase: 04-daily-logging-summary
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/hooks/useFoodLogs.ts
  - src/components/log/PortionStepper.tsx
  - src/components/log/LogMealModal.tsx
  - src/components/log/FreeformLogModal.tsx
autonomous: true
requirements: [TRCK-04, TRCK-06]

must_haves:
  truths:
    - "useFoodLogs hook fetches log entries scoped by household, date, and member"
    - "useInsertFoodLog mutation creates a log entry and invalidates the correct query cache"
    - "useUpdateFoodLog mutation updates servings_logged and invalidates cache"
    - "useDeleteFoodLog mutation deletes a log entry and invalidates cache"
    - "PortionStepper renders stepper with presets (0.5, 1.0, 1.5, 2.0) and manual input"
    - "LogMealModal allows logging a planned meal with a portion size"
    - "FreeformLogModal wraps FoodSearch and prompts for quantity"
  artifacts:
    - path: "src/hooks/useFoodLogs.ts"
      provides: "CRUD hooks for food_logs"
      exports: ["useFoodLogs", "useInsertFoodLog", "useUpdateFoodLog", "useDeleteFoodLog", "useBulkInsertFoodLogs"]
    - path: "src/components/log/PortionStepper.tsx"
      provides: "Stepper input for serving size"
      exports: ["PortionStepper"]
    - path: "src/components/log/LogMealModal.tsx"
      provides: "Modal for plan-based logging"
      exports: ["LogMealModal"]
    - path: "src/components/log/FreeformLogModal.tsx"
      provides: "Modal for freeform food/recipe logging"
      exports: ["FreeformLogModal"]
  key_links:
    - from: "src/hooks/useFoodLogs.ts"
      to: "supabase food_logs table"
      via: "supabase.from('food_logs')"
      pattern: "from\\('food_logs'\\)"
    - from: "src/components/log/LogMealModal.tsx"
      to: "src/hooks/useFoodLogs.ts"
      via: "useInsertFoodLog mutation"
      pattern: "useInsertFoodLog"
    - from: "src/components/log/FreeformLogModal.tsx"
      to: "src/hooks/useFoodLogs.ts"
      via: "useInsertFoodLog mutation"
      pattern: "useInsertFoodLog"
---

<objective>
Build the TanStack Query hooks for food_logs CRUD and the logging UI components (PortionStepper, LogMealModal, FreeformLogModal).

Purpose: Provides the complete data layer and input components needed by the daily dashboard.
Output: Hooks file and three reusable components in src/components/log/.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-daily-logging-summary/04-CONTEXT.md
@.planning/phases/04-daily-logging-summary/04-RESEARCH.md
@.planning/phases/04-daily-logging-summary/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->
From src/types/database.ts (added in Plan 01):
```typescript
export interface FoodLog {
  id: string
  household_id: string
  logged_by: string
  member_user_id: string | null
  member_profile_id: string | null
  log_date: string
  slot_name: string | null
  meal_id: string | null
  item_type: string | null
  item_id: string | null
  item_name: string
  servings_logged: number
  calories_per_serving: number
  protein_per_serving: number
  fat_per_serving: number
  carbs_per_serving: number
  micronutrients: Record<string, number>
  is_private: boolean
  created_at: string
  updated_at: string
}
```

From src/utils/nutrition.ts:
```typescript
export function calcLogEntryNutrition(log: {
  calories_per_serving: number
  protein_per_serving: number
  fat_per_serving: number
  carbs_per_serving: number
  servings_logged: number
}): MacroSummary
```

Existing hooks pattern (from src/hooks/useMeals.ts):
- useQuery with queryKey: ['entity', householdId, ...]
- useMutation with onSuccess invalidating queryClient
- useAuth() for session, useHousehold() for membership

Existing component (reuse):
- src/components/food/FoodSearch.tsx — tabbed search overlay for USDA/OFF/My Foods/Recipes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useFoodLogs hooks</name>
  <files>src/hooks/useFoodLogs.ts</files>
  <action>
Create `src/hooks/useFoodLogs.ts` following the exact pattern from useMeals.ts and useNutritionTargets.ts:

1. `useFoodLogs(householdId, logDate, memberId, memberType)` — useQuery:
   - queryKey: `['food-logs', householdId, logDate, memberId]`
   - Filters by household_id, log_date, and either member_user_id or member_profile_id based on memberType
   - Orders by created_at ascending
   - enabled: all params truthy

2. `useInsertFoodLog()` — useMutation:
   - Accept params: member_user_id or member_profile_id, log_date, slot_name, meal_id, item_type, item_id, item_name, servings_logged, calories_per_serving, protein_per_serving, fat_per_serving, carbs_per_serving, micronutrients, is_private
   - Auto-set household_id from useHousehold() and logged_by from useAuth().session.user.id
   - .insert({...}).select().single()
   - onSuccess: invalidate ['food-logs', householdId, data.log_date, memberId]

3. `useUpdateFoodLog()` — useMutation:
   - Accept { id, servings_logged, is_private? } (the editable fields)
   - .update({...}).eq('id', id).select().single()
   - onSuccess: invalidate relevant food-logs query

4. `useDeleteFoodLog()` — useMutation:
   - Accept id
   - .delete().eq('id', id)
   - onSuccess: invalidate food-logs queries

5. `useBulkInsertFoodLogs()` — useMutation:
   - Accept array of insert params (for "Log all as planned")
   - .insert(rows).select()
   - onSuccess: invalidate food-logs queries for the date

Export InsertFoodLogParams type for use by modal components.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All five hooks exported. Query keys follow ['food-logs', householdId, logDate, memberId] pattern. Mutations invalidate correct cache entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create PortionStepper, LogMealModal, and FreeformLogModal components</name>
  <files>src/components/log/PortionStepper.tsx, src/components/log/LogMealModal.tsx, src/components/log/FreeformLogModal.tsx</files>
  <action>
1. `PortionStepper.tsx`:
   - Controlled component: props `value: number`, `onChange: (v: number) => void`, `min?: number` (default 0.25), `step?: number` (default 0.25)
   - Renders: decrement button (-), value display, increment button (+)
   - Quick-tap presets row: 0.5, 1.0, 1.5, 2.0 — each is a button that calls onChange with that value; active preset gets highlighted styling
   - Below presets: small text input for manual entry (type="number", min=0.25, step=0.25)
   - Tailwind styling matching existing component patterns (rounded-[--radius-btn], bg-primary, etc.)

2. `LogMealModal.tsx`:
   - Props: `isOpen`, `onClose`, `meal` (Meal with meal_items), `slotName`, `logDate`, `memberId`, `memberType`
   - Shows meal name, computed total nutrition (using calcMealNutrition from existing meal_items per-100g snapshots)
   - PortionStepper defaulting to 1.0
   - "Log Meal" button calls useInsertFoodLog with:
     - item_type: 'meal', meal_id: meal.id, item_name: meal.name
     - Per-serving macros = total meal macros (meals are atomic units, 1 serving = full meal per Phase 3 decision)
     - servings_logged from stepper
   - Privacy toggle (checkbox: "Mark as private")
   - Styled as bottom sheet on mobile (fixed bottom, rounded top), centered modal on desktop

3. `FreeformLogModal.tsx`:
   - Props: `isOpen`, `onClose`, `logDate`, `memberId`, `memberType`
   - Two-step flow:
     - Step 1: Render existing `FoodSearch` component for food/recipe selection
     - Step 2: After selection, show item name + nutrition + PortionStepper + "Log" button
   - On selection of a NormalizedFoodResult: item_type = food.source === 'custom' ? 'food' : food.source, item_id = food.id, per-serving macros from the food result (treat 100g as one "serving" for USDA/OFF foods; for custom foods use their serving_grams)
   - On selection of a Recipe: item_type = 'recipe', item_id = recipe.id, per-serving macros from calcRecipePerServing
   - Privacy toggle
   - Uses useInsertFoodLog mutation
   - Styled same as LogMealModal
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>PortionStepper renders stepper with presets and manual input. LogMealModal logs a planned meal with portion size. FreeformLogModal wraps FoodSearch and prompts for quantity. All three components follow existing Tailwind patterns.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — full test suite passes (no regressions)
- src/hooks/useFoodLogs.ts exports 5 hooks
- src/components/log/ contains PortionStepper.tsx, LogMealModal.tsx, FreeformLogModal.tsx
- Hooks follow established TanStack Query patterns from useMeals.ts
</verification>

<success_criteria>
- Food log CRUD operations available via hooks
- PortionStepper provides stepper + presets + manual entry for serving sizes
- LogMealModal handles plan-based logging with computed meal nutrition
- FreeformLogModal handles ad-hoc food/recipe logging via existing FoodSearch
- All components use established Tailwind patterns and project conventions
</success_criteria>

<output>
After completion, create `.planning/phases/04-daily-logging-summary/04-02-SUMMARY.md`
</output>
