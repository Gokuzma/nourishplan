---
phase: 05-portion-suggestions-polish
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/hooks/useFoodSearch.ts
  - src/components/food/FoodSearch.tsx
  - supabase/functions/verify-nutrition/index.ts
  - src/components/recipe/RecipeBuilder.tsx
  - src/components/plan/MicronutrientPanel.tsx
autonomous: true
requirements: [TRCK-05]
must_haves:
  truths:
    - "Single search bar queries both USDA and CNF simultaneously"
    - "Search results show source labels (USDA or CNF)"
    - "CNF results take priority when both sources have the same food"
    - "No OFF tab or OFF references remain in FoodSearch UI"
    - "Recipe builder shows expandable micronutrient breakdown"
    - "Micronutrients ordered: fiber > sodium > minerals > vitamins"
    - "verify-nutrition cross-checks USDA vs CNF instead of USDA vs OFF"
  artifacts:
    - path: "src/hooks/useFoodSearch.ts"
      provides: "Unified parallel USDA+CNF search with deduplication"
    - path: "src/components/food/FoodSearch.tsx"
      provides: "Single-tab unified search UI with source labels"
    - path: "src/components/plan/MicronutrientPanel.tsx"
      provides: "Reusable expandable micronutrient display component"
    - path: "src/components/recipe/RecipeBuilder.tsx"
      provides: "Recipe builder with micronutrient section added"
  key_links:
    - from: "src/hooks/useFoodSearch.ts"
      to: "supabase/functions/search-cnf"
      via: "supabase.functions.invoke('search-cnf')"
      pattern: "invoke.*search-cnf"
    - from: "src/components/plan/MicronutrientPanel.tsx"
      to: "src/utils/nutrition.ts"
      via: "imports MICRONUTRIENT_DISPLAY_ORDER and MICRONUTRIENT_LABELS"
      pattern: "MICRONUTRIENT_DISPLAY_ORDER"
---

<objective>
Refactor food search to unified USDA+CNF with source labels, update verify-nutrition, and add micronutrient display panel for recipes and meal plan.

Purpose: Completes the CNF integration at the UI layer and introduces the micronutrient visibility that the portion suggestion UI (Plan 04) will enhance with per-person scaling.
Output: Unified search, working verification, reusable micronutrient panel on recipe builder.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-portion-suggestions-polish/05-RESEARCH.md
@.planning/phases/05-portion-suggestions-polish/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/types/database.ts (after Plan 01):
```typescript
export interface NormalizedFoodResult {
  id: string
  name: string
  source: 'usda' | 'cnf' | 'custom'
  // ... macros and micronutrients
}
```

From src/utils/nutrition.ts (after Plan 01):
```typescript
export const MICRONUTRIENT_DISPLAY_ORDER = [
  'fiber', 'sodium', 'calcium', 'iron', 'potassium', 'vitamin_c', 'vitamin_a',
] as const

export const MICRONUTRIENT_LABELS: Record<string, string>
export const MICRONUTRIENT_UNITS: Record<string, string>
export const CNF_NUTRIENT_IDS: { /* same as USDA except vitamin_a: 319 */ }
```

From src/hooks/useFoodSearch.ts (current — will be refactored):
```typescript
export function useFoodSearch(tab: 'usda' | 'off', query: string)
// Returns { data: NormalizedFoodResult[], isLoading, error }
```

From src/components/food/FoodSearch.tsx (current):
```typescript
type ActiveTab = 'usda' | 'off' | 'custom'
interface FoodSearchProps {
  onSelect?: (food: NormalizedFoodResult) => void
  mode?: 'browse' | 'select'
}
```

From src/components/log/NutrientBreakdown.tsx (reusable pattern):
- Collapsible micronutrient display with progress bars
- Reuse this pattern for MicronutrientPanel
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unified USDA+CNF search and verify-nutrition update</name>
  <files>src/hooks/useFoodSearch.ts, src/components/food/FoodSearch.tsx, supabase/functions/verify-nutrition/index.ts</files>
  <action>
1. Refactor `src/hooks/useFoodSearch.ts`:
   - Change signature from `useFoodSearch(tab: 'usda' | 'off', query: string)` to `useFoodSearch(query: string)`
   - Fire two parallel TanStack queries: one for `search-usda`, one for `search-cnf` (both via `supabase.functions.invoke`)
   - Merge results with CNF priority: build `Map<lowercase_name, source>`, CNF results first, USDA skipped if name already seen
   - Return `{ data: NormalizedFoodResult[], isLoading: boolean, error: Error | null }`
   - `isLoading` is true while EITHER query is loading (show results progressively as each resolves)

2. Refactor `src/components/food/FoodSearch.tsx`:
   - Remove `ActiveTab` type and tab switching between 'usda'/'off' — replace with two tabs: 'search' and 'custom'
   - 'search' tab calls `useFoodSearch(query)` (unified) and displays results with a small source badge on each item (e.g., "USDA" or "CNF" in a muted pill)
   - 'custom' tab remains unchanged (shows custom foods)
   - Remove all OFF-specific code, imports, and references
   - Update all call sites of `useFoodSearch` — check RecipeBuilder.tsx and any other consumers that passed the `tab` parameter

3. Update `supabase/functions/verify-nutrition/index.ts`:
   - Replace references to OFF/Open Food Facts with CNF
   - Change parameter name from `offValues` to `cnfValues` (or equivalent)
   - Update the cross-check logic to compare USDA values vs CNF values
   - Keep the same graceful degradation pattern (return verified=false on error, never block)
  </action>
  <verify>
    <automated>cd "C:/Claude/Projects/Project 3" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>useFoodSearch takes a single query string and returns merged USDA+CNF results. FoodSearch.tsx shows 'search'/'custom' tabs with source badges. No OFF references remain in search UI or hooks. verify-nutrition updated for USDA vs CNF cross-check.</done>
</task>

<task type="auto">
  <name>Task 2: Micronutrient display panel for recipes</name>
  <files>src/components/plan/MicronutrientPanel.tsx, src/components/recipe/RecipeBuilder.tsx</files>
  <action>
1. Create `src/components/plan/MicronutrientPanel.tsx`:
   - Reusable expandable component following NutrientBreakdown.tsx pattern
   - Props: `micronutrients: Record<string, number> | null | undefined`, `servingsMultiplier?: number` (default 1), `showToggle?: boolean`
   - When `showToggle` is true: local useState for "per-person" vs "per-serving" toggle. Per-person multiplies values by `servingsMultiplier`. Per-serving shows raw values.
   - Display order: MICRONUTRIENT_DISPLAY_ORDER from nutrition.ts
   - Skip any nutrient with null/zero/undefined value
   - Each row shows: label (from MICRONUTRIENT_LABELS), value rounded to 1 decimal, unit (from MICRONUTRIENT_UNITS)
   - Styling: collapsible section with Tailwind, consistent with existing card/panel patterns. Use a disclosure triangle or chevron to expand/collapse.
   - Default state: collapsed

2. Add MicronutrientPanel to `src/components/recipe/RecipeBuilder.tsx`:
   - Below the existing nutrition bar / macro summary
   - Pass recipe's micronutrient data (aggregated from ingredients) and per-serving values
   - If recipe has no micronutrient data (all nulls), hide the panel entirely
   - Per locked decision: "Also add the same expandable micronutrient section to the recipe detail/builder page from Phase 2"
   - The micronutrient data comes from recipe ingredients that have micronutrients in their NormalizedFoodResult data. If ingredients don't have stored micros, the panel will be empty and hidden.
  </action>
  <verify>
    <automated>cd "C:/Claude/Projects/Project 3" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>MicronutrientPanel component created with collapsible display, per-person/per-serving toggle, and display order matching locked decision. RecipeBuilder shows micronutrient panel below macro summary when data is available.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- No references to 'off' in FoodSearch.tsx or useFoodSearch.ts (`grep -r "'off'" src/components/food/ src/hooks/useFoodSearch.ts`)
- MicronutrientPanel.tsx exists and is imported in RecipeBuilder.tsx
- useFoodSearch signature is `useFoodSearch(query: string)` with no tab parameter
</verification>

<success_criteria>
- Unified search fires both USDA and CNF in parallel, merges with CNF priority
- Source badges visible on each search result
- OFF completely removed from search UI
- verify-nutrition updated for USDA vs CNF
- MicronutrientPanel reusable and shown on recipe builder
- Micronutrient display follows locked order: fiber > sodium > calcium > iron > potassium > vitamin C > vitamin A
</success_criteria>

<output>
After completion, create `.planning/phases/05-portion-suggestions-polish/05-03-SUMMARY.md`
</output>
