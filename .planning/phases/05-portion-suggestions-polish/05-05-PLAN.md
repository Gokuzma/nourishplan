---
phase: 05-portion-suggestions-polish
plan: 05
type: execute
wave: 3
depends_on: [05-03, 05-04]
files_modified:
  - vite.config.ts
  - public/icon-192.png
  - public/icon-512.png
autonomous: false
requirements: [TRCK-05]
must_haves:
  truths:
    - "PWA passes Lighthouse installability check"
    - "App responds with 200 when offline (navigateFallback)"
    - "Icons are maskable"
    - "All Phase 5 features work end-to-end"
  artifacts:
    - path: "vite.config.ts"
      provides: "Updated PWA config with navigateFallback and globPatterns"
  key_links:
    - from: "vite.config.ts"
      to: "workbox service worker"
      via: "navigateFallback: '/index.html'"
      pattern: "navigateFallback"
---

<objective>
Fix PWA audit gaps and perform human verification of all Phase 5 features.

Purpose: The app must pass Lighthouse PWA audit for production quality. Human verification confirms portion suggestions, unified search, and micronutrients work correctly in the real UI.
Output: PWA audit passing, Phase 5 verified.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-portion-suggestions-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PWA Lighthouse audit fixes</name>
  <files>vite.config.ts, public/icon-192.png, public/icon-512.png</files>
  <action>
1. Update `vite.config.ts` VitePWA workbox config:
   - Add `navigateFallback: '/index.html'` — this is the primary missing item per research. Without it, Lighthouse "responds with 200 when offline" check fails for SPAs.
   - Add `globPatterns: ['**/*.{js,css,html,ico,png,svg}']` — precache all static assets for offline support
   - Keep existing `runtimeCaching` for navigation (NetworkFirst with timeout) — it complements `navigateFallback`

2. Update manifest icons to include maskable purpose:
   - Change icon entries to include `purpose: 'any maskable'` — current icons are solid sage green squares which are safe for maskable use (no detail to crop)
   - In manifest.icons: `{ src: '/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any maskable' }` and same for 512

3. Run production build and verify:
   - `npm run build` must succeed
   - `npm run preview` to start production server
   - Note: actual Lighthouse audit is manual (checkpoint task below)
  </action>
  <verify>
    <automated>cd "C:/Claude/Projects/Project 3" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>vite.config.ts updated with navigateFallback, globPatterns, and maskable icons. Production build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 5 features</name>
  <action>
    Human verifies all Phase 5 features work end-to-end in the running application.
  </action>
  <verify>
    <automated>echo "Manual verification — human confirms features work"</automated>
  </verify>
  <done>Human approves all Phase 5 features: portion suggestions, unified search, micronutrients, PWA.</done>
  <what-built>
    All Phase 5 features:
    1. Canadian Nutrient File (CNF) replacing Open Food Facts — unified search with source labels
    2. Per-member portion suggestions on each meal plan dish (percentage + servings)
    3. Macro warning indicators when suggestions exceed targets
    4. LogMealModal pre-fills with suggested portion
    5. Micronutrient expandable panel on recipes and meal plan dishes
    6. PWA audit improvements (navigateFallback, maskable icons)
  </what-built>
  <how-to-verify>
    **Portion suggestions:**
    1. Open the app and navigate to the Plan tab
    2. Ensure at least 2 household members have nutrition targets set (go to Members page if needed)
    3. Assign a meal to a slot on the current week
    4. Verify each slot shows a portion suggestion for the current user (e.g., "You: 45% (1.5 svg)")
    5. Expand the suggestion to see all household members with percentages summing to ~100%
    6. If leftover exists, verify "Leftover: X%" is shown
    7. Log a meal for the current day — verify the portion stepper pre-fills with the suggested value
    8. After logging, return to plan view — verify suggestions have updated (remaining calories decreased)

    **Unified search:**
    9. Navigate to Foods tab
    10. Search for "chicken" — verify results from both USDA and CNF appear with source labels
    11. Verify no "OFF" tab or Open Food Facts references exist anywhere

    **Micronutrients:**
    12. Open a recipe in the recipe builder
    13. Verify expandable micronutrient section appears below macros (fiber, sodium, etc.)
    14. On a meal plan dish, verify micronutrient panel is visible in expanded view

    **Macro warning:**
    15. If possible, set up a scenario where a member has already eaten close to their macro target — verify warning icon appears

    **PWA (optional — requires production build):**
    16. Run `npm run build && npm run preview`
    17. Open Chrome DevTools > Lighthouse > check "Progressive Web App"
    18. Run audit — verify installability and offline checks pass
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Production build succeeds (`npm run build`)
- `navigateFallback` present in vite.config.ts
- Human approves portion suggestions, unified search, and micronutrient display
</verification>

<success_criteria>
- PWA config includes navigateFallback and maskable icons
- Human confirms: portion suggestions display correctly per member
- Human confirms: unified search shows USDA + CNF results with labels
- Human confirms: micronutrient panel works on recipes and plan
- Human confirms: LogMealModal pre-fills with suggested portion
</success_criteria>

<output>
After completion, create `.planning/phases/05-portion-suggestions-polish/05-05-SUMMARY.md`
</output>
