---
phase: 06-launch-on-gregok-ca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
  - index.html
  - src/App.tsx
  - src/pages/NotFoundPage.tsx
  - src/pages/OfflinePage.tsx
autonomous: true
requirements:
  - LAUNCH-01
  - LAUNCH-02
  - LAUNCH-03
  - LAUNCH-04
  - LAUNCH-05

must_haves:
  truths:
    - "SPA routes do not 404 on refresh when deployed to Vercel"
    - "Service worker and manifest are served with no-cache headers"
    - "Branded splash screen appears while React app loads"
    - "OG meta tags are present with absolute URLs for social sharing"
    - "Navigating to an unknown route shows a branded 404 page, not a redirect"
    - "Offline state shows a branded offline page instead of browser error"
  artifacts:
    - path: "vercel.json"
      provides: "SPA rewrites and PWA cache headers"
      contains: "rewrites"
    - path: "index.html"
      provides: "Splash screen HTML/CSS and OG meta tags"
      contains: "og:image"
    - path: "src/pages/NotFoundPage.tsx"
      provides: "Custom 404 page component"
      exports: ["default"]
    - path: "src/pages/OfflinePage.tsx"
      provides: "Offline fallback page component"
      exports: ["default"]
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/NotFoundPage.tsx"
      via: "Route path='*' element"
      pattern: "NotFoundPage"
    - from: "index.html"
      to: "public/og-image.png"
      via: "og:image meta tag"
      pattern: "og:image"
---

<objective>
Add Vercel deployment config, splash screen, OG meta tags, custom 404 page, and offline fallback page to the NourishPlan codebase.

Purpose: Prepare the NourishPlan codebase for production deployment on Vercel with launch-quality polish. These are all code/config changes that must be committed before the infrastructure setup in Plan 03.

Output: vercel.json, updated index.html with splash + OG tags, NotFoundPage.tsx, OfflinePage.tsx, updated App.tsx routing
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-launch-on-gregok-ca/06-CONTEXT.md
@.planning/phases/06-launch-on-gregok-ca/06-RESEARCH.md

@index.html
@vite.config.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create vercel.json and add splash screen + OG tags to index.html</name>
  <files>vercel.json, index.html, public/og-image.png</files>
  <action>
1. Create `vercel.json` at project root with:
   - SPA rewrite: `{ "source": "/(.*)", "destination": "/index.html" }` (NOT `/**` — Vercel uses POSIX regex)
   - Cache headers for `sw.js`: `Cache-Control: public, max-age=0, must-revalidate`
   - Cache headers for manifest: `Content-Type: application/manifest+json` and `Cache-Control: public, max-age=0, must-revalidate` matching pattern `/(.*\.webmanifest|manifest\.json)`
   - Include `$schema` for editor support

2. Update `index.html`:
   - Add inline CSS splash screen in `<head>`: `#splash` div with `position: fixed; inset: 0; background: #A8C5A0; z-index: 9999` centered flex layout. Title "NourishPlan" in white 2rem bold text. Subtitle "Family nutrition, together" in white 0.9rem at 75% opacity. Include `transition: opacity 0.3s ease` and `.hidden` class that sets `opacity: 0; pointer-events: none`.
   - Add `<div id="splash">` before `<div id="root">` in body with the title and subtitle divs.
   - Add OG meta tags in `<head>`: `og:type=website`, `og:url=https://nourishplan.gregok.ca/`, `og:title=NourishPlan — Family Nutrition Together`, `og:description=Share one meal plan while each person gets personalized portion suggestions. A family nutrition app built for real life.`, `og:image=https://nourishplan.gregok.ca/og-image.png` with width 1200 and height 630. All URLs must be absolute HTTPS.
   - Add Twitter Card tags: `twitter:card=summary_large_image`, same title/description/image.
   - Update `<title>` to `NourishPlan — Family Nutrition Together`
   - Add `<meta name="description" content="Share one meal plan while each person gets personalized portion suggestions. A family nutrition app built for real life." />`

3. Create `public/og-image.png`: Generate a 1200x630 PNG with sage green (#A8C5A0) background and "NourishPlan" text centered in white. Use a Node.js script with canvas or create a simple solid-color PNG programmatically. If canvas is not available, create a minimal valid PNG with the sage green color (the exact text rendering is less critical than having a valid image file — user can replace with a designed version later).

4. Add splash dismissal in `src/App.tsx`: In the `App` component, add a `useEffect` that finds `#splash`, adds class `hidden`, then removes the element on `transitionend`. This runs once on mount.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>vercel.json exists with SPA rewrites and cache headers. index.html has splash screen HTML/CSS, OG meta tags with absolute URLs, and updated title. public/og-image.png exists. App.tsx dismisses splash on mount. TypeScript compiles and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create NotFoundPage, OfflinePage, and update routing</name>
  <files>src/pages/NotFoundPage.tsx, src/pages/OfflinePage.tsx, src/App.tsx</files>
  <action>
1. Create `src/pages/NotFoundPage.tsx`:
   - Full-page centered layout using Tailwind (matching the pastel theme: cream background, sage accents)
   - Large "404" heading in sage green
   - Message: "Page not found" as subheading
   - Description: "The page you're looking for doesn't exist or has been moved."
   - Link back to home: "Go home" button styled with sage green bg, white text, rounded, matching existing button patterns in the app
   - Import `Link` from `react-router-dom`

2. Create `src/pages/OfflinePage.tsx`:
   - Full-page centered layout, same theme as 404
   - Wifi-off icon (inline SVG — a simple circle with a line through it or similar)
   - Heading: "You're offline"
   - Description: "Check your internet connection and try again."
   - "Try again" button that calls `window.location.reload()`
   - This component is rendered inside the React app when `navigator.onLine === false` on a navigation that fails, NOT as a static HTML file. The existing `navigateFallback: '/index.html'` in the service worker already serves the React app offline, so this page renders within it.

3. Update `src/App.tsx`:
   - Import `NotFoundPage` from `./pages/NotFoundPage`
   - Replace `<Route path="*" element={<Navigate to="/" replace />} />` with `<Route path="*" element={<NotFoundPage />} />`
   - Remove the `Navigate` import if it is no longer used elsewhere (check first)
   - Add the `/offline` route: `<Route path="/offline" element={<OfflinePage />} />` outside the AppShell layout route (it should be accessible without auth)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>NotFoundPage renders a branded 404 with a link home. OfflinePage renders a branded offline state with reload button. App.tsx catch-all route renders NotFoundPage instead of redirecting. /offline route is accessible without auth. TypeScript compiles and tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no TypeScript errors
- `npm test` — all existing tests pass
- `npm run build` — production build succeeds with vercel.json present
- `cat vercel.json` — contains rewrites and cache headers
- `grep "og:image" index.html` — contains absolute HTTPS URL
- `grep "splash" index.html` — contains splash screen markup
</verification>

<success_criteria>
- vercel.json is valid JSON with SPA rewrite and PWA cache headers
- index.html has splash screen (sage green with NourishPlan text), OG meta tags, Twitter Card tags
- public/og-image.png exists as a valid PNG
- NotFoundPage shows branded 404 with link home
- OfflinePage shows branded offline state with reload button
- App.tsx catch-all uses NotFoundPage, splash dismissed on mount
- Production build succeeds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-launch-on-gregok-ca/06-01-SUMMARY.md`
</output>
