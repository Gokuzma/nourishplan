---
phase: 14-how-to-manual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/guide.test.ts
  - src/pages/GuidePage.tsx
  - src/App.tsx
  - src/components/layout/MobileDrawer.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "User can navigate to /guide from any page via MobileDrawer or Sidebar"
    - "User sees a quick-start card with 5 steps at the top of the guide"
    - "User sees 6 accordion sections covering all major features"
    - "Tapping a section header expands it to show intro, numbered steps, and tip callouts"
    - "Navigating to /guide#recipes auto-opens and scrolls to the Recipes section"
    - "Guide content accurately describes current app UI flows"
  artifacts:
    - path: "src/pages/GuidePage.tsx"
      provides: "Guide page with accordion layout, content data, deep-linking"
      min_lines: 100
    - path: "tests/guide.test.ts"
      provides: "Source-check tests for DOCS-01"
      exports: ["describe"]
    - path: "src/App.tsx"
      provides: "/guide route inside AppShell layout"
      contains: "GuidePage"
    - path: "src/components/layout/MobileDrawer.tsx"
      provides: "User Guide nav item"
      contains: "User Guide"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "User Guide nav item"
      contains: "User Guide"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/GuidePage.tsx"
      via: "Route path=/guide"
      pattern: 'path="/guide"'
    - from: "src/components/layout/MobileDrawer.tsx"
      to: "/guide"
      via: "NavLink in drawerItems"
      pattern: "to: '/guide'"
    - from: "src/components/layout/Sidebar.tsx"
      to: "/guide"
      via: "NavLink in navItems"
      pattern: "to: '/guide'"
---

<objective>
Create an in-app how-to guide at `/guide` with accordion sections covering all NourishPlan features, accessible from both mobile drawer and desktop sidebar navigation.

Purpose: Users need a reference guide explaining how to use food logging, recipes, meal planning, nutrition targets, and household management. This is the final phase of v1.1.
Output: GuidePage.tsx, route wiring, nav links, source-check tests.
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-how-to-manual/14-CONTEXT.md
@.planning/phases/14-how-to-manual/14-RESEARCH.md
@.planning/phases/14-how-to-manual/14-UI-SPEC.md

<interfaces>
<!-- Key structures the executor needs from existing files -->

From src/App.tsx (route pattern inside AppShell):
```typescript
// Lines 144-160: Auth + household required routes wrapped in AppShell
<Route element={<AuthGuard><AppShell /></AuthGuard>}>
  <Route path="/" element={<HomePage />} />
  <Route path="/recipes" element={<RecipesPage />} />
  // ... add /guide here
  <Route path="/settings" element={<SettingsPage />} />
</Route>
```

From src/components/layout/MobileDrawer.tsx (drawerItems array):
```typescript
const drawerItems = [
  { label: 'Meals', to: '/meals', icon: '🍽️' },
  { label: 'Household', to: '/household', icon: '👨‍👩‍👧' },
  { label: 'Settings', to: '/settings', icon: '⚙️' },
]
```

From src/components/layout/Sidebar.tsx (navItems array):
```typescript
const navItems = [
  { label: 'Home', to: '/', icon: '🏠' },
  { label: 'Recipes', to: '/recipes', icon: '📖' },
  { label: 'Meals', to: '/meals', icon: '🍽️' },
  { label: 'Plan', to: '/plan', icon: '📋' },
  { label: 'Household', to: '/household', icon: '👨‍👩‍👧' },
  { label: 'Settings', to: '/settings', icon: '⚙️' },
]
```

From tests/settings.test.tsx (source-check test pattern):
```typescript
import { describe, it, expect } from 'vitest';
describe('Account deletion (ACCTM-01)', () => {
  it('Danger Zone section renders at bottom of SettingsPage', async () => {
    const fs = await import('fs');
    const src = fs.readFileSync('src/pages/SettingsPage.tsx', 'utf8');
    expect(src).toContain('Danger Zone');
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create source-check test scaffold for DOCS-01</name>
  <files>tests/guide.test.ts</files>
  <read_first>
    - tests/settings.test.tsx (source-check test pattern to replicate)
  </read_first>
  <action>
Create `tests/guide.test.ts` with source-check assertions using the same `fs.readFileSync` pattern from `tests/settings.test.tsx`. All tests should initially FAIL (files don't exist yet).

Tests to create:

```typescript
import { describe, it, expect } from 'vitest';

describe('User Guide (DOCS-01)', () => {
  it('GuidePage route exists in App.tsx', async () => {
    const fs = await import('fs');
    const src = fs.readFileSync('src/App.tsx', 'utf8');
    expect(src).toContain('path="/guide"');
    expect(src).toContain('GuidePage');
  });

  it('GuidePage contains all 6 major feature sections', async () => {
    const fs = await import('fs');
    const src = fs.readFileSync('src/pages/GuidePage.tsx', 'utf8');
    expect(src).toContain("id: 'getting-started'");
    expect(src).toContain("id: 'adding-foods'");
    expect(src).toContain("id: 'recipes'");
    expect(src).toContain("id: 'meal-plan'");
    expect(src).toContain("id: 'tracking'");
    expect(src).toContain("id: 'household-admin'");
  });

  it('MobileDrawer has User Guide nav item', async () => {
    const fs = await import('fs');
    const src = fs.readFileSync('src/components/layout/MobileDrawer.tsx', 'utf8');
    expect(src).toContain("label: 'User Guide'");
    expect(src).toContain("to: '/guide'");
  });

  it('Sidebar has User Guide nav item', async () => {
    const fs = await import('fs');
    const src = fs.readFileSync('src/components/layout/Sidebar.tsx', 'utf8');
    expect(src).toContain("label: 'User Guide'");
    expect(src).toContain("to: '/guide'");
  });

  it('GuidePage has deep-link hash support', async () => {
    const fs = await import('fs');
    const src = fs.readFileSync('src/pages/GuidePage.tsx', 'utf8');
    expect(src).toContain('window.location.hash');
    expect(src).toContain('scrollIntoView');
  });

  it('GuidePage has quick-start card', async () => {
    const fs = await import('fs');
    const src = fs.readFileSync('src/pages/GuidePage.tsx', 'utf8');
    expect(src).toContain('Get started in 5 steps');
  });
});
```
  </action>
  <verify>
    <automated>npx vitest run tests/guide.test.ts --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|Tests)" || echo "Tests created, expected to fail until Task 2 completes"</automated>
  </verify>
  <acceptance_criteria>
    - tests/guide.test.ts exists and contains `describe('User Guide (DOCS-01)'`
    - File contains 6 `it(` blocks
    - File contains `expect(src).toContain("id: 'getting-started'")`
    - File contains `expect(src).toContain("id: 'household-admin'")`
    - File contains `expect(src).toContain('window.location.hash')`
    - File contains `expect(src).toContain('Get started in 5 steps')`
  </acceptance_criteria>
  <done>Test file exists with 6 source-check assertions covering route, sections, nav links, deep-linking, and quick-start</done>
</task>

<task type="auto">
  <name>Task 2: Create GuidePage with accordion sections and guide content</name>
  <files>src/pages/GuidePage.tsx</files>
  <read_first>
    - src/pages/SettingsPage.tsx (page layout pattern: min-h-screen bg-background px-4 py-8 font-sans, mx-auto max-w-lg)
    - src/pages/HomePage.tsx (food logging flow — verify guide content accuracy)
    - src/pages/RecipesPage.tsx (recipe flow — verify guide content accuracy)
    - src/pages/PlanPage.tsx (meal plan flow — verify guide content accuracy)
    - src/pages/MealsPage.tsx (meals flow — verify guide content accuracy)
    - src/pages/HouseholdPage.tsx (household flow — verify guide content accuracy)
    - src/pages/MemberTargetsPage.tsx (nutrition targets — verify guide content accuracy)
    - .planning/phases/14-how-to-manual/14-UI-SPEC.md (visual contract)
    - .planning/phases/14-how-to-manual/14-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Create `src/pages/GuidePage.tsx` with the following structure:

**1. TypeScript interface for guide section data:**
```typescript
interface GuideSection {
  id: string
  title: string
  intro: string
  steps: string[]
  tips?: string[]
}
```

**2. Content data array `GUIDE_SECTIONS` as a typed const inside the file with these 6 sections (in order):**

Section 1 — `id: 'getting-started'`, title: `'Getting Started'`
- Intro: Brief explanation that NourishPlan helps families plan meals and track nutrition together
- Steps covering: creating an account, logging in, creating a household, inviting family members
- Tip about inviting members via the Household page share link

Section 2 — `id: 'adding-foods'`, title: `'Adding Foods'`
- Intro: Explain the food library — search databases or add custom foods
- Steps covering: using the search bar on the Home page to find foods, searching USDA/CNF databases, adding a custom food with name/serving/macros, editing or deleting custom foods
- Tip: custom foods are shared with your whole household

Section 3 — `id: 'recipes'`, title: `'Building Recipes'`
- Intro: Combine foods into recipes with auto-calculated nutrition
- Steps covering: tapping "New Recipe" on Recipes page, searching and adding ingredients with quantities, setting number of servings, viewing per-serving nutrition, adding notes/variations
- Tip: you can use another recipe as an ingredient (nested recipes)

Section 4 — `id: 'meal-plan'`, title: `'Creating a Meal Plan'`
- Intro: Organize your meals into a weekly plan shared with your household
- Steps covering: navigating to Plan page, creating a meal from recipes/foods on the Meals page, assigning meals to day slots (breakfast/lunch/dinner/snacks), swapping meals on specific days, saving a plan as a reusable template, choosing a start date for a new plan
- Tip: you can print your meal plan from the menu

Section 5 — `id: 'tracking'`, title: `'Tracking Your Day'`
- Intro: Log what you eat and see how you're doing against your nutrition targets
- Steps covering: logging a food from the Home page search, logging a meal from the plan with portion size, viewing daily progress rings for calories and macros, viewing micronutrient breakdown, setting personal nutrition targets from Settings > member targets, editing or deleting log entries
- Tip: portion suggestions show how much each family member should eat based on their targets

Section 6 — `id: 'household-admin'`, title: `'Household Admin Tasks'`
- Intro: "These options are only visible to household admins."
- Steps covering: inviting new members via share link, viewing household members, editing household name from Settings, transferring admin role to another member, deleting your account (and choosing to transfer admin or delete the household)
- Tip: transferring admin rights lets someone else manage the household if you leave

**IMPORTANT:** Read each referenced page source file BEFORE writing that section's content. Verify the actual UI elements, button labels, and flow. For example:
- HomePage has a search bar for food logging (not a separate Food tab — that was removed in Phase 12)
- RecipesPage has a "New Recipe" button
- PlanPage shows a weekly grid with day slots
- Nutrition targets are set per member via Settings or MemberTargetsPage

**3. Quick-start card content (always visible, above accordion):**
Five steps:
1. "Sign in or create your account"
2. "Add foods to your food library"
3. "Build a recipe from your foods"
4. "Put your recipe into a meal plan"
5. "Log what you eat today"

**4. Page component structure:**
```
export function GuidePage() {
  const [openSection, setOpenSection] = useState<string | null>(null)

  // Deep-link hash support on mount
  useEffect(() => {
    const hash = window.location.hash.slice(1)
    if (hash) {
      setOpenSection(hash)
      // Use setTimeout to let React render the open section before scrolling
      setTimeout(() => {
        document.getElementById(hash)?.scrollIntoView({ behavior: 'smooth' })
      }, 100)
    }
  }, [])

  const toggle = (id: string) => setOpenSection(prev => prev === id ? null : id)

  return (
    <div className="min-h-screen bg-background px-4 py-8 font-sans">
      <div className="mx-auto max-w-lg flex flex-col gap-6">
        <h1 className="text-2xl font-bold text-text">User Guide</h1>

        {/* Quick-start card */}
        <div className="bg-surface rounded-[--radius-card] p-4 shadow-sm">
          <h2 className="font-bold text-text mb-2">Get started in 5 steps</h2>
          <ol className="list-decimal list-inside flex flex-col gap-2 text-sm text-text/80">
            {QUICK_START_STEPS.map((step, i) => <li key={i}>{step}</li>)}
          </ol>
        </div>

        {/* Accordion sections */}
        <div className="flex flex-col gap-2">
          {GUIDE_SECTIONS.map(section => (
            <div key={section.id} id={section.id}>
              <button
                onClick={() => toggle(section.id)}
                aria-expanded={openSection === section.id}
                aria-controls={`${section.id}-body`}
                className="w-full flex justify-between items-center py-2 px-4 bg-surface rounded-[--radius-card] text-left font-bold text-text min-h-[44px]"
              >
                <span>{section.title}</span>
                <span className={`transition-transform ${openSection === section.id ? 'rotate-180' : ''}`}>
                  ▼
                </span>
              </button>
              <div
                id={`${section.id}-body`}
                className={openSection === section.id ? 'p-4 flex flex-col gap-2' : 'hidden'}
              >
                <p className="text-sm text-text/80">{section.intro}</p>
                <ol className="list-decimal list-inside flex flex-col gap-2 text-sm text-text">
                  {section.steps.map((step, i) => <li key={i}>{step}</li>)}
                </ol>
                {section.tips?.map((tip, i) => (
                  <div key={i} className="mt-1 rounded-[--radius-btn] bg-accent/20 px-4 py-2 text-sm font-bold text-text">
                    {tip}
                  </div>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  )
}
```

**Writing tone:** Friendly and casual. Use "Tap", "Head to", "You can" — not formal language. Second person ("you") throughout.
  </action>
  <verify>
    <automated>npx vitest run tests/guide.test.ts -t "sections" --reporter=verbose && npx vitest run tests/guide.test.ts -t "quick-start" --reporter=verbose && npx vitest run tests/guide.test.ts -t "deep-link" --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/GuidePage.tsx exists and exports `function GuidePage()`
    - File contains `id: 'getting-started'` and `id: 'adding-foods'` and `id: 'recipes'` and `id: 'meal-plan'` and `id: 'tracking'` and `id: 'household-admin'`
    - File contains `Get started in 5 steps`
    - File contains `window.location.hash` and `scrollIntoView`
    - File contains `aria-expanded` and `aria-controls`
    - File contains `useState` and `useEffect` imports from 'react'
    - File contains `min-h-screen bg-background px-4 py-8 font-sans` (page layout pattern)
    - File contains `mx-auto max-w-lg` (content container pattern)
    - File contains `bg-accent/20` (tip callout styling)
    - File contains `These options are only visible to household admins` in the household-admin section intro
    - No references to FreeformLogModal, Food tab, or other removed features
  </acceptance_criteria>
  <done>GuidePage renders with quick-start card, 6 accordion sections with accurate content, deep-link hash support, proper accessibility attributes, and correct page layout matching existing pages</done>
</task>

<task type="auto">
  <name>Task 3: Add /guide route and navigation links</name>
  <files>src/App.tsx, src/components/layout/MobileDrawer.tsx, src/components/layout/Sidebar.tsx</files>
  <read_first>
    - src/App.tsx (current route definitions — add GuidePage import and route)
    - src/components/layout/MobileDrawer.tsx (current drawerItems array)
    - src/components/layout/Sidebar.tsx (current navItems array)
  </read_first>
  <action>
**1. In `src/App.tsx`:**
- Add import at the top with other page imports: `import { GuidePage } from './pages/GuidePage'`
- Add route inside the AppShell layout block (after the `/settings` route, before the closing `</Route>`):
  ```
  <Route path="/guide" element={<GuidePage />} />
  ```

**2. In `src/components/layout/MobileDrawer.tsx`:**
- Add one entry to the end of the `drawerItems` array (after Settings):
  ```
  { label: 'User Guide', to: '/guide', icon: '📘' },
  ```
  The full array becomes:
  ```typescript
  const drawerItems = [
    { label: 'Meals', to: '/meals', icon: '🍽️' },
    { label: 'Household', to: '/household', icon: '👨‍👩‍👧' },
    { label: 'Settings', to: '/settings', icon: '⚙️' },
    { label: 'User Guide', to: '/guide', icon: '📘' },
  ]
  ```

**3. In `src/components/layout/Sidebar.tsx`:**
- Add one entry to the end of the `navItems` array (after Settings):
  ```
  { label: 'User Guide', to: '/guide', icon: '📘' },
  ```
  The full array becomes:
  ```typescript
  const navItems = [
    { label: 'Home', to: '/', icon: '🏠' },
    { label: 'Recipes', to: '/recipes', icon: '📖' },
    { label: 'Meals', to: '/meals', icon: '🍽️' },
    { label: 'Plan', to: '/plan', icon: '📋' },
    { label: 'Household', to: '/household', icon: '👨‍👩‍👧' },
    { label: 'Settings', to: '/settings', icon: '⚙️' },
    { label: 'User Guide', to: '/guide', icon: '📘' },
  ]
  ```
  </action>
  <verify>
    <automated>npx vitest run tests/guide.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `import { GuidePage } from './pages/GuidePage'`
    - src/App.tsx contains `path="/guide"` and `element={<GuidePage />}`
    - src/components/layout/MobileDrawer.tsx contains `{ label: 'User Guide', to: '/guide', icon: '📘' }`
    - src/components/layout/Sidebar.tsx contains `{ label: 'User Guide', to: '/guide', icon: '📘' }`
    - "User Guide" is the last item in both drawerItems and navItems arrays (after Settings)
    - All 6 tests in tests/guide.test.ts pass
    - `npm test` full suite passes
  </acceptance_criteria>
  <done>Guide page is routable at /guide inside AppShell, accessible from MobileDrawer and Sidebar via "User Guide" nav item, and all source-check tests pass</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/guide.test.ts --reporter=verbose` — all 6 source-check tests pass
2. `npm test` — full test suite passes (no regressions)
3. Source inspection: `src/pages/GuidePage.tsx` contains 6 section IDs, quick-start card, accordion with aria attributes, deep-link hash support
4. Source inspection: `src/App.tsx` has `/guide` route inside AppShell block
5. Source inspection: Both MobileDrawer.tsx and Sidebar.tsx have `{ label: 'User Guide', to: '/guide' }`
</verification>

<success_criteria>
- GuidePage renders at /guide with quick-start card and 6 accordion sections
- All accordion sections have accurate content matching current app features
- Deep-linking via /guide#recipes (and other section hashes) auto-opens and scrolls to the correct section
- "User Guide" nav item appears in both MobileDrawer and Sidebar after Settings
- All source-check tests pass
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/14-how-to-manual/14-01-SUMMARY.md`
</output>
