---
phase: 15-v1-1-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useRecipes.ts
  - src/App.tsx
autonomous: true
requirements:
  - RECP-05
  - DELMG-01
  - ACCTM-01
  - CALC-01
  - CALC-02
  - CALC-03
  - UXLOG-01
  - UXLOG-02
  - UXLOG-03
  - UXLOG-04
  - RCPUX-01
  - RCPUX-02
  - RCPUX-03
  - MPLAN-01
  - MPLAN-02
  - DELMG-02
  - DOCS-01
gap_closure: true

must_haves:
  truths:
    - "Deleting a recipe invalidates meal-plan-slots and meals query cache so plan grid shows no stale data"
    - "ComingSoonPage dead code does not exist in App.tsx"
    - "All settings tests pass with real assertions"
    - "All 16 v1.1 enhancement requirement IDs are defined in REQUIREMENTS.md"
  artifacts:
    - path: "src/hooks/useRecipes.ts"
      provides: "useDeleteRecipe with full cache invalidation"
      contains: "meal-plan-slots"
    - path: "src/App.tsx"
      provides: "Clean route file with no dead code"
  key_links:
    - from: "src/hooks/useRecipes.ts"
      to: "TanStack Query cache"
      via: "invalidateQueries in onSuccess"
      pattern: "invalidateQueries.*meal-plan-slots"
---

<objective>
Close the two remaining integration gaps from the v1.1 milestone audit: fix recipe deletion cache invalidation (INT-CACHE-01) and remove ComingSoonPage dead code (INT-DEAD-01).

Purpose: Ensure deleting a recipe immediately refreshes meal plan slot and meal data without requiring a page reload, and eliminate dead code from the codebase.
Output: Two clean files with no integration gaps.

Note: The audit also flagged RED test stubs and missing requirement IDs, but both have already been resolved — tests/settings.test.tsx has real assertions (all 3 pass) and REQUIREMENTS.md already contains all 16 v1.1 enhancement IDs (CALC-01-03, UXLOG-01-04, RCPUX-01-03, MPLAN-01-02, DELMG-01-02, ACCTM-01, DOCS-01).
</objective>

<execution_context>
@C:/Users/Gokuz/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Gokuz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix useDeleteRecipe cache invalidation (INT-CACHE-01)</name>
  <files>src/hooks/useRecipes.ts</files>
  <read_first>
    - src/hooks/useRecipes.ts (current useDeleteRecipe implementation at line 135)
    - src/hooks/useMeals.ts (query key pattern: ['meals', householdId])
    - src/hooks/useMealPlan.ts (query key pattern: ['meal-plan-slots', planId])
  </read_first>
  <action>
In src/hooks/useRecipes.ts, modify the `useDeleteRecipe` function's `onSuccess` callback (currently at line 147-149) to invalidate three query families instead of one.

Current code (line 147-149):
```typescript
onSuccess: () => {
  queryClient.invalidateQueries({ queryKey: ['recipes'] })
},
```

Replace with:
```typescript
onSuccess: () => {
  queryClient.invalidateQueries({ queryKey: ['recipes'] })
  queryClient.invalidateQueries({ queryKey: ['meals'] })
  queryClient.invalidateQueries({ queryKey: ['meal-plan-slots'] })
},
```

This uses partial key matching — TanStack Query's `invalidateQueries` with `queryKey: ['meals']` matches all query keys starting with `['meals']` regardless of the householdId suffix. Same for `['meal-plan-slots']` regardless of planId suffix. No additional imports or parameters needed.
  </action>
  <verify>
    <automated>grep -n "meal-plan-slots" src/hooks/useRecipes.ts && grep -n "invalidateQueries.*meals" src/hooks/useRecipes.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/useRecipes.ts contains `queryClient.invalidateQueries({ queryKey: ['meal-plan-slots'] })`
    - src/hooks/useRecipes.ts contains `queryClient.invalidateQueries({ queryKey: ['meals'] })`
    - src/hooks/useRecipes.ts still contains `queryClient.invalidateQueries({ queryKey: ['recipes'] })`
    - `npx vitest run` passes (no regressions)
  </acceptance_criteria>
  <done>useDeleteRecipe invalidates recipes, meals, and meal-plan-slots query cache on successful deletion — plan grid and meal lists refresh without page reload</done>
</task>

<task type="auto">
  <name>Task 2: Remove ComingSoonPage dead code (INT-DEAD-01)</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (ComingSoonPage function at line 102-111)
  </read_first>
  <action>
In src/App.tsx, delete the `ComingSoonPage` function definition (lines 102-111). This is a standalone function component that is not referenced anywhere in the route table or imported by any other file.

Delete these exact lines:
```typescript
function ComingSoonPage() {
  return (
    <div className="min-h-screen bg-background px-4 py-8 font-sans flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-2xl font-bold text-primary mb-2">Coming Soon</h1>
        <p className="text-text/60">This feature is not available yet.</p>
      </div>
    </div>
  )
}
```

Do not modify any other code in App.tsx. Verify no imports or references to ComingSoonPage exist elsewhere.
  </action>
  <verify>
    <automated>grep -rn "ComingSoonPage" src/ && echo "FAIL: still referenced" || echo "PASS: no references"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "ComingSoonPage" src/` returns zero matches
    - src/App.tsx does not contain the string "ComingSoonPage"
    - `npx vite build 2>&1 | tail -5` completes without errors
  </acceptance_criteria>
  <done>ComingSoonPage dead code is removed from App.tsx — no dead function definitions remain</done>
</task>

</tasks>

<verification>
1. `grep -n "meal-plan-slots" src/hooks/useRecipes.ts` — confirms cache invalidation added
2. `grep -rn "ComingSoonPage" src/` — confirms dead code removed (zero matches)
3. `npx vitest run tests/settings.test.tsx` — confirms existing tests still pass (3/3)
4. `npx vite build` — confirms no build errors from changes
5. All 16 v1.1 enhancement requirement IDs present in REQUIREMENTS.md (pre-verified: already there)
</verification>

<success_criteria>
- INT-CACHE-01 closed: useDeleteRecipe invalidates ['recipes'], ['meals'], and ['meal-plan-slots'] cache
- INT-DEAD-01 closed: ComingSoonPage function removed from App.tsx
- All existing tests pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-v1-1-audit-gap-closure/15-01-SUMMARY.md`
</output>
