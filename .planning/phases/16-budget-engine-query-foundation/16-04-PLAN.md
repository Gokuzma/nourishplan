---
phase: 16-budget-engine-query-foundation
plan: 04
type: execute
wave: 3
depends_on: ["16-02", "16-03"]
files_modified: []
autonomous: false
requirements:
  - BUDG-01
  - BUDG-02
  - BUDG-03
  - BUDG-04
must_haves:
  truths:
    - "All four BUDG requirements verified by human interaction"
    - "Budget engine UI is visually correct and functionally complete"
  artifacts: []
  key_links: []
---

<objective>
Human verification of all Phase 16 budget engine features.

Purpose: Confirm that budget setting, cost entry, cost display, spend tracking, and query key migration all work correctly in the live app.

Output: Verified phase — all BUDG requirements confirmed working.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-budget-engine-query-foundation/16-CONTEXT.md
@.planning/phases/16-budget-engine-query-foundation/16-UI-SPEC.md
@.planning/phases/16-budget-engine-query-foundation/16-01-SUMMARY.md
@.planning/phases/16-budget-engine-query-foundation/16-02-SUMMARY.md
@.planning/phases/16-budget-engine-query-foundation/16-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy migration and verify all Phase 16 budget engine features</name>
  <files>supabase/migrations/020_budget_engine.sql</files>
  <action>
    Deploy migration 020 to the Supabase project using `npx supabase db push`, then present the verification checklist to the user for manual testing of all BUDG requirements.

    **Pre-flight: Deploy migration 020**
    Run `npx supabase db push` to deploy the budget engine migration to the Supabase project.

    **Then present the following verification checklist to the user:**

    **BUDG-01: Set weekly budget**
    1. Navigate to Settings page
    2. In the Household section (admin only), find the "Weekly Budget" field
    3. Enter a budget amount (e.g., 200.00)
    4. Blur the field — verify the value persists after page reload
    5. Navigate to Plan page — verify the same budget amount appears in the budget section

    **BUDG-04: Enter ingredient cost**
    1. Navigate to Recipes page, open or create a recipe
    2. Add an ingredient (e.g., search for "chicken breast")
    3. Find the "Set price" link on the ingredient row
    4. Click it — verify inline price form appears with amount, quantity, unit, and store fields
    5. Enter a price (e.g., $12.99 for 1kg at "Superstore")
    6. Click "Save Price" — verify the price persists and shows as $X.XX/100g on the row

    **BUDG-02: Recipe cost per serving**
    1. With priced ingredients in a recipe, verify cost badge appears below recipe name
    2. Verify format: "$X.XX/serving · N servings" for fully priced recipes
    3. Add an unpriced ingredient — verify partial indicator: "$X.XX+/serving · (N of M priced)"

    **BUDG-03: Weekly spend vs budget**
    1. On the Plan page, verify BudgetSummarySection appears below the plan grid
    2. Click "Mark as Cooked" on a recipe — verify spend increases
    3. Verify spend bar color transitions (green/amber/red)
    4. Verify "spent $X.XX of $Y.YY · $Z.ZZ remaining" text

    **Query key migration (regression check)**
    1. Navigate all pages — verify data loads correctly
    2. Create/edit/delete an item — verify cache refreshes after mutation
  </action>
  <verify>User confirms all checks pass by typing "approved"</verify>
  <done>All four BUDG requirements verified working by human tester; no regressions from query key migration</done>
</task>

</tasks>

<verification>
Human confirms all BUDG requirements are met via the verification checklist above.
</verification>

<success_criteria>
- All four BUDG requirements verified working by human tester
- No visual or functional regressions from query key migration
- Budget section renders correctly in both light and dark modes
</success_criteria>

<output>
After completion, create `.planning/phases/16-budget-engine-query-foundation/16-04-SUMMARY.md`
</output>
