---
phase: 18
slug: grocery-list-generation
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-30
---

# Phase 18 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (Tailwind CSS 4 @theme tokens, custom components only) |
| Icon library | Unicode emoji (inline, consistent with existing TabBar/Sidebar pattern) |
| Font | Nunito Variable, sans-serif (from `--font-sans` token) |

Source: `src/styles/global.css` — no shadcn, no component library. Project uses handwritten Tailwind components throughout.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge padding, inline pill spacing |
| sm | 8px | Gap between adjacent badges, tight row spacing |
| md | 16px | Default horizontal page padding (px-4), row internal padding |
| lg | 24px | Page top padding (py-6), between list sections |
| xl | 32px | Between major UI blocks (header, category sections) |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Bottom padding reserved for TabBar (pb-[64px]) |

Exceptions:
- Touch targets: minimum 44px height on all interactive buttons (tappable rows, check-off controls, manual add button) — matches existing `min-h-[44px]` convention in InventoryItemRow
- Category section headers: 12px top padding within sections for visual grouping without full lg gap

Source: `src/pages/InventoryPage.tsx`, `src/components/inventory/InventoryItemRow.tsx`, CLAUDE.md page spacing convention.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (text-sm) | 400 (regular) | 1.5 |
| Label | 12px (text-xs) | 400 (regular) | 1.4 |
| Heading (page) | 24px (text-2xl) | 700 (bold) | 1.2 |
| Subheading (category) | 16px (text-base) | 600 (semibold) | 1.3 |

Notes:
- Item names use text-sm font-medium (500) — consistent with InventoryItemRow `font-medium` on food_name
- Badge/pill text uses text-xs at weight 500 (font-medium) for readability at small size
- Estimated cost totals use text-sm font-semibold (600) to draw attention
- Page heading pattern matches InventoryPage: `text-2xl font-bold text-text`

Source: `src/components/inventory/InventoryItemRow.tsx`, `src/pages/InventoryPage.tsx`.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--color-background` (#FAFAF7 / dark: #1A1D1A) | Page background |
| Secondary (30%) | `--color-secondary` (#F5EDE3 / dark: #2A2E2A) | Cards, section dividers, row borders |
| Accent (10%) | `--color-accent` (#E8B4A2 / dark: #F0C4B2) | Reserved elements listed below |
| Destructive | red-600 / dark: red-400 | Remove item action only |

Accent reserved for:
- "Over budget" warning banner text and icon
- "Restock" badge on low-stock staple items (matches ExpiryBadge urgent pattern: `bg-accent/20 text-accent`)
- "Generate Grocery List" primary CTA button background (`bg-accent`)
- Active check-off item visual treatment (strikethrough row with accent/30 tint)

Additional semantic colors:
- Primary (`--color-primary`, #A8C5A0 / dark: #B8D4B0): "Already Have" section badge, checked-off confirmation state, in-stock indicator
- Warning/over-budget: `bg-red-50 dark:bg-red-900/10 border-red-200 text-red-700 dark:text-red-400` — consistent with InventoryItemRow inline confirmation pattern
- "Need to Buy" items without price: `text-text/50` with "?" placeholder — muted, not alarming

Source: `src/styles/global.css`, `src/components/inventory/ExpiryBadge.tsx`, `src/components/inventory/InventoryItemRow.tsx`.

---

## Component Inventory

Components to create for this phase:

| Component | Path | Pattern Source |
|-----------|------|----------------|
| `GroceryPage` | `src/pages/GroceryPage.tsx` | InventoryPage page shell |
| `GroceryCategorySection` | `src/components/grocery/GroceryCategorySection.tsx` | InventoryPage tab/section pattern |
| `GroceryItemRow` | `src/components/grocery/GroceryItemRow.tsx` | InventoryItemRow row pattern |
| `GroceryAlreadyHaveSection` | `src/components/grocery/GroceryAlreadyHaveSection.tsx` | Collapsible section with item count badge |
| `BudgetWarningBanner` | `src/components/grocery/BudgetWarningBanner.tsx` | InventoryItemRow inline-confirmation banner pattern |
| `ManualAddItemInput` | `src/components/grocery/ManualAddItemInput.tsx` | Inline text input with Add button |

Existing components to reuse without modification:
- `ExpiryBadge` — reuse pattern (not the component itself) for "Restock" and "Already Have" badge pills
- `InventorySummaryWidget` — reference for widget layout pattern if Plan page needs a grocery shortcut

---

## Interaction Contracts

### Grocery List Page Layout

```
/grocery route
  px-4 py-6 font-sans pb-[64px]
  ├── Page header: "Grocery List" h1 + regenerate button (top-right)
  ├── Cost summary bar: "Est. total: $X.XX" + over-budget warning if applicable
  ├── [Need to Buy] section — expanded by default
  │   ├── Category sections (Produce, Dairy, etc.) — always expanded
  │   │   └── GroceryItemRow × N (unchecked at top, checked at bottom)
  │   └── Manual items (rendered at bottom of "Need to Buy" with "Added by you" label)
  ├── [Already Have] section — collapsed by default, tap to expand
  │   └── GroceryItemRow × N (read-only, greyed out, no check interaction)
  └── Manual add input — fixed at bottom above TabBar, or inline at section bottom
```

### Check-off Interaction

- Tap row to toggle checked state.
- On check: row gets `line-through text-text/40` styling, item slides to bottom of its category group with a brief transition (150ms ease-out). An "Undo" toast appears for 4 seconds. Tapping Undo reverts the check.
- On uncheck (tap again or Undo): row returns to top of its category group, full opacity restored.
- Check state persists via Supabase realtime — all household members see updates live.
- Touch target: entire row is tappable (min-h-[44px]).

### GroceryItemRow Anatomy

```
[ ] Checkbox  |  Item name (text-sm font-medium)  |  Qty + unit  |  $X.XX or "?"
              |  Restock badge (if is_staple low-stock)
              |  Merged from X recipes (text-xs text-text/50, shown on expand)
```

- Checkbox: 20×20px styled square with `border-2 border-secondary`, checked state fills with `bg-primary`.
- No expand/confirm flow needed — check is the primary action. Undo toast handles accidental taps.
- Restock badge: `bg-accent/20 text-accent text-xs px-1.5 py-0.5 rounded-full font-medium` — "Restock" label.
- Price missing: show `text-text/40` "?" in price column.

### Generate / Regenerate

- "Generate Grocery List" button: shown when no list exists for the current week. Full-width, `bg-accent text-white rounded-[--radius-btn] min-h-[44px]`.
- "Regenerate" button: shown when list exists — small secondary button top-right of page header. `text-sm text-text/60 border border-secondary rounded-[--radius-btn] px-3 py-1.5 min-h-[44px]`.
- Regenerate triggers a confirmation: inline confirmation row (no modal) with "Regenerate — checked items will be reset" warning. Uses same inline confirm pattern as InventoryItemRow.
- Loading state: button disabled with `opacity-50` during generation. No spinner component — opacity is sufficient.

### Over-Budget Warning Banner

Shown between cost summary and item sections when grocery total exceeds household weekly budget.

```
bg-red-50 dark:bg-red-900/10
border border-red-200 dark:border-red-800
rounded-[--radius-card]
px-4 py-3 text-sm text-red-700 dark:text-red-400
```

Copy: "Over budget by $X.XX. Your grocery estimate exceeds this week's $Y.YY budget."

### Manual Add Input

Inline at bottom of "Need to Buy" section:
- Text input: `border border-secondary rounded-[--radius-btn] px-3 py-2 text-sm bg-surface` with placeholder "Add an item..."
- Add button: `bg-primary text-white text-sm px-4 py-2 rounded-[--radius-btn] min-h-[44px]`
- On submit: item appended to bottom of "Need to Buy" with "Added by you" text-xs label. No category assigned — appears in "Other" category.

### Already Have Section

- Collapsible: default collapsed. Toggle shows/hides the section. Header shows item count badge: `bg-primary/10 text-primary text-xs px-2 py-0.5 rounded-full`.
- Items in this section are display-only (no check interaction). `opacity-60`.
- Partial coverage items shown with quantity annotation: "2 of 5 covered" in `text-xs text-text/50`.

### Nav Integration

- "Grocery" nav item added to Sidebar (between Inventory and Household) with icon "🛒".
- "Grocery" nav item added to MobileDrawer (between Inventory and Household).
- AppShell tests must be updated to include "Grocery" in nav label assertions.
- Route: `/grocery`

---

## States

| State | Trigger | Visual Treatment |
|-------|---------|-----------------|
| Empty (no plan) | No active meal plan this week | Empty state: heading + body + Plan page link |
| Empty (no ingredients) | Plan exists but has no ingredients | Empty state: heading + body copy |
| Loading | Generation in progress | Button opacity-50, no spinner |
| Generated | List exists | Full list UI |
| Over budget | Total > weekly budget | Red warning banner |
| Item checked | User taps row | Strikethrough + opacity-40 + slides to bottom + Undo toast |
| Realtime update | Another household member checks off | Optimistic update via Supabase realtime channel |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (no list) | "Generate Grocery List" |
| Regenerate button | "Regenerate" |
| Regenerate confirmation | "Regenerate — checked items will be reset. Continue?" |
| Empty state heading (no plan) | "No active meal plan" |
| Empty state body (no plan) | "Create a meal plan for this week to generate your grocery list." |
| Empty state CTA (no plan) | "Go to Plan" (links to /plan) |
| Empty state heading (plan, no ingredients) | "No ingredients to shop for" |
| Empty state body (plan, no ingredients) | "Your meal plan doesn't have any recipe ingredients yet. Add recipes to your plan to generate a list." |
| Already Have section label | "Already Have" |
| Need to Buy section label | "Need to Buy" |
| Manual item source label | "Added by you" |
| Restock badge | "Restock" |
| Over budget banner | "Over budget by $X.XX. Your grocery estimate exceeds this week's $Y.YY budget." |
| Price missing | "?" |
| Partial coverage annotation | "X of Y covered" |
| Cost summary line | "Est. total: $X.XX" |
| Undo toast | "Item unchecked" with "Undo" action link |
| Error state (generation failed) | "Couldn't generate your list. Check your connection and try again." |
| Checked items count badge | "{N} checked" |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — no shadcn in this project |
| third-party | none | not applicable |

No third-party registry. Project uses hand-built Tailwind components exclusively.

---

## Dark Mode Contract

All components must use CSS token variables, not hardcoded hex values. Token mappings:

| Usage | Light | Dark |
|-------|-------|------|
| Page background | `bg-background` | auto via `.dark` override |
| Card/row surface | `bg-surface` | auto |
| Row border | `border-secondary` | auto |
| Body text | `text-text` | auto |
| Muted text | `text-text/50` | auto |
| Primary badge | `bg-primary/10 text-primary` | auto |
| Accent badge | `bg-accent/20 text-accent` | auto |
| Destructive/over-budget | `bg-red-50 dark:bg-red-900/10 text-red-700 dark:text-red-400` | explicit dark: class required |
| Checked row | `text-text/40 line-through` | auto |

---

## Accessibility

- All interactive rows: `role="button"` with `aria-label="{item name} — {checked/unchecked}"` on the check toggle
- Category sections: `role="region"` with `aria-label="{Category name}"`
- Collapsible Already Have section: `aria-expanded` on toggle button
- Undo toast: `role="status"` with `aria-live="polite"`
- Over-budget banner: `role="alert"` with `aria-live="assertive"`
- All touch targets: minimum 44px height (`min-h-[44px]`)

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
