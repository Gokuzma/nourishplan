---
phase: 19-drag-and-drop-planner
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified: []
autonomous: false
requirements:
  - PLAN-01
  - PLAN-03
user_setup:
  - service: supabase
    why: "Database schema push for is_locked column"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens"
    dashboard_config: []

must_haves:
  truths:
    - "is_locked column exists on meal_plan_slots table in production database"
    - "Drag-and-drop works on desktop with mouse"
    - "Drag-and-drop works on mobile with touch"
    - "Lock badge toggles is_locked and persists across reload"
    - "Drop onto occupied slot shows Swap/Replace menu"
    - "Mobile carousel shows adjacent days with scroll-snap"
  artifacts: []
  key_links: []
---

<objective>
Push the database migration for is_locked column and verify all Phase 19 features end-to-end.

Purpose: [BLOCKING] The is_locked column must exist in the live database for lock toggle to work. Human verification confirms drag-and-drop interaction quality on real devices.
Output: Live database updated, features verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-drag-and-drop-planner/19-CONTEXT.md
@.planning/phases/19-drag-and-drop-planner/19-01-SUMMARY.md
@.planning/phases/19-drag-and-drop-planner/19-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push database migration</name>
  <files></files>
  <read_first>
    supabase/migrations/023_dnd_locked_slots.sql
  </read_first>
  <action>
Run the Supabase schema push to apply the is_locked migration to the live database:

```bash
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase db push
```

If the command requires interactive confirmation that cannot be suppressed, this task becomes a checkpoint:human-action. Try first with `--include-all` flag or piping `echo y` to stdin.

After push, verify the column exists by checking the migration was applied (supabase db push output should show 023_dnd_locked_slots.sql as applied).
  </action>
  <verify>
    <automated>SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase db push 2>&1 | grep -i "locked\|023\|applied\|already"</automated>
  </verify>
  <acceptance_criteria>
    - supabase db push completes without error
    - 023_dnd_locked_slots.sql migration is applied (or already applied)
    - is_locked column exists on meal_plan_slots in the live database
  </acceptance_criteria>
  <done>
    is_locked column exists in production meal_plan_slots table
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify drag-and-drop and lock features</name>
  <files></files>
  <action>
Human visually and interactively verifies all Phase 19 drag-and-drop and lock features on the live or dev site.
  </action>
  <verify>
    <automated>echo "Checkpoint: human verification required"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms drag handle initiates drag on desktop and mobile
    - Human confirms drop onto empty slot moves the meal
    - Human confirms drop onto occupied slot shows Swap/Replace menu
    - Human confirms lock badge toggles and persists after reload
    - Human confirms mobile carousel with adjacent day peek
    - Human confirms touch on card body scrolls (does not drag)
  </acceptance_criteria>
  <done>
    Human types "approved" confirming all drag-and-drop and lock features work correctly
  </done>
  <what-built>
    Drag-and-drop meal rearrangement on the weekly plan grid with:
    - Grip handle on occupied slot cards for drag initiation
    - Lock badge toggle (top-right) to mark slots as locked
    - Cross-day drag support (any slot to any slot)
    - Drop onto empty slot: moves meal, clears source
    - Drop onto occupied slot: Swap/Replace action menu
    - Mobile: horizontal DayCarousel with adjacent day peek
    - Desktop: vertical DayCard stack (unchanged layout)
    - Locked slots show left border accent and filled lock icon
    - DragOverlay follows pointer with shadow/scale effect
  </what-built>
  <how-to-verify>
    1. Navigate to the Plan page (https://nourishplan.gregok.ca or localhost dev server)
    2. Ensure at least 2 meals are assigned to different slots

    **Desktop (mouse) verification:**
    3. Grab the grip handle on a meal slot card — confirm it lifts with shadow effect
    4. Drag to an empty slot on a different day — confirm meal moves, source clears
    5. Drag a meal onto an occupied slot — confirm Swap/Replace menu appears
    6. Choose "Swap" — confirm both meals exchange positions
    7. Repeat occupied drop and choose "Replace" — confirm source clears, target gets dragged meal
    8. Tap the lock badge on a slot — confirm it toggles (filled lock icon + left border)
    9. Reload the page — confirm locked state persists

    **Mobile (touch) verification:**
    10. On a phone or responsive mode (< 768px): confirm horizontal carousel with adjacent days peeking
    11. Swipe/scroll carousel between days — confirm smooth snap behavior
    12. Touch the grip handle — confirm drag starts without interfering with page scroll
    13. Drag to an adjacent visible day slot — confirm drop works
    14. Touch the card body (not handle) — confirm it scrolls, does NOT start drag

    **Edge cases:**
    15. Drag a locked slot — confirm it is draggable (lock only prevents auto-gen)
    16. Click outside the Swap/Replace menu — confirm it dismisses
    17. Press Escape while menu is open — confirm it dismisses
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase CLI -> production DB | Schema migration applied via authenticated CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering | DB migration push | mitigate | SUPABASE_ACCESS_TOKEN is required; migration is additive (ADD COLUMN with default) — no data loss risk |
</threat_model>

<verification>
- DB migration applied successfully
- All drag-and-drop interactions work as described
- Lock toggle persists across reload
- Mobile carousel functions correctly
</verification>

<success_criteria>
- is_locked column exists in production database
- Human confirms drag-and-drop works on desktop and mobile
- Human confirms lock toggle works and persists
- Human confirms Swap/Replace menu functions correctly
</success_criteria>

<output>
After completion, create `.planning/phases/19-drag-and-drop-planner/19-03-SUMMARY.md`
</output>
