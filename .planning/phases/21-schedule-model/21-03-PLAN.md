---
phase: 21-schedule-model
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified: []
autonomous: false
requirements: [SCHED-01, SCHED-02]
must_haves:
  truths:
    - "Database schema is pushed to production Supabase instance"
    - "Schedule tables exist in production with RLS policies active"
  artifacts: []
  key_links: []
---

<objective>
Push the 025_schedule.sql migration to the production Supabase database and verify it applied correctly.

Purpose: The schedule tables must exist in production for the feature to work on the deployed app. Build and type checks pass without the push (types come from config, not the live database), creating a false-positive verification state.
Output: Production database has member_schedule_slots and member_schedule_exceptions tables with RLS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-schedule-model/21-CONTEXT.md
@supabase/migrations/025_schedule.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push database migration to production</name>
  <action>
Push migration 025_schedule.sql to the production Supabase instance.

The user needs to run:
```bash
SUPABASE_ACCESS_TOKEN=<token> npx supabase db push
```

Or if linked locally:
```bash
npx supabase db push
```

This creates the `member_schedule_slots` and `member_schedule_exceptions` tables with all RLS policies, CHECK constraints, and unique indexes in production.
  </action>
  <how-to-verify>
    1. Run the db push command above
    2. Verify no errors in the output
    3. Optionally check Supabase Dashboard > Table Editor to confirm both tables exist
  </how-to-verify>
  <resume-signal>Type "pushed" when migration is applied, or describe any errors</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local -> production DB | Migration applies schema changes to production database |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation | Task |
|-----------|----------|-----------|-------------|------------|------|
| T-21-07 | Tampering | Production schema | accept | Migration is reviewed in Plan 01; db push is standard deployment step. RLS policies protect data at rest. | 21-03-01 |
</threat_model>

<verification>
- `supabase db push` completes without errors
- Both tables visible in Supabase Dashboard
</verification>

<success_criteria>
- Production database has member_schedule_slots and member_schedule_exceptions tables
- RLS policies are active on both tables
</success_criteria>

<output>
After completion, create `.planning/phases/21-schedule-model/21-03-SUMMARY.md`
</output>
