---
phase: 28-resolve-prep-sequence-edge-function-orphans
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PREP-02]
tags: [deployment, supabase, edge-function, phase-28]
user_setup:
  - service: supabase
    why: "Redeploy generate-cook-sequence + generate-reheat-sequence with --no-verify-jwt per D-07 and L-025 so live runtime matches repo source"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (or existing .env.local secret reused from Phase 25 deploy)"
    dashboard_config: []

must_haves:
  truths:
    - "Both edge functions are redeployed with --no-verify-jwt flag"
    - "npx supabase functions list shows both functions ACTIVE"
    - "Deploy output contains no 401/JWT errors"
  artifacts:
    - path: "supabase/functions/generate-cook-sequence/index.ts"
      provides: "Already-shipped edge function (Phase 23) — redeploy only, no source change"
    - path: "supabase/functions/generate-reheat-sequence/index.ts"
      provides: "Already-shipped edge function (Phase 23) — redeploy only, no source change"
  key_links:
    - from: "CLI deploy command"
      to: "Supabase project qyablbzodmftobjslgri"
      via: "npx supabase functions deploy <name> --no-verify-jwt"
      pattern: "--no-verify-jwt"
---

<objective>
Redeploy both orphaned Phase 23 edge functions (`generate-cook-sequence`, `generate-reheat-sequence`) with the `--no-verify-jwt` flag so the live Supabase runtime matches the repo source and the hooks from Plan 28-01 can invoke them without hitting the ES256/HS256 401 wall documented in lessons.md §L-025.

Purpose: CONTEXT.md D-07 — "Redeploy both edge functions in Phase 28 regardless of current Supabase state. Eliminates 'is the live function the same as repo source?' ambiguity." Without this, Plan 28-04's wire-in ships with a non-zero chance the edge functions silently reject every call from the browser.

Output: No new files. Both edge functions are `ACTIVE` on Supabase with the latest `--no-verify-jwt` deploy. Deploy logs captured in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-resolve-prep-sequence-edge-function-orphans/28-CONTEXT.md
@.planning/phases/28-resolve-prep-sequence-edge-function-orphans/28-PATTERNS.md
@lessons.md

<interfaces>
From lessons.md §L-025 (lines 187-195) — LOCKED deploy pattern:
```bash
SUPABASE_ACCESS_TOKEN=<token> npx supabase functions deploy <fn> --project-ref <ref> --no-verify-jwt
```

Both target edge functions already do `adminClient.auth.getUser(token)` inside their handlers (verified at `supabase/functions/generate-reheat-sequence/index.ts:90-100` per PATTERNS.md line 524), so `--no-verify-jwt` is CORRECT — runtime JWT verification is handled inside the function, not at the edge gateway.

From .planning/phases/25-universal-recipe-import/25-03-SUMMARY.md (precedent): the import-recipe deploy used the same command template with the project ref `qyablbzodmftobjslgri` and produced a green ACTIVE status in `supabase functions list`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supply Supabase access token</name>
  <read_first>
    - lessons.md §L-025 (confirm why --no-verify-jwt is required)
    - .planning/phases/28-resolve-prep-sequence-edge-function-orphans/28-CONTEXT.md D-07 (deploy policy)
  </read_first>
  <what-built>Plan 28-03 is about to redeploy two edge functions. It needs `SUPABASE_ACCESS_TOKEN` from the user's environment — this is a human-only step because the token is a secret that cannot be read from the repository.</what-built>
  <how-to-verify>
    Before the executor proceeds:
    1. Confirm you have access to the Supabase Dashboard for project `qyablbzodmftobjslgri` (the nourishplan project).
    2. Either:
       - Supply the token inline via the resume signal: `SUPABASE_ACCESS_TOKEN=sbp_xxxxx approved`
       - OR confirm the token is already exported in the current shell (`echo $SUPABASE_ACCESS_TOKEN | head -c 10 &amp;&amp; echo ...` should show a prefix without revealing the full token)
       - OR confirm the token is in a local `.env.local` that was reused from Phase 25
    3. Type `approved` to let the deploy run. Type anything else (e.g., describe blockers) to halt.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with `SUPABASE_ACCESS_TOKEN=...` prefix) to proceed, or describe the blocker to halt.</resume-signal>
  <acceptance_criteria>
    - User has explicitly typed "approved" (with or without an inline token export).
    - `echo ${SUPABASE_ACCESS_TOKEN:+set}` prints `set` (confirms non-empty token in the executor's environment).
  </acceptance_criteria>
  <done>SUPABASE_ACCESS_TOKEN is available in the environment of the Task 2 Bash command.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Deploy both edge functions with --no-verify-jwt</name>
  <files></files>
  <read_first>
    - supabase/functions/generate-cook-sequence/index.ts (read first 50 lines — verify the file is unchanged from Phase 23; NO source edits in this task)
    - supabase/functions/generate-reheat-sequence/index.ts (read first 50 lines — same check)
    - lessons.md §L-025 (confirm the command template)
  </read_first>
  <action>
Run both deploy commands sequentially. Do NOT edit any edge function source — this is a deploy-only task.

Step 1 — Deploy generate-cook-sequence:
```bash
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase functions deploy generate-cook-sequence --project-ref qyablbzodmftobjslgri --no-verify-jwt
```

Step 2 — Deploy generate-reheat-sequence:
```bash
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase functions deploy generate-reheat-sequence --project-ref qyablbzodmftobjslgri --no-verify-jwt
```

Step 3 — Verify both functions are ACTIVE:
```bash
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase functions list --project-ref qyablbzodmftobjslgri
```

Concrete requirements:
1. BOTH deploy commands MUST include `--no-verify-jwt` (L-025 rule).
2. BOTH deploy commands MUST use `--project-ref qyablbzodmftobjslgri` — this is the known production project ref (confirmed via Phase 25 SUMMARY).
3. Do NOT deploy any OTHER edge functions (`supabase functions deploy` without a name arg deploys all — do NOT use that form).
4. Do NOT edit either `index.ts` file — the source is already correct per Phase 23; this task is runtime-state reconciliation only.
5. If either deploy fails with `401` or `Invalid JWT` at the deploy endpoint (NOT the runtime), STOP and report — that indicates the access token itself is invalid, which is a user-setup issue.
6. If either deploy succeeds but `functions list` shows an old timestamp, re-run the corresponding deploy command once before failing.
7. Capture the deploy output (URL, function id, created_at) for the SUMMARY file.
  </action>
  <verify>
    <automated>SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase functions list --project-ref qyablbzodmftobjslgri 2>&amp;1 | grep -E "generate-cook-sequence|generate-reheat-sequence" | grep -c "ACTIVE"</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase functions list --project-ref qyablbzodmftobjslgri` output contains the line for `generate-cook-sequence` with status `ACTIVE`.
    - Same output contains the line for `generate-reheat-sequence` with status `ACTIVE`.
    - The combined grep count of `ACTIVE` rows matching either function name is exactly 2.
    - Both deploy commands logged a success line (e.g., `Function deployed` or `Finished supabase functions deploy`).
    - Neither deploy command exited with a non-zero status.
    - The `--verify-jwt` flag does NOT appear in either deploy invocation (i.e., `--no-verify-jwt` was present).
    - Neither `supabase/functions/generate-cook-sequence/index.ts` nor `supabase/functions/generate-reheat-sequence/index.ts` was modified during this task (`git diff HEAD -- supabase/functions/generate-cook-sequence/index.ts supabase/functions/generate-reheat-sequence/index.ts` returns empty).
  </acceptance_criteria>
  <done>Both edge functions redeployed with `--no-verify-jwt` and confirmed ACTIVE via `supabase functions list`. No source changes. Deploy logs captured for SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → Supabase Management API | Deploy commands authenticate with `SUPABASE_ACCESS_TOKEN` (user-scoped secret) and push the locally-built Deno bundle to the Supabase platform. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Spoofing | `SUPABASE_ACCESS_TOKEN` | mitigate | Token never logged, never committed — checkpoint Task 1 explicitly instructs the user to supply it via env var, not via file. Deploy commands use `$SUPABASE_ACCESS_TOKEN` expansion so the literal token is not visible in the shell history. |
| T-28-10 | Tampering | deployed function bundle | mitigate | The deploy command reads `supabase/functions/<name>/index.ts` from the local filesystem; the executor Task 2 action explicitly forbids editing either file. A `git diff` guard in acceptance criteria catches any accidental modification. |
| T-28-11 | Information disclosure | function output logs | accept | Supabase edge logs are accessible only via the authenticated Management API; non-sensitive operational logs. |
| T-28-12 | Elevation of privilege | `--no-verify-jwt` flag | mitigate | Flag is CORRECT usage here — both functions implement `adminClient.auth.getUser(token)` inside the handler (L-025), so authorization is enforced one layer in. The flag only disables the edge runtime's pre-handler JWT verification; it does NOT disable the function's own auth check. |

No new secrets introduced. The token is reused from Phase 25; rotation is out of scope.
</threat_model>

<verification>
- `SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase functions list --project-ref qyablbzodmftobjslgri` shows both functions as `ACTIVE`.
- Neither edge function's `index.ts` was modified (`git diff HEAD --stat -- supabase/functions/generate-cook-sequence/ supabase/functions/generate-reheat-sequence/` returns empty / no lines).
- Deploy commands both terminated with exit code 0.
- CookModePage consumer code (if Plan 28-04 has already shipped — it hasn't yet in Wave 1, so N/A) can invoke both functions without a 401 at the edge runtime. This is verified post-Plan-28-04 as part of its own success criteria.
</verification>

<success_criteria>
- Both `generate-cook-sequence` and `generate-reheat-sequence` redeployed with `--no-verify-jwt` per L-025 + D-07.
- Both show `ACTIVE` in `supabase functions list`.
- Source code unchanged — this is a deploy-only plan.
- Deploy output captured in SUMMARY for future-phase forensics.
</success_criteria>

<output>
After completion, create `.planning/phases/28-resolve-prep-sequence-edge-function-orphans/28-03-SUMMARY.md` covering: the two deploy commands run, their exit status, the `supabase functions list` verification output (both ACTIVE), confirmation that no source was modified, and the exact deploy timestamps for audit.
</output>
