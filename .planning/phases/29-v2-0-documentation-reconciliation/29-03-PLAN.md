---
phase: 29-v2-0-documentation-reconciliation
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [IMPORT-01, IMPORT-02, IMPORT-03, IMPORT-04, IMPORT-05]
tags: [documentation, traceability, requirements, sweep, status-flip]

must_haves:
  truths:
    - "IMPORT-01 traceability row reads `| IMPORT-01 | Phase 25 | Validated |` (D-09 suffix dropped, D-17 status flipped)"
    - "IMPORT-02 traceability row reads `| IMPORT-02 | Phase 25 | Validated |`"
    - "IMPORT-03 traceability row reads `| IMPORT-03 | Phase 25 | Partial |` (per 25-VERIFICATION human_needed)"
    - "IMPORT-04 traceability row reads `| IMPORT-04 | Phase 25 | Validated |`"
    - "IMPORT-05 traceability row reads `| IMPORT-05 | Phase 25 | Validated |`"
    - "All v2.0 rows (BUDG/INVT/GROC/PLAN/FEED/SCHED/PREP/PORT/IMPORT) updated per the exact before→after map in this plan's action"
    - "No v2.0 row retains the string `Pending` unless documented in action's target map"
    - "Zero v2.0 rows use the status string `Complete` (normalised to Validated per D-06/D-07)"
    - "Every v2.0 requirement bullet (lines 130-189 in REQUIREMENTS.md) has its checkbox aligned with its traceability row per D-18: [x] for Validated, [~] for Partial, [ ] for Pending"
    - "PREP-02 traceability row is unchanged (`| PREP-02 | Phase 23, Phase 28 (wire-in) | Validated |` — already correct per 28-05 flip)"
    - "v1.0/v1.1 rows at lines 230-279 are byte-identical after edit (D-06: `Complete` legacy stays)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability table sweep complete; checkbox alignment complete; IMPORT rows flipped"
      contains: "| IMPORT-01 | Phase 25 | Validated |"
  key_links:
    - from: ".planning/REQUIREMENTS.md IMPORT rows (lines 324-328)"
      to: ".planning/phases/25-universal-recipe-import/25-VERIFICATION.md"
      via: "Evidence chain: retrospective verification promotes status"
      pattern: "IMPORT-0[1-5] \\| Phase 25 \\| (Validated|Partial)"
    - from: ".planning/REQUIREMENTS.md INVT rows (lines 297-302)"
      to: ".planning/phases/17-inventory-engine/17-VERIFICATION.md"
      via: "Evidence chain: retrospective verification promotes status"
      pattern: "INVT-0[1-6] \\| Phase 17"
    - from: ".planning/REQUIREMENTS.md bullet checkboxes (lines 130-189)"
      to: ".planning/REQUIREMENTS.md traceability rows (lines 293-328)"
      via: "D-18 alignment: checkbox state mirrors row status"
      pattern: "\\[x\\]|\\[~\\]|\\[ \\]"
---

<objective>
Sweep `.planning/REQUIREMENTS.md` to finalize the v2.0 traceability table per D-05/D-07/D-09/D-17 and align every v2.0 requirement-bullet checkbox with its row status per D-18. This is the single file that drives downstream gsd-sdk tooling (per 29-CONTEXT.md code_context line 139 — status-column spelling is parsed), so every status string must exactly match `Validated`, `Partial`, or `Pending` — case-sensitive, no drift.

This plan addresses ALL FIVE phase requirement IDs: IMPORT-01..IMPORT-05 (D-17 status flip from `Phase 25, Phase 29 (gap closure) | Pending` to `Phase 25 | Validated` or `Phase 25 | Partial` per the retrospective evidence produced by Plans 01 and 02).

Depends on Plans 01 and 02: the retros at `.planning/phases/17-inventory-engine/17-VERIFICATION.md` and `.planning/phases/25-universal-recipe-import/25-VERIFICATION.md` must exist before this sweep executes, because they are the evidence base for the INVT and IMPORT row promotions.

Purpose: Close WARN-04 (traceability staleness) and complete the D-17 IMPORT flip. After this plan lands, only Plan 04 (Phase 24 deviation blockquote) remains between the milestone and archive-readiness.
Output: Edits to `.planning/REQUIREMENTS.md` only — no new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md
@.planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md
@.planning/phases/17-inventory-engine/17-VERIFICATION.md
@.planning/phases/25-universal-recipe-import/25-VERIFICATION.md
@.planning/phases/26-wire-cook-mode-to-inventory-and-budget/26-VERIFICATION.md
@.planning/phases/28-resolve-prep-sequence-edge-function-orphans/28-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite v2.0 traceability table and align v2.0 requirement-bullet checkboxes</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — this task edits lines 130-189 for bullet checkboxes AND lines 289-328 for v2.0 traceability rows; understand where both sections live before editing)
    - .planning/REQUIREMENTS.md lines 224-280 (v1.0/v1.1 traceability block — must NOT be touched per D-06; `Complete` legacy values stay)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md lines 34-44 (D-05, D-06, D-07, D-08, D-09 — status vocabulary + suffix hygiene), lines 70-71 (D-17 IMPORT flip rule), line 74 (D-18 checkbox alignment rule)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md lines 187-216 (current-state audit + target-state table), lines 447-457 (vocabulary spelling hazards — exact case-sensitive strings), lines 497-499 (new `[~]` checkbox state hazard)
    - .planning/phases/17-inventory-engine/17-VERIFICATION.md (must exist after Plan 01 — confirms INVT-01..06 retrospective status)
    - .planning/phases/25-universal-recipe-import/25-VERIFICATION.md (must exist after Plan 02 — confirms IMPORT-03 `Partial`, others `Validated`)
    - .planning/phases/18-grocery-list-generation/18-VERIFICATION.md (confirms realtime sync is human_needed → GROC rows → Partial)
    - .planning/phases/19-drag-and-drop-planner/19-VERIFICATION.md (confirms PLAN-01/03 human_needed → Partial)
    - .planning/phases/20-feedback-engine-dietary-restrictions/20-VERIFICATION.md (confirms FEED-01..04 human_needed → Partial)
    - .planning/phases/21-schedule-model/21-VERIFICATION.md (Phase 21 human_needed — but criterion closed by Phase 27 GC, see below)
    - .planning/phases/22-constraint-based-planning-engine/22-VERIFICATION.md (PLAN-02/04/05 human_needed → Partial)
    - .planning/phases/23-prep-optimisation/23-VERIFICATION.md (PREP-01/03 human_needed → Partial; PREP-02 already Validated via Phase 28)
    - .planning/phases/24-dynamic-portioning/24-VERIFICATION.md (PORT-01/02 human_needed for deploy check + ROADMAP reconcile → Partial until Plan 04 runs)
    - .planning/phases/27-wire-schedule-badges-to-plangrid/27-VERIFICATION.md (Phase 27 passed with empty human_verification — SCHED-01/02 get Validated via GC)
    - .planning/phases/28-resolve-prep-sequence-edge-function-orphans/28-VERIFICATION.md (PREP-02 already Validated via wire-in — no edit needed)
  </read_first>
  <action>
Execute TWO coordinated edits on `.planning/REQUIREMENTS.md`. Use the `Edit` tool to make targeted string replacements — do NOT rewrite the whole file.

---

### EDIT 1 — v2.0 requirement-bullet checkboxes (lines 130-189) per D-18

Apply the EXACT string replacements below. Each replacement is a single requirement bullet — replace the full line containing the checkbox and label. Left column is the current string (verified by reading REQUIREMENTS.md lines 130-189); right column is the exact new string.

| Line | Current (REPLACE) | New (WITH) |
|------|-------------------|------------|
| 130 | `- [ ] **BUDG-01**: User can set a weekly household food budget` | `- [x] **BUDG-01**: User can set a weekly household food budget` |
| 131 | `- [ ] **BUDG-02**: Each recipe displays a computed cost per serving based on ingredient costs` | `- [x] **BUDG-02**: Each recipe displays a computed cost per serving based on ingredient costs` |
| 132 | `- [ ] **BUDG-03**: Plan page shows weekly spend vs budget with remaining balance` | `- [~] **BUDG-03**: Plan page shows weekly spend vs budget with remaining balance` |
| 133 | `- [x] **BUDG-04**: User can enter cost per unit/weight on recipe ingredients` | `- [x] **BUDG-04**: User can enter cost per unit/weight on recipe ingredients` (unchanged) |
| 137 | `- [ ] **INVT-01**: User can add items to inventory with storage location (pantry, fridge, freezer)` | `- [x] **INVT-01**: User can add items to inventory with storage location (pantry, fridge, freezer)` |
| 138 | `- [ ] **INVT-02**: Each inventory item tracks quantity and unit (grams, units, ml)` | `- [x] **INVT-02**: Each inventory item tracks quantity and unit (grams, units, ml)` |
| 139 | `- [ ] **INVT-03**: Each inventory item has an optional expiry date with priority sorting` | `- [x] **INVT-03**: Each inventory item has an optional expiry date with priority sorting` |
| 140 | `- [ ] **INVT-04**: User can scan a barcode to add an item to inventory` | `- [x] **INVT-04**: User can scan a barcode to add an item to inventory` |
| 141 | `- [ ] **INVT-05**: Finalizing a meal plan auto-deducts ingredient quantities from inventory` | `- [~] **INVT-05**: Finalizing a meal plan auto-deducts ingredient quantities from inventory` |
| 142 | `- [ ] **INVT-06**: Uneaten portions from a recipe appear as leftover inventory items with expiry` | `- [~] **INVT-06**: Uneaten portions from a recipe appear as leftover inventory items with expiry` |
| 146 | `- [x] **GROC-01**: Grocery list is auto-generated from the active meal plan's ingredients` | `- [~] **GROC-01**: Grocery list is auto-generated from the active meal plan's ingredients` |
| 147 | `- [x] **GROC-02**: Grocery list subtracts items already in inventory ("have" vs "need to buy")` | `- [~] **GROC-02**: Grocery list subtracts items already in inventory ("have" vs "need to buy")` |
| 148 | `- [x] **GROC-03**: Grocery list items are grouped by store category (produce, dairy, etc.)` | `- [~] **GROC-03**: Grocery list items are grouped by store category (produce, dairy, etc.)` |
| 149 | `- [x] **GROC-04**: User can check off grocery items in-store` | `- [~] **GROC-04**: User can check off grocery items in-store` |
| 150 | `- [x] **GROC-05**: Grocery list can be shared with household members` | `- [~] **GROC-05**: Grocery list can be shared with household members` |
| 154 | `- [ ] **PLAN-01**: User can drag and drop meals between slots on the weekly plan grid` | `- [~] **PLAN-01**: User can drag and drop meals between slots on the weekly plan grid` |
| 155 | `- [ ] **PLAN-02**: User can auto-generate a meal plan optimized for nutrition, cost, schedule, and preferences` | `- [~] **PLAN-02**: User can auto-generate a meal plan optimized for nutrition, cost, schedule, and preferences` |
| 156 | `- [ ] **PLAN-03**: Manually placed meals are locked and preserved during auto-generation` | `- [~] **PLAN-03**: Manually placed meals are locked and preserved during auto-generation` |
| 157 | `- [ ] **PLAN-04**: Generated plan highlights nutrition gaps per member with swap suggestions` | `- [~] **PLAN-04**: Generated plan highlights nutrition gaps per member with swap suggestions` |
| 158 | `- [ ] **PLAN-05**: Recipe selection can prioritize using ingredients already in inventory` | `- [~] **PLAN-05**: Recipe selection can prioritize using ingredients already in inventory` |
| 162 | `- [ ] **FEED-01**: User can rate a recipe (1-5 stars) after eating it` | `- [~] **FEED-01**: User can rate a recipe (1-5 stars) after eating it` |
| 163 | `- [ ] **FEED-02**: Each household member can set dietary restrictions (allergens, vegetarian, gluten-free, etc.)` | `- [~] **FEED-02**: Each household member can set dietary restrictions (allergens, vegetarian, gluten-free, etc.)` |
| 164 | `- [ ] **FEED-03**: Each household member can set "won't eat" tags for selective eaters` | `- [~] **FEED-03**: Each household member can set "won't eat" tags for selective eaters` |
| 165 | `- [ ] **FEED-04**: System tracks recipe repeat rate and warns when plan becomes monotonous` | `- [~] **FEED-04**: System tracks recipe repeat rate and warns when plan becomes monotonous` |
| 169 | `- [ ] **SCHED-01**: Each household member can set availability windows per day (prep available, quick meal only, away)` | `- [x] **SCHED-01**: Each household member can set availability windows per day (prep available, quick meal only, away)` |
| 170 | `- [ ] **SCHED-02**: Plan generation respects member schedule constraints` | `- [x] **SCHED-02**: Plan generation respects member schedule constraints` |
| 174 | `- [ ] **PREP-01**: User can view batch prep suggestions for the week's meal plan` | `- [~] **PREP-01**: User can view batch prep suggestions for the week's meal plan` |
| 175 | `- [ ] **PREP-02**: User can view day-of task sequencing for a meal` | `- [x] **PREP-02**: User can view day-of task sequencing for a meal` |
| 176 | `- [ ] **PREP-03**: Freezer-friendly recipes are flagged for make-ahead prep` | `- [~] **PREP-03**: Freezer-friendly recipes are flagged for make-ahead prep` |
| 180 | `- [ ] **PORT-01**: Per-person portion suggestions use calorie targets as primary driver` | `- [~] **PORT-01**: Per-person portion suggestions use calorie targets as primary driver` |
| 181 | `- [ ] **PORT-02**: Portion models adapt based on feedback ratings and logged consumption history` | `- [~] **PORT-02**: Portion models adapt based on feedback ratings and logged consumption history` |
| 185 | `- [ ] **IMPORT-01**: User can paste a blog URL and get a complete recipe with ingredients and per-serving macros` | `- [x] **IMPORT-01**: User can paste a blog URL and get a complete recipe with ingredients and per-serving macros` |
| 186 | `- [ ] **IMPORT-02**: User can paste raw recipe text and get a complete recipe with ingredients and per-serving macros` | `- [x] **IMPORT-02**: User can paste raw recipe text and get a complete recipe with ingredients and per-serving macros` |
| 187 | `- [ ] **IMPORT-03**: User can paste a YouTube cooking video URL and get a recipe extracted from the transcript` | `- [~] **IMPORT-03**: User can paste a YouTube cooking video URL and get a recipe extracted from the transcript` |
| 188 | `- [ ] **IMPORT-04**: Imported recipe appears in the recipe builder ready to edit (name, servings, ingredients, steps, source attribution)` | `- [x] **IMPORT-04**: Imported recipe appears in the recipe builder ready to edit (name, servings, ingredients, steps, source attribution)` |
| 189 | `- [ ] **IMPORT-05**: Recipe import adds no new tables — only a nullable `recipes.source_url` column` | `- [x] **IMPORT-05**: Recipe import adds no new tables — only a nullable `recipes.source_url` column` |

**Critical:** BUDG-04 (line 133) is already `[x]`; leave it unchanged. Do NOT touch any v1.0/v1.1 bullet (lines 10-122) — those all use `[x]` + `Complete` per D-06 legacy preservation.

---

### EDIT 2 — v2.0 Traceability table (lines 293-328) complete rewrite per D-05/D-07/D-09/D-17

The entire v2.0 traceability block between line 291 (`| Requirement | Phase | Status |`) and line 328 (last IMPORT row) gets rewritten. Use the `Edit` tool with `old_string` matching the current 36-row block verbatim, and `new_string` being the exact table below. Leave the header line 289 (`**v2.0 Traceability:**`), the separator line 291 (`|-------------|-------|--------|`), and everything after line 328 untouched.

**Target state (copy verbatim — no whitespace drift, case-sensitive `Validated`/`Partial`/`Pending`, `(gap closure)` / `(wire-in)` suffixes preserved per D-08 EXCEPT IMPORT rows drop `Phase 29 (gap closure)` per D-09):**

```markdown
| BUDG-01 | Phase 16 | Validated |
| BUDG-02 | Phase 16 | Validated |
| BUDG-03 | Phase 16, Phase 26 (gap closure) | Partial |
| BUDG-04 | Phase 16 | Validated |
| INVT-01 | Phase 17 | Validated |
| INVT-02 | Phase 17 | Validated |
| INVT-03 | Phase 17 | Validated |
| INVT-04 | Phase 17 | Validated |
| INVT-05 | Phase 17, Phase 26 (gap closure) | Partial |
| INVT-06 | Phase 17, Phase 26 (gap closure) | Partial |
| GROC-01 | Phase 18 | Partial |
| GROC-02 | Phase 18 | Partial |
| GROC-03 | Phase 18 | Partial |
| GROC-04 | Phase 18 | Partial |
| GROC-05 | Phase 18 | Partial |
| PLAN-01 | Phase 19 | Partial |
| PLAN-02 | Phase 22 | Partial |
| PLAN-03 | Phase 19 | Partial |
| PLAN-04 | Phase 22 | Partial |
| PLAN-05 | Phase 22 | Partial |
| FEED-01 | Phase 20 | Partial |
| FEED-02 | Phase 20 | Partial |
| FEED-03 | Phase 20 | Partial |
| FEED-04 | Phase 20 | Partial |
| SCHED-01 | Phase 21, Phase 27 (gap closure) | Validated |
| SCHED-02 | Phase 21, Phase 27 (gap closure) | Validated |
| PREP-01 | Phase 23 | Partial |
| PREP-02 | Phase 23, Phase 28 (wire-in) | Validated |
| PREP-03 | Phase 23 | Partial |
| PORT-01 | Phase 24 | Partial |
| PORT-02 | Phase 24 | Partial |
| IMPORT-01 | Phase 25 | Validated |
| IMPORT-02 | Phase 25 | Validated |
| IMPORT-03 | Phase 25 | Partial |
| IMPORT-04 | Phase 25 | Validated |
| IMPORT-05 | Phase 25 | Validated |
```

**Rationale table (DO NOT include this in the file — internal reasoning only):**

| Req | Target | Driver |
|-----|--------|--------|
| BUDG-01/02 | Validated | 16-VERIFICATION.md status=passed, empty human_verification |
| BUDG-03 | Partial | 26-VERIFICATION.md human_needed (grocery reconcile criterion #5) |
| BUDG-04 | Validated | was `Complete` (legacy); normalise to v2.0 vocab per D-06/D-07 |
| INVT-01..04 | Validated | 17-VERIFICATION.md retrospective (Plan 01), all criteria VALIDATED |
| INVT-05/06 | Partial | 26-VERIFICATION.md human_needed (same criterion #5 as BUDG-03) |
| GROC-01..05 | Partial | 18-VERIFICATION.md status=passed BUT human_verification populated (realtime sync test); per D-05 → Partial. Was `Complete` (legacy drift); normalise. |
| PLAN-01/03 | Partial | 19-VERIFICATION.md status=human_needed (DnD mobile + lock badge criteria) |
| PLAN-02/04/05 | Partial | 22-VERIFICATION.md status=passed + populated human_verification (live edge function run + NutritionGapCard swap UAT) |
| FEED-01..04 | Partial | 20-VERIFICATION.md status=passed + populated human_verification (rating card flow + dietary restrictions Settings) |
| SCHED-01/02 | Validated | 27-VERIFICATION.md status=passed + EMPTY human_verification closes the wiring gap that Phase 21 flagged; Phase 21's badges-on-SlotCards criterion was the exact gap Phase 27 gap-closure addressed |
| PREP-01/03 | Partial | 23-VERIFICATION.md status=passed + populated human_verification (batch prep modal UAT) |
| PREP-02 | Validated | unchanged — already `Validated` via Phase 28 wire-in; 28-VERIFICATION.md confirms |
| PORT-01/02 | Partial | 24-VERIFICATION.md status=human_needed (deploy check + ROADMAP reconcile). Plan 04 resolves the ROADMAP-reconcile criterion but the deploy-check criterion remains; Partial is correct until a future UAT closes the deploy check. |
| IMPORT-01/02/04/05 | Validated | 25-VERIFICATION.md retrospective (Plan 02), 4/5 criteria VALIDATED from Playwright UAT |
| IMPORT-03 | Partial | 25-VERIFICATION.md human_needed entry (YouTube transcript unmeasured) |

**Critical hygiene rules (from D-06, D-08, D-09, 29-PATTERNS.md integration hazards 1-3):**
1. Case-sensitive: exactly `Validated`, `Partial`, `Pending` (no lowercase, no `VALIDATED`, no `Verified`).
2. Suffix hygiene per D-08: keep `(gap closure)` / `(wire-in)` where a phase did CODE work (BUDG-03, INVT-05, INVT-06, SCHED-01, SCHED-02, PREP-02). Per D-09: drop `Phase 29 (gap closure)` from IMPORT rows because Phase 29 adds no code.
3. Zero v2.0 rows should end with `| Complete |` after edit (D-07 normalization). The string `Complete` should only appear in v1.0/v1.1 rows (lines 230-279, untouched).
4. Preserve the table header `| Requirement | Phase | Status |` and separator `|-------------|-------|--------|` at lines 291-292 exactly as-is.
5. Preserve line 320 format `| PREP-02 | Phase 23, Phase 28 (wire-in) | Validated |` exactly — this is the canonical Phase 28 precedent and must survive byte-identical.

**Do NOT touch:**
- Lines 10-122 (v1.0/v1.1 requirement bullets — all `[x]`, all `Complete` per D-06)
- Lines 178-181 (Portioning section bullets — line 181 PORT-02 gets checkbox flip in Edit 1 but the deviation blockquote append is Plan 04's job, not this plan's)
- Lines 230-279 (v1.0/v1.1 traceability block)
- Lines 281-287 (Coverage block)
- Line 330-337 (v1.0/v1.1 Traceability footer + metadata)

After both edits are applied, run `npx tsc --noEmit` is NOT required (doc-only change). Verification is grep-based.
  </action>
  <verify>
    <automated>grep -c "^| IMPORT-01 | Phase 25 | Validated |$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^| IMPORT-03 | Phase 25 | Partial |$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^| IMPORT-05 | Phase 25 | Validated |$" .planning/REQUIREMENTS.md | grep -q "^1$" && ! grep -q "Phase 29 (gap closure)" .planning/REQUIREMENTS.md && grep -c "^| PREP-02 | Phase 23, Phase 28 (wire-in) | Validated |$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^| SCHED-01 | Phase 21, Phase 27 (gap closure) | Validated |$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^| BUDG-04 | Phase 16 | Validated |$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^- \[x\] \*\*IMPORT-01\*\*:" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^- \[~\] \*\*IMPORT-03\*\*:" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^- \[x\] \*\*SCHED-01\*\*:" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^- \[~\] \*\*PLAN-02\*\*:" .planning/REQUIREMENTS.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    **v2.0 Traceability table — IMPORT rows (D-17 flip + D-09 suffix drop):**
    - `grep -c "^| IMPORT-01 | Phase 25 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| IMPORT-02 | Phase 25 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| IMPORT-03 | Phase 25 | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| IMPORT-04 | Phase 25 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| IMPORT-05 | Phase 25 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "Phase 29 (gap closure)" .planning/REQUIREMENTS.md` returns 0 (suffix fully dropped per D-09)

    **v2.0 Traceability table — BUDG rows:**
    - `grep -c "^| BUDG-01 | Phase 16 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| BUDG-02 | Phase 16 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| BUDG-03 | Phase 16, Phase 26 (gap closure) | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| BUDG-04 | Phase 16 | Validated |$" .planning/REQUIREMENTS.md` returns 1 (was `Complete`, now normalised)

    **v2.0 Traceability table — INVT rows:**
    - `grep -c "^| INVT-01 | Phase 17 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| INVT-04 | Phase 17 | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| INVT-05 | Phase 17, Phase 26 (gap closure) | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| INVT-06 | Phase 17, Phase 26 (gap closure) | Partial |$" .planning/REQUIREMENTS.md` returns 1

    **v2.0 Traceability table — GROC/PLAN/FEED rows (all Partial):**
    - `grep -c "^| GROC-0[1-5] | Phase 18 | Partial |$" .planning/REQUIREMENTS.md` returns 5
    - `grep -c "^| PLAN-01 | Phase 19 | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| PLAN-02 | Phase 22 | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| FEED-0[1-4] | Phase 20 | Partial |$" .planning/REQUIREMENTS.md` returns 4

    **v2.0 Traceability table — SCHED/PREP/PORT rows:**
    - `grep -c "^| SCHED-01 | Phase 21, Phase 27 (gap closure) | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| SCHED-02 | Phase 21, Phase 27 (gap closure) | Validated |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| PREP-01 | Phase 23 | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| PREP-02 | Phase 23, Phase 28 (wire-in) | Validated |$" .planning/REQUIREMENTS.md` returns 1 (unchanged — Phase 28 precedent)
    - `grep -c "^| PREP-03 | Phase 23 | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| PORT-01 | Phase 24 | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^| PORT-02 | Phase 24 | Partial |$" .planning/REQUIREMENTS.md` returns 1

    **v2.0 Requirement-bullet checkbox alignment (D-18):**
    - `grep -c "^- \[x\] \*\*BUDG-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*BUDG-03\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*INVT-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*INVT-05\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*GROC-01\*\*:" .planning/REQUIREMENTS.md` returns 1 (was `[x]`)
    - `grep -c "^- \[~\] \*\*PLAN-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*FEED-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*SCHED-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*SCHED-02\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*PREP-02\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*PREP-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*PREP-03\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*PORT-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*PORT-02\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*IMPORT-01\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[~\] \*\*IMPORT-03\*\*:" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*IMPORT-05\*\*:" .planning/REQUIREMENTS.md` returns 1

    **Vocabulary hygiene (D-06/D-07 — normalise v2.0 to Validated/Partial/Pending, `Complete` stays for v1.0 rows only):**
    - `grep -c "^| .* | Phase .* | Complete |$" .planning/REQUIREMENTS.md` returns exactly 50 (v1.0/v1.1 rows — byte-identical preservation). If it returns fewer than 50 → v1.0 rows were accidentally touched (revert). If it returns MORE than 50 → some v2.0 row still has `Complete` (scrub and rerun).
    - `grep -cE "^| (BUDG|INVT|GROC|PLAN|FEED|SCHED|PREP|PORT|IMPORT)-[0-9]+ \| .* \| Complete \|$" .planning/REQUIREMENTS.md` returns 0 (no v2.0 row uses `Complete`)
    - `grep -cE "^| (BUDG|INVT|GROC|PLAN|FEED|SCHED|PREP|PORT|IMPORT)-[0-9]+ \| .* \| Pending \|$" .planning/REQUIREMENTS.md` returns 0 (no v2.0 row left `Pending`)

    **Untouched-area preservation:**
    - `grep -c "^- \[x\] \*\*BUDG-04\*\*:" .planning/REQUIREMENTS.md` returns 1 (BUDG-04 bullet was already `[x]` — must stay `[x]`)
    - `wc -l .planning/REQUIREMENTS.md` returns between 334 and 340 lines (minor length drift expected for checkbox char swaps `[x]`→`[~]` and status-string-length changes; no section removals)
    - `grep -c "^**v2.0 Traceability:**$" .planning/REQUIREMENTS.md` returns 1 (header preserved)
    - `grep -c "^| AUTH-01 | Phase 1 | Complete |$" .planning/REQUIREMENTS.md` returns 1 (first v1.0 row preserved byte-identical)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md traceability sweep complete: IMPORT rows flipped per D-17/D-09, all 36 v2.0 traceability rows use `Validated`/`Partial` vocabulary per D-05/D-07, every v2.0 requirement bullet checkbox aligned with its row status per D-18, and v1.0/v1.1 rows left byte-identical per D-06 legacy preservation. WARN-04 is closed. Plan 04 can proceed to append the PORT-02 deviation blockquote to REQUIREMENTS.md line 181 without re-editing any of the checkbox/status work done here.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure documentation edit to a markdown file under version control. No runtime, no user input, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-03 | Tampering | `.planning/REQUIREMENTS.md` traceability table (downstream gsd-sdk tooling parses status strings) | mitigate | Acceptance criteria above enforce exact case-sensitive spellings (`Validated`/`Partial`/`Pending`). Any drift fails the grep-based verification before commit. |
| T-29-04 | Information disclosure | N/A | accept | No secrets or PII in a traceability table; all values are already in public git history. |

All other STRIDE categories are **N/A** — documentation files have no runtime threat surface.
</threat_model>

<verification>
- Every D-17 IMPORT row target state grep-matches exactly (5 grep assertions)
- `Phase 29 (gap closure)` string returns zero matches (D-09 enforcement)
- Every D-18 checkbox state matches its row status (17 grep assertions covering one representative per status class per family)
- Zero v2.0 rows retain `Complete` or `Pending` statuses (3 negative grep assertions)
- v1.0/v1.1 legacy preserved: AUTH-01 row byte-identical, total `Complete` row count stays at 50
</verification>

<success_criteria>
WARN-04 (traceability staleness) is closed. REQUIREMENTS.md v2.0 section (lines 130-189 bullets + lines 293-328 traceability) is the single source of truth for requirement status and every downstream tool / future planner / milestone archive can trust the status strings verbatim. Plan 04 can append the PORT-02 deviation blockquote without conflict.
</success_criteria>

<output>
After completion, create `.planning/phases/29-v2-0-documentation-reconciliation/29-03-SUMMARY.md` recording:
- Before/after row counts per status tier for v2.0 traceability
- Evidence that v1.0/v1.1 legacy rows were untouched (AUTH-01 grep match)
- List of the 5 IMPORT status flips with before/after
- List of the 36 v2.0 requirement bullets whose checkboxes were re-aligned (source of truth for Plan 04 to verify no re-edit is needed)
</output>
