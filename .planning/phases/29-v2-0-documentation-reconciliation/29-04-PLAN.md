---
phase: 29-v2-0-documentation-reconciliation
plan: 04
type: execute
wave: 3
depends_on: [29-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: []
tags: [documentation, deviation, phase-24, port-02, roadmap-reconciliation]

must_haves:
  truths:
    - "ROADMAP.md Phase 24 block (lines 471-483) has a deviation blockquote appended immediately below the 'UI hint: yes' line (line 482)"
    - "The deviation blockquote uses the exact D-16 standardised template (verbatim from 29-CONTEXT.md specifics lines 148-155)"
    - "REQUIREMENTS.md PORT-02 requirement bullet (line 181) has the same deviation blockquote indented below it (matching 29-PATTERNS.md lines 254-262 pattern)"
    - "Phase 24 original Goal line + Success Criteria lines are byte-identical before and after (D-15: 'Annotate in place, do not rewrite')"
    - "PORT-02 original bullet text is byte-identical before and after (D-15 principle: annotate, don't edit)"
    - "Checkbox states set by Plan 03 (PORT-02 = [~]) remain intact — Plan 04 appends, never touches the bullet line itself"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 24 block with D-02 pivot deviation blockquote appended"
      contains: "> **D-02 pivot (2026-04-15):**"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PORT-02 bullet with D-02 pivot deviation blockquote appended"
      contains: "> **D-02 pivot (2026-04-15):**"
  key_links:
    - from: ".planning/ROADMAP.md Phase 24 block"
      to: ".planning/phases/24-dynamic-portioning/24-CONTEXT.md D-02"
      via: "Deviation blockquote pointer"
      pattern: "24-dynamic-portioning/24-CONTEXT.md"
    - from: ".planning/REQUIREMENTS.md PORT-02 bullet"
      to: ".planning/phases/24-dynamic-portioning/24-CONTEXT.md D-02"
      via: "Deviation blockquote pointer"
      pattern: "24-dynamic-portioning/24-CONTEXT.md"
---

<objective>
Append the D-16 standardised deviation blockquote to TWO locations to close WARN-05 (Phase 24 ROADMAP vs shipped scope mismatch):

1. `.planning/ROADMAP.md` Phase 24 block — immediately after the `**UI hint**: yes` line (line 482)
2. `.planning/REQUIREMENTS.md` PORT-02 bullet (line 181) — as indented continuation below the bullet

Both blockquotes are byte-identical (same D-16 template). Per D-15: "Annotate in place, do not rewrite." — the original Phase 24 Goal / Success Criteria / PORT-02 text stays untouched. This establishes the in-repo precedent for the D-16 deviation-annotation pattern (no existing analogs — this plan creates the pattern).

This plan addresses ZERO phase requirement IDs (IMPORT-01..05 are covered by Plans 02 and 03). It is the final doc-drift closure before the v2.0 milestone can archive.

Depends on Plan 03 because both plans edit `.planning/REQUIREMENTS.md` — Plan 03 touches line 181's checkbox (`[ ]` → `[~]`); Plan 04 appends a blockquote beneath it. Sequential ordering prevents file-overlap conflicts. ROADMAP.md has no overlap with any earlier plan.

Purpose: Close WARN-05 (final v2.0 audit doc-drift item). After this plan lands, the milestone is ready for `/gsd-complete-milestone` (out of scope per 29-CONTEXT.md Deferred Ideas).
Output: Two appended blockquotes (no rewrites, no deletions). Expected line-delta: +6 lines in ROADMAP.md, +6 lines in REQUIREMENTS.md (blockquote is 5 wrapped lines + one blank separator).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md
@.planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md
@.planning/phases/24-dynamic-portioning/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append D-16 deviation blockquote to ROADMAP.md Phase 24 block</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 471-485 (Phase 24 block — confirm current line 482 ends with `**UI hint**: yes` and line 483 is currently blank; the blockquote appends between these)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md lines 65-68 (D-15 "annotate in place, do not rewrite" rule), lines 148-155 (D-16 exact template)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md lines 279-307 (ROADMAP Phase 24 block pattern — APPEND rule, no-rewrite enforcement), lines 459-475 (Deviation blockquote format rules — markdown quotes, bold label, ISO date, CONTEXT pointer)
    - .planning/phases/24-dynamic-portioning/24-CONTEXT.md (confirm D-02 is the decision being cited — the blockquote references this file)
  </read_first>
  <action>
Use the `Edit` tool to append the D-16 deviation blockquote inside the Phase 24 block in `.planning/ROADMAP.md`.

**Current state of the Phase 24 block (verified from lines 471-483 — use this as the `old_string`):**

```markdown
### Phase 24: Dynamic Portioning
**Goal**: Portion suggestions adapt over time based on each member's satiety feedback and consumption history, moving beyond static calorie-target splits
**Depends on**: Phase 20
**Requirements**: PORT-01, PORT-02
**Success Criteria** (what must be TRUE):
  1. Per-member portion suggestions use each member's calorie target as the primary driver — members with higher targets receive proportionally larger suggested portions
  2. When a member has logged a recipe multiple times and consistently adjusts the suggested portion, the system adapts future suggestions for that recipe toward the observed amount
**Plans**: 2 plans
Plans:
- [x] 24-01-PLAN.md — RecipeMixPanel component (three-slider panel with localStorage persistence) and PlanGrid wiring
- [x] 24-02-PLAN.md — generate-plan edge function enrichment (cook frequency, last-cooked, per-member ratings, cost per serving, tier-aware AI prompts) + redeploy
**UI hint**: yes
```

**Target state (same block with deviation blockquote appended — use this as the `new_string`):**

```markdown
### Phase 24: Dynamic Portioning
**Goal**: Portion suggestions adapt over time based on each member's satiety feedback and consumption history, moving beyond static calorie-target splits
**Depends on**: Phase 20
**Requirements**: PORT-01, PORT-02
**Success Criteria** (what must be TRUE):
  1. Per-member portion suggestions use each member's calorie target as the primary driver — members with higher targets receive proportionally larger suggested portions
  2. When a member has logged a recipe multiple times and consistently adjusts the suggested portion, the system adapts future suggestions for that recipe toward the observed amount
**Plans**: 2 plans
Plans:
- [x] 24-01-PLAN.md — RecipeMixPanel component (three-slider panel with localStorage persistence) and PlanGrid wiring
- [x] 24-02-PLAN.md — generate-plan edge function enrichment (cook frequency, last-cooked, per-member ratings, cost per serving, tier-aware AI prompts) + redeploy
**UI hint**: yes

> **D-02 pivot (2026-04-15):** Shipped scope is tier-aware recipe selection
> (generate-plan edge function enrichment + RecipeMixPanel three-slider
> component) rather than portion-size adaptation. Portions remain
> calorie-target-driven (PORT-01). See `.planning/phases/24-dynamic-portioning/24-CONTEXT.md`
> D-02 for the full rationale.
```

**Critical rules (from D-15, D-16, 29-PATTERNS.md):**
1. **Do NOT edit the Goal line** — it stays verbatim ("Portion suggestions adapt over time..."). The blockquote documents that the goal was changed; it does not rewrite the goal.
2. **Do NOT edit the Success Criteria** — both criterion 1 and criterion 2 stay byte-identical.
3. **Do NOT edit the Plans list** — `24-01-PLAN.md` and `24-02-PLAN.md` descriptions stay verbatim.
4. The blockquote immediately follows `**UI hint**: yes` with ONE blank line between, then the 5 blockquote lines, then whatever blank line(s) originally separated Phase 24 from Phase 25. The Edit tool's `new_string` above preserves ALL of this exactly.
5. Bold label is `**D-02 pivot (2026-04-15):**` — no variations (not `D-02 scope change`, not `Phase 24 pivot`).
6. Date is the D-02 decision date from 24-CONTEXT.md (2026-04-15), NOT today's date. The blockquote records WHEN the scope pivot was decided, not when the annotation was added.
7. The blockquote is 5 lines, each starting with `> `. Line wrapping is at specific points that match 29-CONTEXT.md lines 148-155 — preserve the exact wrap points in the template above.
  </action>
  <verify>
    <automated>grep -q "^> \*\*D-02 pivot (2026-04-15):\*\* Shipped scope is tier-aware recipe selection$" .planning/ROADMAP.md && grep -q "^> (generate-plan edge function enrichment + RecipeMixPanel three-slider$" .planning/ROADMAP.md && grep -q "^> D-02 for the full rationale\.$" .planning/ROADMAP.md && grep -q "^\*\*Goal\*\*: Portion suggestions adapt over time based on each member's satiety feedback and consumption history" .planning/ROADMAP.md && grep -q "^- \[x\] 24-01-PLAN\.md — RecipeMixPanel component" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^> \*\*D-02 pivot (2026-04-15):\*\* Shipped scope is tier-aware recipe selection$" .planning/ROADMAP.md` returns 1
    - `grep -c "^> (generate-plan edge function enrichment + RecipeMixPanel three-slider$" .planning/ROADMAP.md` returns 1
    - `grep -c "^> component) rather than portion-size adaptation\. Portions remain$" .planning/ROADMAP.md` returns 1
    - `grep -c "^> calorie-target-driven (PORT-01)\. See \`\.planning/phases/24-dynamic-portioning/24-CONTEXT\.md\`$" .planning/ROADMAP.md` returns 1
    - `grep -c "^> D-02 for the full rationale\.$" .planning/ROADMAP.md` returns 1
    - Original Phase 24 Goal line preserved byte-identical: `grep -c "^\*\*Goal\*\*: Portion suggestions adapt over time based on each member's satiety feedback and consumption history, moving beyond static calorie-target splits$" .planning/ROADMAP.md` returns 1
    - Original Success Criterion 1 preserved: `grep -c "Per-member portion suggestions use each member's calorie target as the primary driver" .planning/ROADMAP.md` returns 1
    - Original Success Criterion 2 preserved: `grep -c "When a member has logged a recipe multiple times and consistently adjusts the suggested portion" .planning/ROADMAP.md` returns 1
    - Both Plan bullets preserved: `grep -c "^- \[x\] 24-01-PLAN\.md — RecipeMixPanel component" .planning/ROADMAP.md` returns 1 AND `grep -c "^- \[x\] 24-02-PLAN\.md — generate-plan edge function enrichment" .planning/ROADMAP.md` returns 1
    - Phase 25 header still exists and is unchanged: `grep -c "^### Phase 25: Universal Recipe Import$" .planning/ROADMAP.md` returns 1
    - File length delta: `wc -l .planning/ROADMAP.md` returns a value between (original+5) and (original+7) lines (5 new blockquote lines plus an optional blank-line adjustment)
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 24 block has the D-16 deviation blockquote appended immediately after `**UI hint**: yes`. Original Goal, Success Criteria, and Plans list are byte-identical. The in-repo pattern for deviation annotations is now established (first use). WARN-05 (ROADMAP half) is closed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append D-16 deviation blockquote to REQUIREMENTS.md PORT-02 bullet</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md line 181 — CONFIRM Plan 03 already flipped the checkbox to `- [~] **PORT-02**: Portion models adapt based on feedback ratings and logged consumption history`. If the checkbox is still `[ ]`, Plan 03 did not complete — halt and return to Plan 03 before continuing.
    - .planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md lines 65-68 (D-15), lines 148-155 (D-16 exact template)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md lines 247-262 (PORT-02 deviation blockquote pattern — APPEND below bullet, indented continuation)
    - .planning/phases/24-dynamic-portioning/24-CONTEXT.md (confirms the D-02 decision being cited)
  </read_first>
  <action>
Use the `Edit` tool to append the D-16 deviation blockquote beneath the PORT-02 bullet in `.planning/REQUIREMENTS.md`.

**Current state after Plan 03 (verified — line 181 is the PORT-02 bullet, which Plan 03 flipped to `[~]`):**

```markdown
- [~] **PORT-02**: Portion models adapt based on feedback ratings and logged consumption history
```

**Target state (use this as `new_string` in the Edit call):**

```markdown
- [~] **PORT-02**: Portion models adapt based on feedback ratings and logged consumption history
  > **D-02 pivot (2026-04-15):** Shipped scope is tier-aware recipe selection
  > (generate-plan edge function enrichment + RecipeMixPanel three-slider
  > component) rather than portion-size adaptation. Portions remain
  > calorie-target-driven (PORT-01). See `.planning/phases/24-dynamic-portioning/24-CONTEXT.md`
  > D-02 for the full rationale.
```

**Critical rules:**
1. The blockquote is indented with TWO SPACES before `> ` — this is the Markdown indented-continuation pattern for bullet-list children (per 29-PATTERNS.md lines 254-262). The ROADMAP version (Task 1 above) uses no indent because it follows a `**UI hint**:` line, not a bullet.
2. Bullet text `- [~] **PORT-02**: Portion models adapt based on feedback ratings and logged consumption history` stays byte-identical (D-15: annotate, don't rewrite).
3. The `[~]` checkbox set by Plan 03 MUST be preserved. If Plan 03 did not flip it and line 181 still reads `- [ ] **PORT-02**`, stop and re-run Plan 03 — do NOT silently set the checkbox here (Plan 03 owns that change per the acceptance criteria there).
4. The blockquote body is byte-identical to Task 1's ROADMAP blockquote (same template, just indented by 2 spaces).
5. The next line after the blockquote is still `### Recipe Import` (line 183 in the current REQUIREMENTS.md — will shift by +5 after this edit) — do NOT alter the `### Recipe Import` section header or any of the IMPORT bullets beneath it.

**Edge case — Edit tool uniqueness:** Since the ROADMAP version of the blockquote (Task 1) already exists in another file by the time this task runs, and REQUIREMENTS.md has no other PORT-02 bullet or D-02 pivot blockquote, the `old_string` of the current PORT-02 bullet line is unique within REQUIREMENTS.md. Use that full line as the match target.
  </action>
  <verify>
    <automated>grep -c "^- \[~\] \*\*PORT-02\*\*: Portion models adapt based on feedback ratings and logged consumption history$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^  > \*\*D-02 pivot (2026-04-15):\*\* Shipped scope is tier-aware recipe selection$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^  > D-02 for the full rationale\.$" .planning/REQUIREMENTS.md | grep -q "^1$" && grep -c "^### Recipe Import$" .planning/REQUIREMENTS.md | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[~\] \*\*PORT-02\*\*: Portion models adapt based on feedback ratings and logged consumption history$" .planning/REQUIREMENTS.md` returns 1 (bullet untouched, Plan 03's checkbox flip preserved)
    - `grep -c "^  > \*\*D-02 pivot (2026-04-15):\*\* Shipped scope is tier-aware recipe selection$" .planning/REQUIREMENTS.md` returns 1 (two-space indent preserved)
    - `grep -c "^  > (generate-plan edge function enrichment + RecipeMixPanel three-slider$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^  > component) rather than portion-size adaptation\. Portions remain$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^  > calorie-target-driven (PORT-01)\. See \`\.planning/phases/24-dynamic-portioning/24-CONTEXT\.md\`$" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^  > D-02 for the full rationale\.$" .planning/REQUIREMENTS.md` returns 1
    - `### Recipe Import` section header still exists and is unshifted in role: `grep -c "^### Recipe Import$" .planning/REQUIREMENTS.md` returns 1
    - PORT-01 bullet NOT touched: `grep -c "^- \[~\] \*\*PORT-01\*\*: Per-person portion suggestions use calorie targets as primary driver$" .planning/REQUIREMENTS.md` returns 1 (from Plan 03)
    - All 5 IMPORT bullets still exist beneath `### Recipe Import` (byte-identical to Plan 03 state): `grep -c "^- \[x\] \*\*IMPORT-01\*\*:" .planning/REQUIREMENTS.md` returns 1, `grep -c "^- \[~\] \*\*IMPORT-03\*\*:" .planning/REQUIREMENTS.md` returns 1
    - Plan 03's v2.0 traceability rows still pass: `grep -c "^| IMPORT-01 | Phase 25 | Validated |$" .planning/REQUIREMENTS.md` returns 1, `grep -c "^| PORT-02 | Phase 24 | Partial |$" .planning/REQUIREMENTS.md` returns 1
    - File length delta from Plan 03 end state: `wc -l .planning/REQUIREMENTS.md` returns between (plan-03-end-lines + 5) and (plan-03-end-lines + 6) lines
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md PORT-02 bullet now has the D-16 deviation blockquote appended as an indented continuation, bullet text byte-identical, Plan 03's checkbox state (`[~]`) preserved, `### Recipe Import` header and all IMPORT bullets untouched. WARN-05 (REQUIREMENTS half) is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — pure documentation appends. No runtime surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering | `.planning/ROADMAP.md` Phase 24 block | accept | Git version control; blockquote is additive, original text preserved byte-identical per D-15. |
| T-29-06 | Tampering | `.planning/REQUIREMENTS.md` PORT-02 bullet | accept | Same. |

All other STRIDE categories are **N/A**.
</threat_model>

<verification>
- ROADMAP.md Phase 24 block has 5-line D-16 blockquote appended (5 grep matches, all exact)
- REQUIREMENTS.md PORT-02 bullet has indented 5-line D-16 blockquote (5 grep matches, all with leading 2-space indent)
- Both blockquotes cite the same CONTEXT.md file and use the same verbatim template
- Zero original text was modified in either file (Goal / Success Criteria / Plans list / bullet text all byte-identical)
- Plan 03's upstream work (v2.0 traceability table, checkbox states) is still intact after Plan 04 edits
</verification>

<success_criteria>
WARN-05 is closed and the D-16 deviation-annotation pattern is established in the repo for future pivots. All four v2.0 milestone doc-drift items (WARN-02, WARN-03, WARN-04, WARN-05) are now resolved:
- WARN-02 closed by pre-existing IMPORT descriptions at REQUIREMENTS.md:185-189 (confirmed correct per D-17)
- WARN-03 closed by Plans 01 + 02 (retrospective VERIFICATION.md files)
- WARN-04 closed by Plan 03 (traceability sweep + checkbox alignment)
- WARN-05 closed by Plan 04 (this plan — ROADMAP + PORT-02 deviation blockquotes)
</success_criteria>

<output>
After completion, create `.planning/phases/29-v2-0-documentation-reconciliation/29-04-SUMMARY.md` recording:
- Exact line numbers where the two blockquotes now live
- Line-count delta for each file (expected ~+5 lines each)
- Byte-identical preservation confirmation for Phase 24 Goal / Success Criteria / Plans and PORT-02 bullet text
- WARN-02/03/04/05 closure summary (all four now closed — milestone archive ready pending GuidePage refresh in Plan 05)
</output>
