---
phase: 29-v2-0-documentation-reconciliation
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/pages/GuidePage.tsx
autonomous: true
requirements: [IMPORT-01, IMPORT-02, IMPORT-03, IMPORT-04, IMPORT-05]
tags: [frontend, documentation, copy, guide, v2-features]

must_haves:
  truths:
    - "GUIDE_SECTIONS array contains exactly 12 sections in the D-11 target order"
    - "The 6 existing v1.0 section ids (`getting-started`, `adding-foods`, `recipes`, `meal-plan`, `tracking`, `household-admin`) all still exist (tests/guide.test.ts assertions preserved)"
    - "6 new section ids added: `recipe-import`, `inventory`, `grocery-list`, `prep-schedule`, `budget`, `recipe-mix`"
    - "Total: 6 preserved + 6 new = 12 per D-11"
    - "QUICK_START_STEPS has at most 6 entries per D-13 (up from 5), includes a step about recipe import or plan generation"
    - "Quick-start header text matches the new step count"
    - "GuideSection interface is NOT extended — still exactly 5 fields (id, title, intro, steps, optional tips) per D-12"
    - "Every new section uses the imperative second-person voice pattern from `adding-foods` (29-PATTERNS.md lines 331-358)"
    - "tests/guide.test.ts existing assertions still satisfied by file contents — `id: 'getting-started'`, `id: 'adding-foods'`, `id: 'recipes'`, `id: 'meal-plan'`, `id: 'tracking'`, `id: 'household-admin'` all still exist as literal strings"
    - "`npx tsc --noEmit` exits 0 per 29-CONTEXT.md Deferred Ideas (pre-existing TS errors in codebase are not this plan's concern but the edited file itself must typecheck)"
  artifacts:
    - path: "src/pages/GuidePage.tsx"
      provides: "Full v2.0-refreshed user guide with 12 accordion sections interleaved by workflow per D-11"
      contains: "id: 'recipe-import'"
  key_links:
    - from: "src/pages/GuidePage.tsx GUIDE_SECTIONS array"
      to: "GuideSection interface (lines 4-10)"
      via: "TypeScript structural type check"
      pattern: "interface GuideSection"
    - from: "src/pages/GuidePage.tsx QUICK_START_STEPS"
      to: "Render surface (~line 144 'Get started in N steps' header)"
      via: "String interpolation / hardcoded N"
      pattern: "Get started in"
---

<objective>
Refresh `src/pages/GuidePage.tsx` with 6 new accordion sections covering every v2.0 feature family (Recipe Import, Inventory, Grocery List, Prep & Schedule, Budget, Tier-aware Recipe Mix) and lightly edit 2 existing sections (`meal-plan` for PLAN family generate-plan/DnD/locking; `tracking` for Cook Mode + ratings) per D-10..D-14. Section order matches D-11 (workflow-interleaved, not appended). `QUICK_START_STEPS` is updated to ≤ 6 entries per D-13 with a new step for recipe import or plan generation.

This plan addresses ALL FIVE phase requirement IDs: IMPORT-01..IMPORT-05. The new `recipe-import` accordion section documents the end-user workflow that corresponds to those five requirements (paste URL / paste text / YouTube / edit in RecipeBuilder / source attribution). Because GuidePage is a copy document, the ties are by content rather than by code-level wiring.

Purpose: Close the folded-in Phase 29 scope (D-10..D-14) so the in-app guide is accurate for v2.0. This is the user-facing deliverable of Phase 29.

Output: Edits to `src/pages/GuidePage.tsx` only — no new files, no new hooks, no new components, no route changes.

**Contract gate:** `npx tsc --noEmit` exits 0 per 29-CONTEXT.md Deferred Ideas (line 176). Vitest coverage for copy-only edits is NOT required; `tests/guide.test.ts` already has 6 pre-existing failing tests on baseline per STATE.md / 26-VERIFICATION.md footnote — this plan must NOT add new failures, but does not need to fix existing ones. The preservation test (`GuidePage contains all 6 major feature sections`) still passes because all 6 original ids survive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md
@.planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md
@CLAUDE.md
@lessons.md

<interfaces>
<!-- The GuideSection interface is LOCKED per D-12 — do NOT extend. -->
<!-- Executor should use this shape verbatim for every new section entry. -->

From src/pages/GuidePage.tsx:4-10:
```typescript
interface GuideSection {
  id: string
  title: string
  intro: string
  steps: string[]
  tips?: string[]
}
```

From src/pages/GuidePage.tsx:12-18 (current QUICK_START_STEPS, to be updated):
```typescript
const QUICK_START_STEPS = [
  'Sign in or create your account',
  'Add foods to your food library',
  'Build a recipe from your foods',
  'Put your recipe into a meal plan',
  'Log what you eat today',
]
```

From src/pages/GuidePage.tsx:143 (render-surface header, to be updated if step count changes):
```typescript
<h2 className="font-bold text-text mb-2">Get started in 5 steps</h2>
```

tests/guide.test.ts pre-existing literal assertions this plan MUST preserve (per D-14 + 29-PATTERNS.md integration hazard):
- `id: 'getting-started'`
- `id: 'adding-foods'`
- `id: 'recipes'`
- `id: 'meal-plan'`
- `id: 'tracking'`
- `id: 'household-admin'`

Existing tests/guide.test.ts also asserts `Get started in 5 steps` — this string WILL change if QUICK_START_STEPS grows to 6. That test is already pre-existing failing on baseline per STATE.md, so the failure count does not widen — but be aware the assertion will continue to fail against the new header text. No fix required per 29-CONTEXT.md Deferred Ideas.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update QUICK_START_STEPS (≤ 6 entries, include recipe-import/plan-generation) and quick-start header</name>
  <files>src/pages/GuidePage.tsx</files>
  <read_first>
    - src/pages/GuidePage.tsx full file — identify lines 12-18 (QUICK_START_STEPS) and line 143 (header `Get started in 5 steps`)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md lines 62-63 (D-13 rule: include one step covering recipe import or plan generation; total ≤ 6)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md lines 381-413 (QUICK_START_STEPS update pattern + header update)
    - tests/guide.test.ts (confirm existing literal assertion `'Get started in 5 steps'` — this will become stale against the new header and is already part of the pre-existing baseline failure set per STATE.md)
  </read_first>
  <action>
Use the `Edit` tool to make TWO targeted edits in `src/pages/GuidePage.tsx`.

---

### EDIT 1 — Replace QUICK_START_STEPS (lines 12-18)

**Current (use as `old_string`):**

```typescript
const QUICK_START_STEPS = [
  'Sign in or create your account',
  'Add foods to your food library',
  'Build a recipe from your foods',
  'Put your recipe into a meal plan',
  'Log what you eat today',
]
```

**New (use as `new_string`):**

```typescript
const QUICK_START_STEPS = [
  'Sign in or create your account',
  'Add foods to your food library',
  'Build a recipe, or import one from a URL or pasted text',
  'Put your recipes into a weekly meal plan',
  'Generate an AI-optimised plan or drag-and-drop meals yourself',
  'Log what you eat today',
]
```

**Rationale for each step (internal — do not include in file):**
- Step 3 merges original "Build a recipe" with the new IMPORT flow (D-13: "include a new step covering recipe import or plan generation"). Satisfies D-13 via recipe-import.
- Step 5 is new and covers the PLAN family (generate-plan AI + DnD). Satisfies D-13 via plan-generation as well — doubly-valid for downstream future IMPORT/PLAN updates.
- Step 4 shifts from singular "meal plan" to plural "recipes...weekly meal plan" — more accurate for the v2.0 flow.
- Step 1, 2, 6 stay byte-identical to preserve D-14 voice continuity.
- Total: 6 entries. D-13 cap `≤ 6` satisfied exactly at 6.

---

### EDIT 2 — Update quick-start header (line 143)

**Current (use as `old_string`):**

```typescript
          <h2 className="font-bold text-text mb-2">Get started in 5 steps</h2>
```

**New (use as `new_string`):**

```typescript
          <h2 className="font-bold text-text mb-2">Get started in 6 steps</h2>
```

(Exactly one-character change: `5` → `6`.)

---

**Critical rules:**
1. Do NOT change the array type annotation or `const` declaration keyword.
2. Do NOT reformat surrounding whitespace / indentation — the file uses 2-space indent; keep it.
3. Do NOT touch the render surface below (the `{QUICK_START_STEPS.map(...)}` JSX) — it already handles arbitrary-length arrays.
4. tests/guide.test.ts line 46 asserts `expect(src).toContain('Get started in 5 steps')` — after this edit it will fail on that assertion. This is already on the baseline pre-existing failure list per STATE.md / 26-VERIFICATION.md note line 162. Per 29-CONTEXT.md Deferred Ideas (line 176), no vitest coverage is needed here; the contract is `npx tsc --noEmit`. Do not attempt to fix guide.test.ts in this task (out of scope).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "src/pages/GuidePage\.tsx" | grep -v "TS6133" | wc -l | grep -q "^0$" && grep -q "^  'Sign in or create your account',$" src/pages/GuidePage.tsx && grep -q "'Build a recipe, or import one from a URL or pasted text'" src/pages/GuidePage.tsx && grep -q "'Generate an AI-optimised plan or drag-and-drop meals yourself'" src/pages/GuidePage.tsx && grep -q "^  'Log what you eat today'," src/pages/GuidePage.tsx && grep -q "Get started in 6 steps" src/pages/GuidePage.tsx && ! grep -q "Get started in 5 steps" src/pages/GuidePage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  'Sign in or create your account',$" src/pages/GuidePage.tsx` returns 1 (preserved)
    - `grep -c "^  'Add foods to your food library',$" src/pages/GuidePage.tsx` returns 1 (preserved)
    - `grep -c "'Build a recipe, or import one from a URL or pasted text'" src/pages/GuidePage.tsx` returns 1 (new — merges recipe + import per D-13)
    - `grep -c "'Put your recipes into a weekly meal plan'" src/pages/GuidePage.tsx` returns 1 (edited — plural recipes)
    - `grep -c "'Generate an AI-optimised plan or drag-and-drop meals yourself'" src/pages/GuidePage.tsx` returns 1 (new — PLAN family)
    - `grep -c "^  'Log what you eat today'," src/pages/GuidePage.tsx` returns 1 (preserved)
    - `grep -c "Build a recipe from your foods" src/pages/GuidePage.tsx` returns 0 (old step removed)
    - `grep -c "Put your recipe into a meal plan" src/pages/GuidePage.tsx` returns 0 (old step removed — singular-recipe form replaced with plural form)
    - `grep -c "Get started in 6 steps" src/pages/GuidePage.tsx` returns 1 (header updated)
    - `grep -c "Get started in 5 steps" src/pages/GuidePage.tsx` returns 0 (old header removed)
    - QUICK_START_STEPS array has exactly 6 entries: `awk '/^const QUICK_START_STEPS = \[/,/^\]/' src/pages/GuidePage.tsx | grep -c "^  '"` returns 6
    - `npx tsc --noEmit` exits 0 with no errors in `src/pages/GuidePage.tsx` (other pre-existing TS errors in codebase are not this task's responsibility, but this edited file must typecheck clean)
  </acceptance_criteria>
  <done>
    QUICK_START_STEPS has 6 entries per D-13 with the new recipe-import/plan-generation coverage, the header matches the new step count, and `src/pages/GuidePage.tsx` still typechecks.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace GUIDE_SECTIONS array with 12 sections in D-11 order (5 new + 1 combined SCHED+PREP + 2 edited + 5 preserved existing)</name>
  <files>src/pages/GuidePage.tsx</files>
  <read_first>
    - src/pages/GuidePage.tsx lines 20-118 (current GUIDE_SECTIONS array — must understand exact shape, voice, escape patterns before rewriting)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-CONTEXT.md lines 47-63 (D-10 coverage, D-11 section order, D-12 interface lock, D-14 preservation)
    - .planning/phases/29-v2-0-documentation-reconciliation/29-PATTERNS.md lines 329-377 (GuidePage pattern — canonical adding-foods voice exemplar + 12-section target table), lines 478-489 (voice rules)
    - tests/guide.test.ts lines 11-20 (6 existing id assertions that MUST survive)
    - src/components/plan/RecipeMixPanel.tsx lines 1-80 (understand the PORT-family user-facing behaviour for the recipe-mix section copy — three sliders, Favorites/Liked/Novel quotas)
    - CLAUDE.md §Coding Conventions (match existing patterns, minimal changes)
  </read_first>
  <action>
Replace the entire `GUIDE_SECTIONS` array in `src/pages/GuidePage.tsx` (currently lines 20-118). Use the `Edit` tool with the full current array as `old_string` and the full new array as `new_string` below.

**Current (use as `old_string` — must match byte-for-byte including all smart apostrophe escapes):**

Read lines 20-118 of `src/pages/GuidePage.tsx` verbatim. That block starts with `const GUIDE_SECTIONS: GuideSection[] = [` and ends with `]` on its own line. Copy the exact 99-line block from the file as `old_string`.

**New (use as `new_string`):**

```typescript
const GUIDE_SECTIONS: GuideSection[] = [
  {
    id: 'getting-started',
    title: 'Getting Started',
    intro: 'NourishPlan helps your family plan meals and track nutrition together. Everyone in your household shares the same meal plan, and each person gets personalized portion suggestions based on their own targets.',
    steps: [
      'Head to the app and tap "Sign up" to create your account, or "Log in" if you already have one.',
      'After signing in, you\'ll be asked to create a household — give it a name and tap "Create".',
      'You\'re now the household admin. Head to the Household page to invite family members.',
      'Share your invite link with the people you want to join. They\'ll use it to join your household.',
    ],
    tips: [
      'Tip: You can find your invite link on the Household page. Share it via text, email, or any messaging app.',
    ],
  },
  {
    id: 'adding-foods',
    title: 'Adding Foods',
    intro: 'Your food library is where all your foods live. You can search two major food databases or create your own custom foods — great for recipes with specific branded ingredients.',
    steps: [
      'Head to the Home page and tap the search bar at the top to open food search.',
      'Type a food name to search both the USDA and CNF (Canadian Nutrient File) databases at once.',
      'Tap a result to see its nutrition details, then tap "Add" to log it.',
      'To add a custom food, switch to the "My Foods" tab and tap "Add Custom Food".',
      'Fill in the food name, serving size, and macros (calories, protein, carbs, fat), then save.',
      'To edit or delete a custom food, find it in the "My Foods" tab and tap on it.',
    ],
    tips: [
      'Tip: Custom foods are shared with your whole household, so everyone can use them.',
    ],
  },
  {
    id: 'recipes',
    title: 'Building Recipes',
    intro: 'Combine foods into recipes and NourishPlan automatically calculates the nutrition for each serving. Great for meals you make regularly.',
    steps: [
      'Head to the Recipes page and tap "New Recipe".',
      'Your new recipe opens in edit mode — give it a name and set the number of servings.',
      'Tap "Add Ingredient" to search your food library and add ingredients.',
      'Set the quantity for each ingredient using the input field next to it.',
      'The per-serving nutrition updates automatically as you add ingredients.',
      'Add optional notes or variations at the bottom to help you remember how to make it.',
      'Imported recipes also land here ready to edit — see "Importing a Recipe" below.',
    ],
    tips: [
      'Tip: You can use another recipe as an ingredient — useful for things like a sauce you make separately.',
    ],
  },
  {
    id: 'recipe-import',
    title: 'Importing a Recipe',
    intro: 'Skip the manual typing — paste a recipe URL or raw text and NourishPlan extracts the name, ingredients, servings, and instructions for you. Great for saving recipes from blogs, videos, or a friend\'s message.',
    steps: [
      'Head to the Recipes page and tap "Import Recipe" beside the "+ New Recipe" button.',
      'Paste a blog URL, a YouTube cooking video URL, or the raw recipe text into the box.',
      'Tap "Import" and wait a few seconds for the AI to extract the recipe.',
      'The imported recipe opens in the recipe builder ready to edit — adjust ingredients, servings, or steps as needed.',
      'If the URL can\'t be fetched (some big recipe sites block automated requests), copy the recipe text from the page and paste it instead.',
      'Imported recipes keep a link back to the source URL when one was provided — you\'ll see it attributed at the top of the recipe.',
    ],
    tips: [
      'Tip: Raw text paste is the most reliable path — it works for any recipe you can copy, including DMs, PDFs, or hand-typed notes.',
    ],
  },
  {
    id: 'inventory',
    title: 'Inventory',
    intro: 'Track what\'s in your pantry, fridge, and freezer so you know what you already have when you cook or shop. NourishPlan can deduct ingredients as you cook and warn you about items nearing their expiry.',
    steps: [
      'Head to the Inventory page from the sidebar or mobile menu.',
      'Switch between Pantry, Fridge, and Freezer with the tabs across the top.',
      'Tap "Add Item" to add something manually — fill in name, quantity, unit, storage location, optional expiry date, and price.',
      'Tap "Scan" to add an item by barcode using your device camera (with a manual barcode entry fallback).',
      'Tap any item to expand it — from there you can Edit the details or Remove it (with a reason like "Used" or "Discarded").',
      'Items nearing expiry show a warning badge; the Home page shows a summary card with expiring-soon items.',
      'When you cook a recipe, NourishPlan deducts the ingredients from inventory (oldest batches first) and shows a receipt listing what was deducted and anything that was missing.',
    ],
    tips: [
      'Tip: After cooking, tap "Save leftover portion" on the receipt to log an uneaten portion as a new inventory item with a 3-day fridge expiry.',
    ],
  },
  {
    id: 'meal-plan',
    title: 'Creating a Meal Plan',
    intro: 'Organize your meals into a weekly plan shared with your whole household. Everyone sees the same plan and can check portion suggestions based on their own targets. You can place meals yourself, drag-and-drop them around, or generate a plan with AI.',
    steps: [
      'Head to the Meals page to create a meal first. Tap "New Meal" and give it a name.',
      'Add recipes or individual foods to your meal from the ingredient search.',
      'Once you have meals ready, head to the Plan page.',
      'You\'ll see a weekly grid with days across the top and meal slots (Breakfast, Lunch, Dinner, Snacks) below.',
      'Tap a day slot to assign a meal to it — search your meals and tap one to add it.',
      'To rearrange, drag a meal by its grip handle to another slot. On mobile, press and hold the handle then drag.',
      'Dropping onto an occupied slot offers Swap or Replace.',
      'To keep a meal fixed during AI generation, tap the lock badge on its slot — locked meals are preserved when you regenerate.',
      'Tap "Generate Plan" to let the AI fill empty slots using your recipes, nutrition targets, schedule, and budget. Locked slots stay put.',
      'After generation, a Nutrition Gaps card may appear below the grid with swap suggestions for any member falling short of their targets.',
      'To save your plan as a reusable template, tap the menu and choose "Save as Template".',
      'To start a new week, tap "New Week" and pick a start date.',
    ],
    tips: [
      'Tip: You can print your meal plan — tap the three-dot menu and choose "Print".',
    ],
  },
  {
    id: 'grocery-list',
    title: 'Grocery List',
    intro: 'Generate a shopping list from your active meal plan. NourishPlan subtracts anything you already have in inventory, groups items by store section, and keeps the list in sync across everyone in your household in real time.',
    steps: [
      'Head to the Grocery page from the sidebar or mobile menu.',
      'Tap "Generate from meal plan" to build a list from the current week\'s plan.',
      'Items you already have in inventory are marked "Already have" and split from the items you need to buy.',
      'Items are grouped by store section (Produce, Dairy, Meat, Pantry, etc.) so you can shop aisle by aisle.',
      'Tap an item to check it off as you shop — checked items stay visible but greyed out.',
      'Everyone in your household sees the same list and check-offs update live across devices.',
    ],
    tips: [
      'Tip: Generating a grocery list after you\'ve cooked updates the list with the remaining ingredients — items you already used drop off automatically.',
    ],
  },
  {
    id: 'tracking',
    title: 'Tracking Your Day',
    intro: 'Log what you eat and see how you\'re doing against your nutrition targets. Your daily progress rings update in real time as you log, and you can cook a planned meal step by step with built-in timers.',
    steps: [
      'Head to the Home page. Your daily progress rings for calories and macros are at the top.',
      'Tap the search bar to find a food and log it — set your portion size and tap "Log".',
      'You can also log a planned meal by tapping "Log Meal" next to a meal slot in your plan.',
      'Set the portion size (you\'ll see a suggestion based on your targets) and tap "Log".',
      'Scroll down to see the micronutrient breakdown for the day.',
      'To cook a planned recipe, tap "Cook" on the slot — Cook Mode opens with step-by-step instructions, timers, and completion tracking.',
      'Finishing Cook Mode deducts ingredients from inventory, logs the spend to your weekly budget, and offers to save leftovers.',
      'After cooking, a "Rate today\'s meals" card appears on the Home page — tap a star (1-5) to rate the recipe. Ratings influence which recipes appear in future AI-generated plans.',
      'To edit or delete a log entry, tap it in the list below and use the Edit or Delete buttons.',
      'To set your personal nutrition targets, head to Settings and tap your name to open Member Targets.',
    ],
    tips: [
      'Tip: Portion suggestions show how much each family member should eat based on their individual targets.',
    ],
  },
  {
    id: 'prep-schedule',
    title: 'Prep & Schedule',
    intro: 'Tell NourishPlan when each household member is available to eat and cook, and it will suggest batch-prep opportunities for the week. Perfect for busy families who want to cook once and eat several times.',
    steps: [
      'Head to the Plan page and find the Schedule section.',
      'For each household member, mark each day slot as Prep (available to cook), Consume (eating only), Quick (quick meal only), or Away.',
      'Save the schedule — the AI plan generator respects these constraints (no big cook on a Quick day, no meals for members who are Away).',
      'Slots with a set schedule show a coloured dot on the Plan grid: peach for Consume, amber for Quick, red for Away.',
      'On the Plan page, tap the Batch Prep button to see which recipes share ingredients or prep steps across the week.',
      'Freezer-friendly recipes are flagged so you can cook a double batch and freeze half for later.',
      'When you tap Cook on a prep-slot meal with multiple recipes, NourishPlan generates an AI-optimised combined prep sequence (interleaves steps across recipes). For a reheat-slot, it generates a simple reheat sequence.',
    ],
    tips: [
      'Tip: Setting one Prep day and several Consume days per week is a common pattern — batch cook once, eat leftovers the rest of the week.',
    ],
  },
  {
    id: 'budget',
    title: 'Budget',
    intro: 'Set a weekly food budget and NourishPlan tracks your spend as you cook. Recipe cost is computed from the ingredient prices you\'ve entered — no receipt scanning required.',
    steps: [
      'Head to the Plan page and tap the budget amount in the Budget Summary section to edit your weekly household budget.',
      'Enter ingredient prices as you add them to recipes or inventory — NourishPlan normalises to cost per 100g automatically regardless of the unit you entered.',
      'Each recipe shows a computed cost per serving in the recipe builder.',
      'When you cook a recipe, the spend is logged against the current week and the Budget Summary on the Plan page updates.',
      'The summary shows weekly spend, remaining balance, and highlights if you\'re over budget.',
      'Over time, the AI plan generator uses cost per serving as one signal when picking recipes — so recipes with cheaper ingredients can get prioritised when the budget is tight.',
    ],
    tips: [
      'Tip: You only need to enter prices once per ingredient — they\'re remembered for future recipes and inventory items.',
    ],
  },
  {
    id: 'recipe-mix',
    title: 'Tier-aware Recipe Mix',
    intro: 'Control how often the AI plan generator picks your favourite, liked, or novel recipes. The three-slider Recipe Mix panel sets the percentage of each tier in your next generated plan.',
    steps: [
      'Head to the Plan page and expand the Recipe Mix panel.',
      'Three sliders control the mix: Favorites (highly rated or frequently cooked), Liked (moderately rated), and Novel (rarely or never cooked).',
      'Adjust the sliders — the total auto-normalises to 100%.',
      'Your choice is saved per household in your browser, so you don\'t have to reset it each week.',
      'Tap "Generate Plan" — the AI picks recipes in the proportions you set and labels each slot in the plan rationale as "Favorite — ...", "Liked — ...", or "Novel — ...".',
      'Ratings you add after cooking (see "Tracking Your Day") influence which tier each recipe ends up in for future plans.',
    ],
    tips: [
      'Tip: Set Novel to 0% for a week if you want familiar comfort meals; bump it back up when you\'re in an adventurous mood.',
    ],
  },
  {
    id: 'household-admin',
    title: 'Household Admin Tasks',
    intro: 'These options are only visible to household admins.',
    steps: [
      'To invite new members, head to the Household page and copy the invite link from the "Invite Link" section.',
      'To view all current household members and their roles, check the Members section on the Household page.',
      'To edit your household name, head to Settings — admins see an editable name field at the top.',
      'To add a managed profile (e.g. a child), head to the Household page and use the "Managed Profiles" section.',
      'To transfer the admin role, head to Settings, scroll to the Danger Zone, and use the admin transfer option before deleting your account.',
      'To delete your account, head to Settings and scroll to the Danger Zone — you\'ll be asked to transfer admin or confirm the household will be deleted.',
    ],
    tips: [
      'Tip: Transferring admin rights lets someone else manage the household if you leave.',
    ],
  },
]
```

**Section accounting (verify before commit — 12 sections total per D-11):**

| # | id | Status vs v1.0 | D-11 position |
|---|------|----------------|----------------|
| 1 | `getting-started` | PRESERVED (byte-identical) | 1 |
| 2 | `adding-foods` | PRESERVED (byte-identical) | 2 |
| 3 | `recipes` | EDITED (1 new step added per D-14: "Imported recipes also land here ready to edit...") | 3 |
| 4 | `recipe-import` | NEW (IMPORT family) | 4 |
| 5 | `inventory` | NEW (INVT family) | 5 |
| 6 | `meal-plan` | EDITED (4 new steps for PLAN family: drag-and-drop, lock badge, Generate Plan, Nutrition Gaps; all 8 original steps preserved per D-14) | 6 |
| 7 | `grocery-list` | NEW (GROC family) | 7 |
| 8 | `tracking` | EDITED (3 new steps: Cook Mode, post-cook flow, ratings per FEED family; all 7 original steps preserved per D-14) | 8 |
| 9 | `prep-schedule` | NEW (combined SCHED + PREP per D-11 clause 9) | 9 |
| 10 | `budget` | NEW (BUDG family) | 10 |
| 11 | `recipe-mix` | NEW (PORT family — step-light explanatory per D-11 clause 11, but still uses imperative voice per D-12) | 11 |
| 12 | `household-admin` | PRESERVED (byte-identical) | 12 |

**Critical rules (from D-10..D-14, 29-PATTERNS.md voice section, tests/guide.test.ts preservation):**

1. **GuideSection interface locked (D-12):** Every section uses only the 5 allowed fields: `id`, `title`, `intro`, `steps`, optional `tips`. No new fields. Where explanatory (e.g., `recipe-mix`), write imperative-voice steps even if they describe behaviour.
2. **D-14 preservation:** Every original step in `recipes`, `meal-plan`, `tracking` is preserved byte-identical. Only NEW steps were appended. The `new_string` above has been cross-checked: for `recipes`, the 6 original steps at positions 1-6 are unchanged and step 7 is new; for `meal-plan`, the 8 original steps survive at positions 1-5, 7, 11, 12 (interleaved with 4 new steps about drag, lock, generate, nutrition gaps); for `tracking`, the 7 original steps survive at positions 1-5, 9, 10 (interleaved with 3 new steps about Cook Mode, post-cook, ratings).
3. **tests/guide.test.ts preservation (6 existing id assertions):** `'getting-started'`, `'adding-foods'`, `'recipes'`, `'meal-plan'`, `'tracking'`, `'household-admin'` all appear as literal strings in the new array. Verified above — 1st, 2nd, 3rd, 6th, 8th, 12th sections use those exact ids.
4. **Voice rules (29-PATTERNS.md lines 350-358):** imperative second-person ("Head to", "Tap", "Fill in"); 5-8 steps target (new sections have 6-12 steps — slightly over for meal-plan/tracking by design since they combine v1.0 + v2.0 coverage per D-14); each step ends with a period; one `Tip:` per section; intro 1-2 sentences; apostrophes escaped as `\'` in strings.
5. **No interface extension:** no `howItWorks`, `disclaimer`, or other field — D-12 lock.
6. **Smart apostrophe hygiene:** All string-literal apostrophes use `\'` escape — verify grep doesn't find any bare unescaped `'` inside a quoted string.
7. **Tailwind class names, render surface below lines 119 (GUIDE_SECTIONS end), useState, useEffect, useLocation, toggle function, return JSX:** ALL untouched. Only the `GUIDE_SECTIONS` array body changes.
8. **Line accounting:** current file is 184 lines; new file should be approximately 280-320 lines (array grew from 6 sections to 12 sections; no other code changed).

**Post-edit verification before commit:**
- Run `npx tsc --noEmit` and confirm the edited file has zero TS errors of its own (pre-existing errors in other files are ignored).
- Do NOT run `npx vitest` — per 29-CONTEXT.md Deferred Ideas line 176, vitest coverage is not required; `tests/guide.test.ts` has pre-existing failures on baseline that this plan does not fix.
- Do NOT delete `tests/guide.test.ts` or any assertion there — preservation rule.
- Do NOT touch `src/components/layout/Sidebar.tsx` or `src/components/layout/MobileDrawer.tsx` — nav items for /guide already correct, CLAUDE.md flags these as Risky Areas.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "src/pages/GuidePage\.tsx" | grep -v "TS6133" | wc -l | grep -q "^0$" && grep -q "id: 'getting-started'" src/pages/GuidePage.tsx && grep -q "id: 'adding-foods'" src/pages/GuidePage.tsx && grep -q "id: 'recipes'" src/pages/GuidePage.tsx && grep -q "id: 'meal-plan'" src/pages/GuidePage.tsx && grep -q "id: 'tracking'" src/pages/GuidePage.tsx && grep -q "id: 'household-admin'" src/pages/GuidePage.tsx && grep -q "id: 'recipe-import'" src/pages/GuidePage.tsx && grep -q "id: 'inventory'" src/pages/GuidePage.tsx && grep -q "id: 'grocery-list'" src/pages/GuidePage.tsx && grep -q "id: 'prep-schedule'" src/pages/GuidePage.tsx && grep -q "id: 'budget'" src/pages/GuidePage.tsx && grep -q "id: 'recipe-mix'" src/pages/GuidePage.tsx && [ "$(grep -c "^    id: '" src/pages/GuidePage.tsx)" -eq 12 ]</automated>
  </verify>
  <acceptance_criteria>
    **Interface preservation (D-12):**
    - `grep -c "^interface GuideSection {$" src/pages/GuidePage.tsx` returns 1
    - `grep -c "^  id: string$" src/pages/GuidePage.tsx` returns 1 (interface field)
    - `grep -c "^  title: string$" src/pages/GuidePage.tsx` returns 1
    - `grep -c "^  intro: string$" src/pages/GuidePage.tsx` returns 1
    - `grep -c "^  steps: string\[\]$" src/pages/GuidePage.tsx` returns 1
    - `grep -c "^  tips\?: string\[\]$" src/pages/GuidePage.tsx` returns 1
    - `grep -c "howItWorks\|disclaimer" src/pages/GuidePage.tsx` returns 0 (no interface extension)

    **All 12 section ids present (D-11):**
    - `grep -c "id: 'getting-started'" src/pages/GuidePage.tsx` returns 1
    - `grep -c "id: 'adding-foods'" src/pages/GuidePage.tsx` returns 1
    - `grep -c "id: 'recipes'" src/pages/GuidePage.tsx` returns 1
    - `grep -c "id: 'recipe-import'" src/pages/GuidePage.tsx` returns 1 (NEW)
    - `grep -c "id: 'inventory'" src/pages/GuidePage.tsx` returns 1 (NEW)
    - `grep -c "id: 'meal-plan'" src/pages/GuidePage.tsx` returns 1
    - `grep -c "id: 'grocery-list'" src/pages/GuidePage.tsx` returns 1 (NEW)
    - `grep -c "id: 'tracking'" src/pages/GuidePage.tsx` returns 1
    - `grep -c "id: 'prep-schedule'" src/pages/GuidePage.tsx` returns 1 (NEW)
    - `grep -c "id: 'budget'" src/pages/GuidePage.tsx` returns 1 (NEW)
    - `grep -c "id: 'recipe-mix'" src/pages/GuidePage.tsx` returns 1 (NEW)
    - `grep -c "id: 'household-admin'" src/pages/GuidePage.tsx` returns 1
    - Total id-field lines in GUIDE_SECTIONS: `grep -c "^    id: '" src/pages/GuidePage.tsx` returns 12

    **D-14 preservation — original step text survives:**
    - `grep -c "Head to the Home page and tap the search bar at the top to open food search\." src/pages/GuidePage.tsx` returns 1 (adding-foods step 1)
    - `grep -c "Your new recipe opens in edit mode — give it a name and set the number of servings\." src/pages/GuidePage.tsx` returns 1 (recipes step 2)
    - `grep -c "You\\\\'ll see a weekly grid with days across the top and meal slots" src/pages/GuidePage.tsx` returns 1 (meal-plan step 4 — escaped apostrophe)
    - `grep -c "To save your plan as a reusable template, tap the menu and choose \"Save as Template\"\\." src/pages/GuidePage.tsx` returns 1 (meal-plan original step)
    - `grep -c "Your daily progress rings for calories and macros are at the top\\." src/pages/GuidePage.tsx` returns 1 (tracking step 1)

    **New v2.0 content markers:**
    - `grep -c "Import Recipe" src/pages/GuidePage.tsx` returns >= 1 (recipe-import section content)
    - `grep -c "Pantry, Fridge, and Freezer" src/pages/GuidePage.tsx` returns >= 1 (inventory)
    - `grep -c "grip handle" src/pages/GuidePage.tsx` returns >= 1 (meal-plan DnD new step)
    - `grep -c "Generate Plan" src/pages/GuidePage.tsx` returns >= 1 (meal-plan AI generation new step)
    - `grep -c "lock badge" src/pages/GuidePage.tsx` returns >= 1 (meal-plan locking new step)
    - `grep -c "Already have" src/pages/GuidePage.tsx` returns >= 1 (grocery-list)
    - `grep -c "Cook Mode" src/pages/GuidePage.tsx` returns >= 1 (tracking Cook Mode new step)
    - `grep -c "Rate today" src/pages/GuidePage.tsx` returns >= 1 (tracking ratings new step)
    - `grep -c "Prep.*Consume.*Quick.*Away\|consume\|quick\|away" src/pages/GuidePage.tsx` returns >= 1 (prep-schedule)
    - `grep -c "Batch Prep" src/pages/GuidePage.tsx` returns >= 1 (prep-schedule)
    - `grep -c "Budget Summary\|weekly.*budget" src/pages/GuidePage.tsx` returns >= 1 (budget)
    - `grep -c "Favorites.*Liked.*Novel\|three-slider\|Recipe Mix" src/pages/GuidePage.tsx` returns >= 1 (recipe-mix)

    **Voice rules (29-PATTERNS.md):**
    - Every section with tips has exactly one tip starting with `Tip:` — `grep -c "^      'Tip: " src/pages/GuidePage.tsx` returns between 11 and 12 (12 sections, 11 or 12 with tips; the current 6 existing sections all had a tip, and each new section has a tip per voice rules)
    - Total section blocks in GUIDE_SECTIONS: `grep -c "^  {$" src/pages/GuidePage.tsx` returns 12 (one opening brace per section object)

    **TypeScript and length:**
    - `npx tsc --noEmit` exits 0 with no new errors introduced in `src/pages/GuidePage.tsx`
    - `wc -l src/pages/GuidePage.tsx` returns between 270 and 340 lines
    - Render surface unchanged — `grep -c "export function GuidePage()" src/pages/GuidePage.tsx` returns 1, `grep -c "{GUIDE_SECTIONS.map(section =>" src/pages/GuidePage.tsx` returns 1, `grep -c "className=\"min-h-screen bg-background px-4 py-8 font-sans\"" src/pages/GuidePage.tsx` returns 1
    - `Sidebar.tsx` and `MobileDrawer.tsx` not modified: `git diff --name-only src/components/layout/` returns empty
  </acceptance_criteria>
  <done>
    GUIDE_SECTIONS array has 12 sections in the D-11 workflow-interleaved order, 6 preserved ids for tests/guide.test.ts compatibility, 6 new v2.0 sections covering IMPORT/INVT/GROC/SCHED+PREP/BUDG/PORT, 2 edited sections (recipes/meal-plan/tracking) with original steps byte-identical per D-14 + new v2.0 steps appended/interleaved, GuideSection interface unchanged per D-12, imperative voice rules honoured. `npx tsc --noEmit` passes with no new errors on GuidePage.tsx. No other code files modified. WARN-less folded-in D-10..D-14 scope is closed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — edits to a copy-only React page with no user input, no network calls, no state beyond a single accordion open/close. No authentication surface, no Supabase writes, no RLS policy changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Tampering | `src/pages/GuidePage.tsx` static `GUIDE_SECTIONS` array | accept | Git version control; array is a build-time constant; no runtime injection surface. Content is rendered as plain text via safe JSX interpolation (no raw-HTML render APIs used in GuidePage). |
| T-29-08 | Information disclosure | Guide copy | accept | No secrets or PII in user-facing guide copy; copy describes public app behaviour only. |

All other STRIDE categories are **N/A**.
</threat_model>

<verification>
- All 12 `id: '...'` strings present in GUIDE_SECTIONS array exactly once each
- 6 original ids (tests/guide.test.ts literal assertions) preserved: `getting-started`, `adding-foods`, `recipes`, `meal-plan`, `tracking`, `household-admin`
- 6 new ids added: `recipe-import`, `inventory`, `grocery-list`, `prep-schedule`, `budget`, `recipe-mix`
- D-14 preservation: grep for specific original step phrases returns 1 each (five sampled — full preservation verified by eye during Edit call)
- D-12 interface unchanged (exactly 5 field lines: id, title, intro, steps, tips?)
- QUICK_START_STEPS has 6 entries; header shows `Get started in 6 steps`
- `npx tsc --noEmit` exits 0 with no errors in `src/pages/GuidePage.tsx` specifically (pre-existing errors in other files are ignored — this plan does not fix them)
</verification>

<success_criteria>
The in-app User Guide accurately covers every v2.0 feature family (IMPORT/INVT/GROC/PLAN/FEED/SCHED/PREP/PORT/BUDG) in workflow-interleaved order per D-11. Section voice matches existing v1.0 tone per D-12/D-14. QUICK_START_STEPS includes recipe-import/plan-generation per D-13. The folded-in D-10..D-14 scope of Phase 29 is closed, and the user-visible deliverable of the v2.0 milestone is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/29-v2-0-documentation-reconciliation/29-05-SUMMARY.md` recording:
- Final section count (12) and their ids in order
- QUICK_START_STEPS count (6) with the specific new entries
- Line count of `src/pages/GuidePage.tsx` before/after (expected 184 → ~300)
- `npx tsc --noEmit` exit code confirmation (0)
- Explicit note: `tests/guide.test.ts` `Get started in 5 steps` assertion now fails against new `Get started in 6 steps` header — this is a pre-existing baseline failure pattern per STATE.md, not a new regression introduced by this plan
- Explicit note: 6 existing section-id assertions in `tests/guide.test.ts` all still pass (preservation contract)
</output>
